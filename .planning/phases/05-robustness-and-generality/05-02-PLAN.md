---
phase: 05-robustness-and-generality
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - TRC-23-02333/summarize_trace_sl_rcss.py
  - TRC-23-02333/test_summarize_trace_sl_rcss.py
autonomous: true
requirements: [ROBUST-01, ROBUST-02, ROBUST-03, ROBUST-04, ROBUST-05, ROBUST-06]
must_haves:
  truths:
    - "D-01/D-02: Aggregated robustness evidence is generated from artifact CSVs and focuses on held-out GLS/MAP rows."
    - "D-03: Existing method/layout labels are preserved while grouping by new condition columns."
    - "D-10: Candidate-count sensitivity aggregation preserves 50/100/200/500-style candidate_count values and runtime rows when present."
    - "D-12/D-13: Summaries group by robustness/candidate/split condition columns before computing means, deltas, winners, and paired tests."
  artifacts:
    - path: "TRC-23-02333/summarize_trace_sl_rcss.py"
      provides: "Condition-aware aggregation, robustness delta summaries, and candidate/runtime grouping"
      exports: ["main"]
    - path: "TRC-23-02333/test_summarize_trace_sl_rcss.py"
      provides: "Regression tests for condition grouping and candidate_count preservation"
  key_links:
    - from: "TRC-23-02333/summarize_trace_sl_rcss.py"
      to: "TRC-23-02333/trace_sl_results/*/gls_map_layout_summary.csv"
      via: "grouping with robustness condition columns"
      pattern: "robustness_family|robustness_condition|split_mode|candidate_count"
    - from: "TRC-23-02333/summarize_trace_sl_rcss.py"
      to: "TRC-23-02333/trace_sl_results/*/runtime_candidate_sensitivity.csv"
      via: "candidate_count runtime aggregation"
      pattern: "candidate_count"
---

<objective>
Make the existing TRACE-SL summarizer condition-aware so Phase 5 robustness and candidate-count settings are never collapsed into one aggregate mean.

Purpose: D-13 requires grouping by robustness, candidate, and split condition columns before comparisons; ROBUST-01..06 need aggregate evidence tables that preserve stress settings.
Output: Updated summarizer and tests that prove condition columns survive combined metrics, layout summaries, paired deltas/tests, selected-source summaries, candidate diagnostics, runtime summaries, and Markdown output.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-robustness-and-generality/05-CONTEXT.md
@.planning/phases/04-core-experiment-evidence/04-VERIFICATION.md
@TRC-23-02333/summarize_trace_sl_rcss.py
@TRC-23-02333/test_summarize_trace_sl_rcss.py
@TRC-23-02333/transparent_estimator_eval.py

<interfaces>
Existing summarizer anchors:
- `collect_input_frames(input_roots)` reads seed-level `metrics.csv`, `certificate_correlations.csv`, and `rcss_candidates.csv` and adds `candidate_count` from `candidates_<N>` roots.
- Main aggregation currently builds `evidence_group_cols = ["budget"]`, optionally appending `candidate_count`.
- Paired comparisons use a pivot indexed by `split_seed` plus evidence grouping columns.
- Runtime sensitivity already groups by `candidate_count` plus optional `budget`, `source`, and `status`.
New evaluator columns from Plan 05-01 must be treated as optional grouping columns: `robustness_family`, `robustness_condition`, `failure_rate`, `noise_scale`, `missing_rate`, `missing_block_steps`, `cost_proxy`, `cost_budget`, `split_mode`, and `candidate_count`.
</interfaces>
</context>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 5 | Show usefulness under robustness/generalization settings. | 05-01..05-04 | COVERED | This plan makes evidence aggregation condition-aware. |
| REQ | ROBUST-01 | Sensor failure degradation. | 05-01, 05-02, 05-03, 05-04 | COVERED | Sensor-failure condition columns grouped here. |
| REQ | ROBUST-02 | Observation noise degradation. | 05-01, 05-02, 05-03, 05-04 | COVERED | Noise condition columns grouped here. |
| REQ | ROBUST-03 | Missing readings/time blocks. | 05-01, 05-02, 05-03, 05-04 | COVERED | Missingness condition columns grouped here. |
| REQ | ROBUST-04 | Nonuniform costs/proxy. | 05-01, 05-02, 05-03, 05-04 | COVERED | Cost proxy/cost budget grouped here. |
| REQ | ROBUST-05 | Temporal distribution shift. | 05-01, 05-02, 05-03, 05-04 | COVERED | `split_mode` grouped here. |
| REQ | ROBUST-06 | Candidate-count performance/runtime. | 05-02, 05-03, 05-04 | COVERED | Candidate count and runtime grouping covered here. |
| CONTEXT | D-01 | Artifact-backed evidence. | 05-02 | COVERED | Summarizer consumes artifact CSVs only. |
| CONTEXT | D-02 | Held-out GLS/MAP main evidence. | 05-02 | COVERED | GLS/MAP filtered summaries remain primary. |
| CONTEXT | D-03 | Stable labels. | 05-02 | COVERED | No label renaming. |
| CONTEXT | D-04 | Stress-test not universal proof. | 05-04 | COVERED | Wording finalized later. |
| CONTEXT | D-10 | Candidate 50/100/200/500 coverage or caveat. | 05-02, 05-03, 05-04 | COVERED | Grouping preserves candidate counts. |
| CONTEXT | D-12 | Condition columns. | 05-02 | COVERED | Optional columns preserved. |
| CONTEXT | D-13 | Do not collapse stress settings. | 05-02 | COVERED | Main purpose of this plan. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add condition-aware grouping helpers and tests</name>
  <files>TRC-23-02333/summarize_trace_sl_rcss.py, TRC-23-02333/test_summarize_trace_sl_rcss.py</files>
  <read_first>
    <file>.planning/phases/05-robustness-and-generality/05-CONTEXT.md</file>
    <file>TRC-23-02333/summarize_trace_sl_rcss.py</file>
    <file>TRC-23-02333/test_summarize_trace_sl_rcss.py</file>
  </read_first>
  <behavior>
    - Given a metrics DataFrame with two `robustness_condition` values at the same budget/layout/method, layout summaries contain two separate rows rather than one collapsed mean, per D-13.
    - `candidate_count` remains a grouping key when present, and `split_mode` remains a grouping key when present, per D-10/D-12.
    - Missing optional condition columns in older Stage 12/13 artifacts do not break aggregation.
  </behavior>
  <action>Add a helper such as `condition_group_columns(frame)` that returns `budget` plus any present condition columns in a stable order: `candidate_count`, `robustness_family`, `robustness_condition`, `failure_rate`, `noise_scale`, `missing_rate`, `missing_block_steps`, `cost_proxy`, `cost_budget`, and `split_mode`. Update or add direct tests in `test_summarize_trace_sl_rcss.py` using synthetic DataFrames to prove separate condition rows and backward compatibility with old artifacts. Do not add pytest or new package dependencies; keep the existing direct Python test style.</action>
  <verify>
    <automated>python -m py_compile TRC-23-02333/summarize_trace_sl_rcss.py TRC-23-02333/test_summarize_trace_sl_rcss.py &amp;&amp; python TRC-23-02333/test_summarize_trace_sl_rcss.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before implementation and pass after implementation.
    - Optional condition columns are included only when present.
    - Existing Stage 12/13 aggregation remains runnable with older schemas.
  </acceptance_criteria>
  <done>Summarizer has a reusable condition grouping contract for Phase 5 artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply condition grouping to summaries, deltas, winners, candidates, and Markdown</name>
  <files>TRC-23-02333/summarize_trace_sl_rcss.py, TRC-23-02333/test_summarize_trace_sl_rcss.py</files>
  <read_first>
    <file>.planning/phases/05-robustness-and-generality/05-CONTEXT.md</file>
    <file>.planning/phases/04-core-experiment-evidence/04-REVIEW.md</file>
    <file>TRC-23-02333/summarize_trace_sl_rcss.py</file>
  </read_first>
  <behavior>
    - `combined_metrics.csv` preserves condition columns from inputs.
    - `gls_map_layout_summary.csv`, `gls_map_delta_summary.csv`, `gls_map_paired_delta_tests.csv`, `gls_map_ablation_summary.csv`, `gls_map_per_split_winners.csv`, and `gls_map_win_counts.csv` include present condition columns.
    - `combined_rcss_candidates.csv`, `rcss_selected_sources.csv`, `candidate_sensitivity_summary.csv`, and `runtime_candidate_sensitivity.csv` include candidate/robustness grouping columns where present.
    - `SUMMARY.md` explains that robustness tables are grouped by condition and are stress-test evidence, not universal robustness proof per D-04.
  </behavior>
  <action>Replace hardcoded `evidence_group_cols = ["budget"]` logic with the new grouping helper for GLS/MAP layout summaries, pivot indexes, paired comparison rows, winner counts, selected-source summaries, candidate summaries, and runtime summaries. Ensure sorting uses only present columns. Add tests that create temporary `seed_*/metrics.csv` and `rcss_candidates.csv` fixtures with two conditions and assert generated output CSVs contain separate rows for each condition. Preserve existing labels and output filenames per D-03 and Phase 4 compatibility.</action>
  <verify>
    <automated>python -m py_compile TRC-23-02333/summarize_trace_sl_rcss.py TRC-23-02333/test_summarize_trace_sl_rcss.py &amp;&amp; python TRC-23-02333/test_summarize_trace_sl_rcss.py &amp;&amp; python TRC-23-02333/summarize_trace_sl_rcss.py --input-root TRC-23-02333/trace_sl_results/pems7_228_stage13_candidate_sensitivity/candidates_50 TRC-23-02333/trace_sl_results/pems7_228_stage13_candidate_sensitivity/candidates_100 --runtime-root TRC-23-02333/trace_sl_results/pems7_228_stage13_candidate_sensitivity --output-dir /tmp/trace_sl_phase5_summary_check &amp;&amp; python - <<'PY'
import pandas as pd
layout = pd.read_csv('/tmp/trace_sl_phase5_summary_check/gls_map_layout_summary.csv')
assert 'candidate_count' in layout.columns
assert set(layout['candidate_count'].dropna().astype(int)).issuperset({50, 100})
runtime = pd.read_csv('/tmp/trace_sl_phase5_summary_check/runtime_candidate_sensitivity.csv')
assert 'candidate_count' in runtime.columns
print('condition-summary-ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - Existing Phase 4 Stage 13 candidate-count outputs remain grouped by candidate count.
    - New Phase 5 robustness condition columns are not averaged away.
    - Generated Markdown includes condition-aware/stress-test language and no universal robustness proof wording.
  </acceptance_criteria>
  <done>Summarizer can aggregate Phase 5 robustness and candidate-sensitivity artifacts without collapsing stress settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated CSVs -> summarizer | Inputs may mix old schemas without condition columns and new Phase 5 schemas with condition columns. |
| aggregate summaries -> paper claims | Collapsed stress settings can produce misleading robustness claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-02-01 | Tampering | grouping logic | mitigate | Centralize optional condition-column detection and test mixed-condition fixtures. |
| T-05-02-02 | Repudiation | aggregate artifacts | mitigate | Preserve condition columns in output CSVs so each row can be traced to stress settings. |
| T-05-02-03 | Information Disclosure | raw datasets | accept | Summarizer reads result artifacts only, not raw dataset files. |
| T-05-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the task automated checks and confirm `/tmp/trace_sl_phase5_summary_check/gls_map_layout_summary.csv` and `runtime_candidate_sensitivity.csv` preserve candidate_count; synthetic tests must prove robustness condition preservation.
</verification>

<success_criteria>
- ROBUST-01..06 aggregate outputs can represent each stress/candidate/split condition independently.
- Existing Phase 4 aggregate workflows continue to run.
- No stable method or layout labels are renamed.
</success_criteria>

<output>
Create `.planning/phases/05-robustness-and-generality/05-02-SUMMARY.md` when done.
</output>
