---
phase: 05-robustness-and-generality
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - scripts/run_stage14_pems7_228_robustness.sh
  - scripts/run_stage14_candidate_sensitivity_pems7_228.sh
  - TRC-23-02333/trace_sl_results/README.md
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/combined_metrics.csv
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/gls_map_layout_summary.csv
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/gls_map_paired_delta_tests.csv
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/SUMMARY.md
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/combined_metrics.csv
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/runtime_candidate_sensitivity.csv
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/candidate_sensitivity_summary.csv
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/stage14_timing.csv
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/SUMMARY.md
  - TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/candidate_sensitivity_caveat.json
autonomous: true
requirements: [ROBUST-01, ROBUST-02, ROBUST-03, ROBUST-04, ROBUST-05, ROBUST-06]
must_haves:
  truths:
    - "D-01: Stage 14 robustness and candidate-count evidence is generated as CSV/Markdown/script artifacts, not merely launch commands."
    - "D-05/D-06/D-07/D-08/D-09: PeMS7_228 Stage 14 robustness evidence contains held-out GLS/MAP rows for sensor failure, noise, missingness, cost proxy, and chronological split conditions."
    - "D-10: Candidate-count sensitivity evidence contains 50, 100, 200, and 500 performance plus runtime rows, or a machine-readable limited-tractability caveat artifact that the Phase 5 validator checks."
    - "D-12/D-13: Generated aggregate CSVs preserve robustness condition columns and candidate_count/runtime columns for downstream validation."
    - "D-14/D-15: Core Phase 5 completion uses a deterministic PeMS7_228 bundle; external robustness remains documented as optional/supporting."
  artifacts:
    - path: "scripts/run_stage14_pems7_228_robustness.sh"
      provides: "Reproducible PeMS7_228 robustness launcher with DRY_RUN and actual reduced evidence-run modes"
    - path: "scripts/run_stage14_candidate_sensitivity_pems7_228.sh"
      provides: "Reproducible 50/100/200/500 candidate-count and runtime launcher with DRY_RUN, timing, and caveat support"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/combined_metrics.csv"
      provides: "Artifact-backed ROBUST-01..05 held-out metrics with robustness condition columns"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/gls_map_layout_summary.csv"
      provides: "Condition-aware held-out GLS/MAP aggregate layout evidence"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/gls_map_paired_delta_tests.csv"
      provides: "Condition-aware paired delta tests for held-out GLS/MAP evidence"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/SUMMARY.md"
      provides: "Human-readable Stage 14 robustness summary generated from aggregate CSVs"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/combined_metrics.csv"
      provides: "ROBUST-06 candidate-count performance evidence"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/runtime_candidate_sensitivity.csv"
      provides: "ROBUST-06 runtime evidence grouped by candidate_count"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/candidate_sensitivity_summary.csv"
      provides: "ROBUST-06 performance summary for candidate counts"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/stage14_timing.csv"
      provides: "Measured per-count runtime/status rows for candidate counts 50, 100, 200, and 500"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/candidate_sensitivity_caveat.json"
      provides: "Machine-readable limited-tractability caveat only when a 50/100/200/500 run cannot complete"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/SUMMARY.md"
      provides: "Human-readable Stage 14 candidate sensitivity summary generated from aggregate CSVs"
    - path: "TRC-23-02333/trace_sl_results/README.md"
      provides: "Curated Stage 14 inventory and interpretation guidance"
  key_links:
    - from: "scripts/run_stage14_pems7_228_robustness.sh"
      to: "TRC-23-02333/transparent_estimator_eval.py"
      via: "robustness CLI flags"
      pattern: "--robustness-family|--robustness-condition|--failure-rate|--noise-scale|--missing-rate|--missing-block-steps|--split-mode|--cost-proxy"
    - from: "scripts/run_stage14_pems7_228_robustness.sh"
      to: "TRC-23-02333/summarize_trace_sl_rcss.py"
      via: "post-run aggregate summary"
      pattern: "summarize_trace_sl_rcss.py"
    - from: "scripts/run_stage14_candidate_sensitivity_pems7_228.sh"
      to: "TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/stage14_timing.csv"
      via: "measured runtime table"
      pattern: "candidate_count|runtime_seconds|status"
    - from: "TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/candidate_sensitivity_caveat.json"
      to: ".planning/phases/05-robustness-and-generality/validate_phase5_robustness.py"
      via: "ROBUST-06 limited-tractability exception"
      pattern: "allowed_exception|missing_candidate_counts|ROBUST-06"
---

<objective>
Create reproducible Stage 14 launchers, generate deterministic PeMS7_228 Stage 14 robustness and candidate-sensitivity evidence bundles, and register the resulting artifacts in the curated result inventory.

Purpose: ROBUST-01..06 require executable, artifact-backed evidence paths; D-01 forbids treating launch commands as evidence, and D-14 prefers a deterministic PeMS7_228 core robustness bundle over an unbounded multi-dataset sweep.
Output: Stage 14 shell scripts, real aggregate CSV/Markdown artifacts under `TRC-23-02333/trace_sl_results/`, optional machine-readable ROBUST-06 caveat when tractability limits a candidate count, and README entries for curated robustness artifacts.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-robustness-and-generality/05-CONTEXT.md
@.planning/phases/04-core-experiment-evidence/04-VERIFICATION.md
@TRC-23-02333/transparent_estimator_eval.py
@TRC-23-02333/summarize_trace_sl_rcss.py
@scripts/run_stage12_pems7_228.sh
@scripts/run_stage13_candidate_sensitivity_pems7_228.sh
@TRC-23-02333/trace_sl_results/README.md

<interfaces>
Launcher patterns to preserve:
- Stage 12 exports BLAS thread caps, supports `DRY_RUN=1`, iterates `SEEDS`, writes `seed_<seed>` directories, pipes logs through `tee`, and calls `summarize_trace_sl_rcss.py` at the end.
- Stage 13 exports BLAS thread caps, supports `DRY_RUN=1`, iterates `CANDIDATE_COUNTS`, writes `candidates_<N>/seed_<seed>`, records timing CSVs, and aggregates candidate roots with `--runtime-root`.
Stage 14 must follow these patterns, add robustness flags from Plan 05-01, and run reduced actual evidence bundles as part of this plan so Stage 14 evidence is artifact-backed per D-01.
</interfaces>
</context>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 5 | Robustness/generalization evidence. | 05-01..05-04 | COVERED | This plan supplies executable launchers, generated evidence artifacts, and inventory docs. |
| REQ | ROBUST-01 | Sensor failure 5/10/20%. | 05-03 | COVERED | Stage 14 robustness artifact generation includes baseline and failure rates 0.05/0.10/0.20. |
| REQ | ROBUST-02 | Observation noise. | 05-03 | COVERED | Stage 14 robustness artifact generation includes nonzero noise condition rows. |
| REQ | ROBUST-03 | Missing readings/time blocks. | 05-03 | COVERED | Stage 14 robustness artifact generation includes random and block missingness rows. |
| REQ | ROBUST-04 | Nonuniform costs/proxy. | 05-03 | COVERED | Stage 14 robustness artifact generation includes cost proxy columns and feasibility metadata. |
| REQ | ROBUST-05 | Temporal shift. | 05-03 | COVERED | Stage 14 robustness artifact generation includes chronological split rows. |
| REQ | ROBUST-06 | Candidate 50/100/200/500 performance/runtime. | 05-03 | COVERED | Stage 14 candidate artifact generation includes counts 50/100/200/500 or writes validator-checked caveat JSON. |
| CONTEXT | D-01 | Artifact-backed evidence. | 05-03 | COVERED | Tasks run reduced evidence commands and verify aggregate CSV/Markdown outputs. |
| CONTEXT | D-10 | Candidate sensitivity coverage or caveat. | 05-03 | COVERED | Full default sweep specified; caveat must be machine-readable and validator-checked if tractability limits a count. |
| CONTEXT | D-14 | Small PeMS7_228 Stage 14 bundle. | 05-03 | COVERED | Plan centers PeMS7_228 actual reduced evidence generation. |
| CONTEXT | D-15 | External robustness optional. | 05-03, 05-04 | COVERED | README marks external robustness as supporting/not required. |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add launcher and generate Stage 14 PeMS7_228 robustness evidence</name>
  <files>scripts/run_stage14_pems7_228_robustness.sh, TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/, TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/combined_metrics.csv, TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/gls_map_layout_summary.csv, TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/gls_map_paired_delta_tests.csv, TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness/SUMMARY.md</files>
  <read_first>
    <file>.planning/phases/05-robustness-and-generality/05-CONTEXT.md</file>
    <file>scripts/run_stage12_pems7_228.sh</file>
    <file>TRC-23-02333/transparent_estimator_eval.py</file>
  </read_first>
  <action>Create `scripts/run_stage14_pems7_228_robustness.sh` following Stage 12 style: `#!/usr/bin/env bash`, `set -euo pipefail`, repo-relative defaults, BLAS thread exports, `PYTHON_BIN`, `DRY_RUN`, `run_or_print`, deterministic `SEEDS`, `BUDGETS`, `NUM_LAYOUTS`, RCSS, scenario, validation-swap, and `MAX_TEST_STEPS` defaults. Use `OUTPUT_DIR=TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness` by default. Iterate named conditions `baseline`, `failure_0.05`, `failure_0.10`, `failure_0.20`, `noise_0.05`, `random_missing_0.10`, `block_missing_12`, `cost_proxy_budget`, and `chronological_split`. For each condition, call `transparent_estimator_eval.py` with Phase 3/4 portfolio flags and Plan 05-01 robustness CLI flags, write to `${OUTPUT_DIR}/${condition}/seed_${seed}`, and aggregate all condition roots with `summarize_trace_sl_rcss.py --input-root "${OUTPUT_DIR}"/* --output-dir "${OUTPUT_DIR}"`. Execute the actual reduced PeMS7_228 evidence run as part of this task using deterministic low-cost overrides, not only `DRY_RUN`. Do not commit, delete, or move raw datasets; only generated artifacts under `trace_sl_results` are evidence.</action>
  <verify>
    <automated>bash -n scripts/run_stage14_pems7_228_robustness.sh &amp;&amp; DRY_RUN=1 SEEDS="25" BUDGETS="0.10" NUM_LAYOUTS=1 RCSS_RANDOM_CANDIDATES=1 RCSS_QUALITY_CANDIDATES=1 scripts/run_stage14_pems7_228_robustness.sh &amp;&amp; SEEDS="25 26" BUDGETS="0.10" NUM_LAYOUTS=1 RCSS_RANDOM_CANDIDATES=2 RCSS_QUALITY_CANDIDATES=2 SCENARIO_COUNT=2 VALIDATION_SWAP_STARTS=1 VALIDATION_SWAP_ITER=1 VALIDATION_SWAP_ADD_POOL=5 VALIDATION_SWAP_REMOVE_POOL=5 MAX_TEST_STEPS=1 THREADS_PER_JOB=1 scripts/run_stage14_pems7_228_robustness.sh &amp;&amp; python - <<'PY'
from pathlib import Path
import pandas as pd
root = Path('TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness')
required_files = ['combined_metrics.csv', 'gls_map_layout_summary.csv', 'gls_map_paired_delta_tests.csv', 'SUMMARY.md']
missing_files = [name for name in required_files if not (root / name).exists()]
assert not missing_files, missing_files
combined = pd.read_csv(root / 'combined_metrics.csv')
layout = pd.read_csv(root / 'gls_map_layout_summary.csv')
tests = pd.read_csv(root / 'gls_map_paired_delta_tests.csv')
required_cols = {'robustness_family','robustness_condition','failure_rate','noise_scale','missing_rate','missing_block_steps','cost_budget','split_mode','method'}
assert required_cols.issubset(combined.columns), required_cols - set(combined.columns)
assert required_cols - {'method'} <= set(layout.columns), required_cols - {'method'} - set(layout.columns)
assert 'robustness_condition' in tests.columns, tests.columns.tolist()
conditions = set(combined['robustness_condition'].dropna().astype(str))
for condition in ['baseline','failure_0.05','failure_0.10','failure_0.20','noise_0.05','random_missing_0.10','block_missing_12','cost_proxy_budget','chronological_split']:
    assert condition in conditions, condition
assert (combined['method'] == 'gls_map').any(), 'missing held-out gls_map rows'
print('stage14-robustness-evidence-ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - Real `combined_metrics.csv`, `gls_map_layout_summary.csv`, `gls_map_paired_delta_tests.csv`, and `SUMMARY.md` exist under `pems7_228_stage14_robustness`.
    - ROBUST-01..05 condition families are present as machine-readable rows with required condition columns.
    - Held-out `gls_map` rows are present for robustness evidence per D-02.
    - The launcher writes under `TRC-23-02333/trace_sl_results/pems7_228_stage14_robustness` and never writes under `TRC-23-02333/dataset/`.
  </acceptance_criteria>
  <done>Stage 14 PeMS7_228 robustness evidence is reproducible and artifact-backed.</done>
</task>

<task type="auto">
  <name>Task 2: Add launcher and generate Stage 14 candidate-count/runtime evidence</name>
  <files>scripts/run_stage14_candidate_sensitivity_pems7_228.sh, TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/, TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/combined_metrics.csv, TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/runtime_candidate_sensitivity.csv, TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/candidate_sensitivity_summary.csv, TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/stage14_timing.csv, TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/SUMMARY.md, TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity/candidate_sensitivity_caveat.json</files>
  <read_first>
    <file>.planning/phases/05-robustness-and-generality/05-CONTEXT.md</file>
    <file>scripts/run_stage13_candidate_sensitivity_pems7_228.sh</file>
    <file>TRC-23-02333/summarize_trace_sl_rcss.py</file>
  </read_first>
  <action>Create `scripts/run_stage14_candidate_sensitivity_pems7_228.sh` by adapting the Stage 13 pattern. Defaults: `OUTPUT_DIR=TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity`, `CANDIDATE_COUNTS="50 100 200 500"`, `SEEDS="25"`, `BUDGETS="0.20"`, and reduced runtime knobs overridable through environment variables. Preserve BLAS thread caps, `DRY_RUN=1`, per-candidate `candidates_${candidate_count}/seed_${seed}` directories, and `stage14_timing.csv` with columns `candidate_count,seed,runtime_seconds,status,output_dir,caveat_path`. For real runs, generate performance and runtime artifacts for candidate counts 50, 100, 200, and 500. If and only if a candidate count cannot complete because of local tractability, write `candidate_sensitivity_caveat.json` with keys `requirement`, `allowed_exception`, `missing_candidate_counts`, `completed_candidate_counts`, `reason`, `evidence_attempted`, and `validator_disposition`; the validator in 05-04 must treat that artifact as the only allowed ROBUST-06 limited exception. Do not silently skip candidate_count 500.</action>
  <verify>
    <automated>bash -n scripts/run_stage14_candidate_sensitivity_pems7_228.sh &amp;&amp; DRY_RUN=1 CANDIDATE_COUNTS="50 100 200 500" SEEDS="25" BUDGETS="0.20" NUM_LAYOUTS=1 scripts/run_stage14_candidate_sensitivity_pems7_228.sh &amp;&amp; CANDIDATE_COUNTS="50 100 200 500" SEEDS="25" BUDGETS="0.20" NUM_LAYOUTS=1 SCENARIO_COUNT=1 VALIDATION_SWAP_STARTS=1 VALIDATION_SWAP_ITER=1 VALIDATION_SWAP_ADD_POOL=5 VALIDATION_SWAP_REMOVE_POOL=5 MAX_TEST_STEPS=1 THREADS_PER_JOB=1 scripts/run_stage14_candidate_sensitivity_pems7_228.sh &amp;&amp; python - <<'PY'
from pathlib import Path
import json
import pandas as pd
root = Path('TRC-23-02333/trace_sl_results/pems7_228_stage14_candidate_sensitivity')
required = ['combined_metrics.csv', 'runtime_candidate_sensitivity.csv', 'candidate_sensitivity_summary.csv', 'stage14_timing.csv', 'SUMMARY.md']
missing = [name for name in required if not (root / name).exists()]
assert not missing, missing
timing = pd.read_csv(root / 'stage14_timing.csv')
assert {'candidate_count','runtime_seconds','status','output_dir'}.issubset(timing.columns), timing.columns.tolist()
counts = set(timing['candidate_count'].dropna().astype(int))
expected = {50, 100, 200, 500}
caveat_path = root / 'candidate_sensitivity_caveat.json'
if expected <= counts and (timing[timing['candidate_count'].astype(int).isin(expected)]['status'].astype(str).str.lower() == 'success').all():
    combined = pd.read_csv(root / 'combined_metrics.csv')
    runtime = pd.read_csv(root / 'runtime_candidate_sensitivity.csv')
    summary = pd.read_csv(root / 'candidate_sensitivity_summary.csv')
    for frame, name in [(combined, 'combined'), (runtime, 'runtime'), (summary, 'summary')]:
        assert 'candidate_count' in frame.columns, name
        assert expected <= set(frame['candidate_count'].dropna().astype(int)), name
else:
    assert caveat_path.exists(), 'missing ROBUST-06 caveat artifact'
    caveat = json.loads(caveat_path.read_text(encoding='utf-8'))
    assert caveat.get('requirement') == 'ROBUST-06', caveat
    assert caveat.get('allowed_exception') is True, caveat
    assert set(caveat.get('missing_candidate_counts', [])) <= expected, caveat
    assert caveat.get('evidence_attempted') is True, caveat
print('stage14-candidate-evidence-ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `stage14_timing.csv` records candidate counts 50, 100, 200, and 500 with runtime/status information.
    - `combined_metrics.csv`, `runtime_candidate_sensitivity.csv`, and `candidate_sensitivity_summary.csv` contain candidate_count evidence for 50/100/200/500 when all runs complete.
    - If a tractability limit prevents a count, `candidate_sensitivity_caveat.json` is present, machine-readable, and explicitly tied to ROBUST-06; no missing candidate count is silently ignored.
    - The final summarizer command uses all `candidates_*` roots and `--runtime-root` to generate runtime/candidate summaries.
  </acceptance_criteria>
  <done>Stage 14 candidate-count sensitivity has artifact-backed 50/100/200/500 runtime/performance evidence or a validator-checkable limited-tractability caveat.</done>
</task>

<task type="auto">
  <name>Task 3: Register generated Stage 14 evidence in the result inventory</name>
  <files>TRC-23-02333/trace_sl_results/README.md</files>
  <read_first>
    <file>.planning/phases/05-robustness-and-generality/05-CONTEXT.md</file>
    <file>.planning/phases/04-core-experiment-evidence/04-VERIFICATION.md</file>
    <file>TRC-23-02333/trace_sl_results/README.md</file>
  </read_first>
  <action>Update `TRC-23-02333/trace_sl_results/README.md` to include Stage 14 robustness and Stage 14 candidate sensitivity rows after the evidence generation tasks complete. State that PeMS7_228 Stage 14 is the core robustness/generalization evidence path, Stage 14 candidate sensitivity covers 50/100/200/500-style candidate budgets when generated, and `candidate_sensitivity_caveat.json` is the only allowed ROBUST-06 limited-tractability exception when present. List generated aggregate files: `combined_metrics.csv`, `gls_map_layout_summary.csv`, `gls_map_paired_delta_tests.csv`, `candidate_sensitivity_summary.csv`, `runtime_candidate_sensitivity.csv`, `stage14_timing.csv`, and `SUMMARY.md`. Keep wording as stress-test evidence for usefulness under perturbations, not universal deployment robustness proof per D-04.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('TRC-23-02333/trace_sl_results/README.md').read_text(encoding='utf-8')
required = ['pems7_228_stage14_robustness', 'pems7_228_stage14_candidate_sensitivity', '50/100/200/500', 'combined_metrics.csv', 'gls_map_layout_summary.csv', 'gls_map_paired_delta_tests.csv', 'runtime_candidate_sensitivity.csv', 'candidate_sensitivity_summary.csv', 'stage14_timing.csv', 'candidate_sensitivity_caveat.json', 'stress-test', 'supporting/optional']
missing = [token for token in required if token not in text]
assert not missing, missing
assert 'universal deployment robustness proof' not in text.lower()
print('stage14-readme-ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - Result inventory names both Stage 14 directories, generated CSV/Markdown artifacts, and their claim status.
    - README does not cite raw datasets as evidence.
    - README preserves Phase 4 Stage 12/13 guidance and stable method labels.
  </acceptance_criteria>
  <done>Curated result inventory describes how to interpret generated Stage 14 robustness and candidate-count artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shell environment -> launcher | Environment variables can override run scale, output paths, seeds, and Python executable. |
| launcher -> local filesystem | Runs write generated outputs and timing logs under trace_sl_results. |
| generated evidence -> claims | Missing candidate counts or stress conditions can create overbroad conclusions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-03-01 | Tampering | launcher output roots | mitigate | Use repo-relative defaults under `TRC-23-02333/trace_sl_results/` and explicit `OUTPUT_DIR` variable. |
| T-05-03-02 | Repudiation | candidate runtime evidence | mitigate | Write `stage14_timing.csv` with candidate_count, seed, runtime_seconds, status, output_dir, and caveat_path. |
| T-05-03-03 | Denial of Service | candidate_count 500 | mitigate | Keep default seeds/budget small, thread caps enabled, expose scale knobs as environment overrides, and write machine-readable caveat only after an attempted run cannot complete. |
| T-05-03-04 | Information Disclosure | raw datasets | mitigate | Scripts point evaluator at local data roots but README and validators cite generated artifacts, not raw data contents; no task commits/deletes raw datasets. |
| T-05-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the task automated checks. Completion requires real Stage 14 aggregate artifacts under both PeMS7_228 Stage 14 directories, not DRY_RUN output alone. ROBUST-06 must have 50/100/200/500 performance plus runtime evidence or `candidate_sensitivity_caveat.json` with the required machine-readable limited-tractability fields.
</verification>

<success_criteria>
- Stage 14 robustness artifacts cover ROBUST-01..05 condition families with held-out GLS/MAP rows and condition columns.
- Stage 14 candidate artifacts cover ROBUST-06 candidate counts 50/100/200/500 and runtime recording, or provide a validator-checkable limited-tractability caveat.
- Result inventory explains generated Stage 14 claim status without overclaiming or citing raw data.
</success_criteria>

<output>
Create `.planning/phases/05-robustness-and-generality/05-03-SUMMARY.md` when done.
</output>
