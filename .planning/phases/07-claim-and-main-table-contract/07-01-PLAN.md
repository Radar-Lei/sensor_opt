---
phase: 07-claim-and-main-table-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_trace_sl_claim_contracts.py
  - scripts/test_generate_trace_sl_claim_contracts.py
autonomous: true
requirements:
  - CLAIM-01
  - CLAIM-02
  - CLAIM-03
  - CLAIM-04
  - EVID-01
  - EVID-02
  - HAND-01
must_haves:
  truths:
    - "Author can generate a machine-readable claim contract that frames TRACE-SL as transparent reconstruction-aware sparse sensor layout design."
    - "Author can separate Transportation Science-ready claims from TR Part B-level extensions without manuscript prose."
    - "Author can fail closed when forbidden wording, low-budget caveat, source columns, or evidence routing rules are missing."
    - "Author can derive the main PeMS7_228 table contract from committed Stage12 aggregate evidence only."
  artifacts:
    - path: "scripts/generate_trace_sl_claim_contracts.py"
      provides: "Deterministic claim and main-table contract generator"
      exports: ["build_all_contracts", "build_claim_contract_rows", "build_main_table_contract_rows", "validate_claim_contract_rows", "validate_main_table_contract_rows"]
    - path: "scripts/test_generate_trace_sl_claim_contracts.py"
      provides: "Regression tests for claim/table contract generation and fail-fast guardrails"
      contains: "ClaimContractGenerationTests"
  key_links:
    - from: "scripts/generate_trace_sl_claim_contracts.py"
      to: "TRC-23-02333/trace_sl_results/pems7_228_stage12_baseline_portfolio/gls_map_layout_summary.csv"
      via: "tracked aggregate CSV load"
      pattern: "pems7_228_stage12_baseline_portfolio.*gls_map_layout_summary"
    - from: "scripts/generate_trace_sl_claim_contracts.py"
      to: "TRC-23-02333/trace_sl_results/pems7_228_stage12_baseline_portfolio/gls_map_paired_delta_tests.csv"
      via: "tracked paired statistics CSV load"
      pattern: "pems7_228_stage12_baseline_portfolio.*gls_map_paired_delta_tests"
    - from: "scripts/test_generate_trace_sl_claim_contracts.py"
      to: "scripts/generate_trace_sl_claim_contracts.py"
      via: "dynamic import of generator under test"
      pattern: "spec_from_file_location"
---

<objective>
Create the deterministic Phase 7 generator and tests that define the frozen Transportation Science claim contract and main PeMS7_228 table contract.

Purpose: The later artifact-generation plan must have fail-fast code that enforces the user-approved claim boundaries, low-budget multistart caveat, Stage12 PeMS7_228 evidence source, and robustness/external-evidence lanes.
Output: `scripts/generate_trace_sl_claim_contracts.py` and `scripts/test_generate_trace_sl_claim_contracts.py`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-claim-and-main-table-contract/07-CONTEXT.md
@.planning/phases/07-claim-and-main-table-contract/07-PATTERNS.md
@scripts/generate_trace_sl_paper_sources.py
@scripts/test_generate_trace_sl_paper_sources.py
@TRC-23-02333/trace_sl_results/README.md
@TRC-23-02333/trace_sl_results/paper_sources/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write fail-fast contract regression tests</name>
  <read_first>
    - @scripts/test_generate_trace_sl_claim_contracts.py (target new file; create if absent)
    - @scripts/test_generate_trace_sl_paper_sources.py
    - @.planning/phases/07-claim-and-main-table-contract/07-CONTEXT.md
    - @.planning/phases/07-claim-and-main-table-contract/07-PATTERNS.md
    - @.planning/REQUIREMENTS.md
  </read_first>
  <files>scripts/test_generate_trace_sl_claim_contracts.py</files>
  <behavior>
    - Test 1: Claim contract rows include claim lanes for Transportation Science-ready, TR Part B-level extension, wording guardrail, external evidence held outside core, and robustness stress-test/appendix routing.
    - Test 2: Forbidden wording list contains exactly `optimal`, `certified`, `globally robust`, `guaranteed MAE improvement`, and `generalizes across networks`, while bounded allowed wording contains `certificate-guided`, `posterior-certificate-aware`, `stress-tested`, `improves in the tested setting`, and conditionally supported `external evidence`.
    - Test 3: Missing Stage12 layout columns, missing paired-stat columns, missing required reviewer-facing layout labels, or empty outputs raise `FileNotFoundError` or `ValueError`.
    - Test 4: PeMS7_1026 and Seattle rows cannot have `claim_lane=core_in_domain` or `claim_status=transportation_science_ready` in Phase 7.
    - Test 5: Robustness rows cannot be routed outside `stress_test` or `appendix` lanes unless the source row declares multi-seed perturbation evidence.
    - Test 6: 10% PeMS7_228 rows and dominance-sensitive rows involving `multistart_swap_by_validation` carry the caveat tag `pems7_228_low_budget_multistart_not_dominant`.
  </behavior>
  <action>Create `scripts/test_generate_trace_sl_claim_contracts.py` following the dynamic-import and temporary CSV fixture style from `scripts/test_generate_trace_sl_paper_sources.py`. The tests must import `scripts/generate_trace_sl_claim_contracts.py`, create minimal Stage12-style CSV fixtures with `budget`, `layout_type`, `mean`, `std`, `count`, and paired-stat columns, and assert the exact columns and guardrails needed for CLAIM-01 through CLAIM-04, EVID-01, EVID-02, and HAND-01. Include negative tests for absent forbidden-wording rows, absent low-budget multistart caveat, PeMS7_1026/Seattle core-claim elevation, robustness lane violations, and missing source/provenance columns. Do not create manuscript prose assertions; test contract fields, labels, lanes, caveat tags, and provenance only.</action>
  <verify>
    <automated>python scripts/test_generate_trace_sl_claim_contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the test file contains `GENERATOR_PATH = PROJECT_ROOT / "scripts" / "generate_trace_sl_claim_contracts.py"` and a `ClaimContractGenerationTests` unittest class.
    - Source assertion: the test file references all five forbidden terms and the caveat tag `pems7_228_low_budget_multistart_not_dominant`.
    - Source assertion: at least one test asserts PeMS7_1026/Seattle are not core claims, and at least one test asserts robustness remains stress-test or appendix lane.
    - CLI assertion: running `python scripts/test_generate_trace_sl_claim_contracts.py` fails before Task 2 implements the generator and passes after Task 2.
  </acceptance_criteria>
  <done>Regression tests encode every Phase 7 claim/table guardrail before implementation and fail closed for the specified missing or violating inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic claim/table contract generator</name>
  <read_first>
    - @scripts/generate_trace_sl_claim_contracts.py (target new file; create if absent)
    - @scripts/generate_trace_sl_paper_sources.py
    - @scripts/generate_trace_sl_repro_manifest.py
    - @scripts/test_generate_trace_sl_claim_contracts.py
    - @.planning/phases/07-claim-and-main-table-contract/07-CONTEXT.md
    - @TRC-23-02333/trace_sl_results/pems7_228_stage12_baseline_portfolio/gls_map_layout_summary.csv
    - @TRC-23-02333/trace_sl_results/pems7_228_stage12_baseline_portfolio/gls_map_paired_delta_tests.csv
  </read_first>
  <files>scripts/generate_trace_sl_claim_contracts.py</files>
  <action>Create `scripts/generate_trace_sl_claim_contracts.py` as a standalone Python CLI using the existing paper-source generator patterns: `TRACE_RESULTS_ROOT`, `STAGE12_DIR`, `project_relative`, `load_csv`, `source_metadata`, `rows_from_frame`, `markdown_table`, `write_table_outputs`, `write_json`, `assert_source_is_tracked`, `resolve_output_dir`, `build_all_contracts`, `parse_args`, and `main`. Define `FORBIDDEN_WORDING`, `BOUNDED_ALLOWED_WORDING`, `LOW_BUDGET_MULTISTART_CAVEAT_TAG`, `MAIN_METHOD_LABEL=validation_swap_selected`, and `MAIN_TABLE_LAYOUT_LABELS` containing `validation_swap_selected`, `multistart_swap_by_validation`, `rcss_selected`, `best_random_by_validation`, `random`, `top_variance`, `greedy_a_trace`, `graph_sampling_laplacian`, `observability_proxy`, and `qr_pod_modes`; include `greedy_d_logdet` only as a secondary certificate baseline if present without making it required for EVID-02. Build `claim_contract.csv` rows with columns `source_stage`, `source_dir`, `source_csv`, `claim_id`, `claim_lane`, `claim_status`, `allowed_wording`, `forbidden_wording`, `evidence_source`, `evidence_artifact`, `required_metric_basis`, `required_caveat_tag`, `phase_scope`, and `notes`. Build `claim_contract.json` with deterministic schema metadata, fixed timestamp, forbidden wording, bounded allowed wording, claim lanes, evidence routing, caveat tags, and source artifact pointers. Build `main_table_contract.csv` rows from Stage12 PeMS7_228 `gls_map_layout_summary.csv` joined with `gls_map_paired_delta_tests.csv` for paired deltas and p-values where `layout=validation_swap_selected`; output columns must include `source_stage`, `source_dir`, `source_csv`, `budget`, `method_label`, `layout_type`, `table_role`, `test_mae_mean`, `test_mae_std`, `test_rmse_mean`, `test_mape_mean`, `paired_baseline`, `delta_mean`, `ci95_low`, `ci95_high`, `paired_t_p`, `wilcoxon_p`, `win_count`, `split_count`, `caveat_tag`, and `claim_lane`. Validate tracked aggregate sources only under `TRC-23-02333/trace_sl_results/`; never read, list, delete, or write raw files under `TRC-23-02333/dataset/`. Do not use validation MAE as claim-facing evidence; set the metric basis to held-out test evidence with paired comparisons where available, per user decision lines 16-37 in `07-CONTEXT.md`.</action>
  <verify>
    <automated>python scripts/test_generate_trace_sl_claim_contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `build_claim_contract_rows`, `build_main_table_contract_rows`, `validate_claim_contract_rows`, `validate_main_table_contract_rows`, and `build_all_contracts` exist as top-level functions.
    - Source assertion: generator constants include all required forbidden terms, all required bounded allowed wording phrases, `LOW_BUDGET_MULTISTART_CAVEAT_TAG`, `MAIN_METHOD_LABEL`, and `MAIN_TABLE_LAYOUT_LABELS`.
    - Source assertion: `assert_source_is_tracked` rejects paths outside `TRC-23-02333/trace_sl_results/` and uses `git ls-files --error-unmatch`.
    - CLI assertion: `python scripts/test_generate_trace_sl_claim_contracts.py` passes.
  </acceptance_criteria>
  <done>The generator passes the contract regression tests and exposes deterministic functions for claim-contract and main-table artifact generation without touching raw datasets or manuscript prose.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed aggregate CSVs -> generator | Curated result files are treated as inputs but still require schema, emptiness, and provenance validation. |
| generator -> paper_sources artifacts | Machine-readable contracts become downstream claim/table authority and must not silently omit caveats or routing constraints. |
| local filesystem -> evidence inventory | Raw ignored datasets exist locally but must not be listed or consumed as evidence artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `assert_source_is_tracked` and source CSV loaders | mitigate | Verify every source is under `TRC-23-02333/trace_sl_results/` and tracked by git before loading. |
| T-07-02 | Information Disclosure | source-artifact pointer generation | mitigate | Reject or omit any path under `TRC-23-02333/dataset/`; use committed aggregate CSV/JSON/Markdown artifacts only. |
| T-07-03 | Repudiation | generated claim/table contracts | mitigate | Preserve `source_stage`, `source_dir`, and `source_csv` on every derived row. |
| T-07-04 | Tampering | claim wording and caveat policy | mitigate | Add fail-fast validators and regression tests for forbidden wording, external/core routing, robustness lane routing, and low-budget multistart caveat tags. |
| T-07-SC | Tampering | package installs | accept | No package-manager install is planned; generator uses existing pandas dependency and standard library patterns. |
</threat_model>

<verification>
Run `python scripts/test_generate_trace_sl_claim_contracts.py` from `/home/samuel/projects/sensor_opt`. Confirm no generated artifact references `TRC-23-02333/dataset/`, and confirm tests fail closed for missing columns, missing caveats, forbidden wording omissions, external evidence lane violations, and robustness lane violations.
</verification>

<success_criteria>
- `scripts/generate_trace_sl_claim_contracts.py` exists and can build claim contract, JSON policy, and main-table contract rows from committed Stage12 PeMS7_228 aggregate sources.
- `scripts/test_generate_trace_sl_claim_contracts.py` exists and passes, covering CLAIM-01, CLAIM-02, CLAIM-03, CLAIM-04, EVID-01, EVID-02, and HAND-01 guardrails.
- The code enforces the 10% PeMS7_228 multistart caveat and prevents PeMS7_1026, Seattle, and robustness evidence from entering unsupported core-claim lanes.
</success_criteria>

<output>
Create `.planning/phases/07-claim-and-main-table-contract/07-01-SUMMARY.md` when done.
</output>
