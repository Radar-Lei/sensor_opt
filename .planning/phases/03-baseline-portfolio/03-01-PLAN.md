---
phase: 03-baseline-portfolio
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - TRC-23-02333/transparent_estimator_eval.py
  - TRC-23-02333/summarize_trace_sl_rcss.py
  - scripts/run_stage12_pems7_228.sh
  - .planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md
  - .planning/phases/03-baseline-portfolio/check_phase3_baseline_wiring.py
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
  - BASE-04
  - BASE-05
  - BASE-06
requirements_addressed: [BASE-01, BASE-02, BASE-03, BASE-04, BASE-05, BASE-06]
must_haves:
  truths:
    - "D-01: Phase 3 maps every baseline to BASE-01..BASE-06 and to a reviewer objection."
    - "D-02: Feasible baselines get evaluator support; only learning-style or incompatible objectives may be documented deferrals with reasons."
    - "D-03: Every implemented baseline is evaluated through held-out hidden-node reconstruction metrics, especially gls_map."
    - "D-04: Phase 3 adds reviewer-grade baseline portfolio rows without weakening the TRACE-SL claim."
    - "D-05: TRC-23-02333/transparent_estimator_eval.py remains the evaluator source of truth; no copied evaluator is created."
    - "D-06: New baselines appear as stable layout_type rows without breaking existing metrics/layout artifact schemas."
    - "D-07: Run-script wiring is limited to a reusable Stage 12 baseline portfolio launcher."
    - "D-08: Phase 3 validates via compile, help, shell syntax, and no-raw-data static artifact-schema checks before full evidence runs."
    - "D-09: A-optimal and D-optimal design baselines appear as independent layout_type rows."
    - "D-10: Graph sampling/GSP coverage is either graph_sampling_laplacian or explicitly tied to existing GSP evidence."
    - "D-11: QR/SVD/POD sparse placement is represented by a training-data-only qr_pod_modes layout row where compatible."
    - "D-12: The observability/TSLP proxy documents objective mismatch while preserving held-out reconstruction comparison."
    - "D-13: Learning-style reconstruction is lightweight if implemented, otherwise documented as a transparent-method deferral."
    - "D-14: multistart_swap_by_validation is a named comparator or predeclared portfolio member."
    - "D-15: 03-BASELINE-INVENTORY.md records implementation, representation, deferral, and Phase 4 evidence status."
    - "D-16: Baseline names are stable and paper-readable for Phase 4 aggregation and Phase 7 writing."
    - "D-17: Validation MAE remains selection evidence only; later claims use held-out test metrics."
    - "D-18: Raw datasets remain local/ignored, are not committed, and are not read during Phase 3 verification."
  artifacts:
    - path: "TRC-23-02333/transparent_estimator_eval.py"
      provides: "Baseline portfolio layout generation, CLI flags, and held-out evaluation rows"
    - path: "TRC-23-02333/summarize_trace_sl_rcss.py"
      provides: "Aggregate summaries that recognize Phase 3 baseline layout_type names"
    - path: "scripts/run_stage12_pems7_228.sh"
      provides: "Reusable paper-facing baseline portfolio smoke/full launcher"
    - path: ".planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md"
      provides: "BASE-01..BASE-06 baseline coverage, reviewer objection mapping, and deferral/evidence status"
    - path: ".planning/phases/03-baseline-portfolio/check_phase3_baseline_wiring.py"
      provides: "No-raw-data static/AST validation that Phase 3 layout_type names, flags, summarizer hooks, and artifact schema paths are wired"
  key_links:
    - from: "TRC-23-02333/transparent_estimator_eval.py baseline helper functions"
      to: "final evaluate_layout(test, ...) loop"
      via: "layouts tuples with stable layout_type names"
      pattern: "layouts.append.*layout_type.*evaluate_layout\(test"
    - from: "scripts/run_stage12_pems7_228.sh"
      to: "TRC-23-02333/transparent_estimator_eval.py"
      via: "explicit --include-* baseline CLI flags"
      pattern: "--include-baseline-portfolio|--include-graph-sampling-baseline|--include-qr-pod-baseline"
    - from: "TRC-23-02333/summarize_trace_sl_rcss.py"
      to: "metrics.csv layout_type rows"
      via: "gls_map_layout_summary.csv and paired delta baseline list"
      pattern: "observability_proxy|graph_sampling|qr_pod|multistart_swap_by_validation"
---

<objective>
Create a reviewer-resistant TRACE-SL baseline portfolio in the existing evaluator and artifact workflow.

Purpose: Phase 3 defends the strong Transportation Science claim against OR/TSLP-style observability, A/D-optimal experimental design, graph sampling/GSP, sparse QR/SVD/POD placement, learning-style reconstruction, and multistart refinement objections without changing the claim into a weaker heuristic story.

Output: Minimal evaluator additions, a Stage 12 baseline launcher, aggregate-summary awareness, and a phase-local baseline inventory covering BASE-01 through BASE-06.
</objective>

<execution_context>
@/home/samuel/.claude_codex/get-shit-done/workflows/execute-plan.md
@/home/samuel/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samuel/projects/sensor_opt/.planning/STATE.md
@/home/samuel/projects/sensor_opt/.planning/ROADMAP.md
@/home/samuel/projects/sensor_opt/.planning/REQUIREMENTS.md
@/home/samuel/projects/sensor_opt/.planning/phases/03-baseline-portfolio/03-CONTEXT.md
@/home/samuel/projects/sensor_opt/.planning/phases/03-baseline-portfolio/03-PATTERNS.md
@/home/samuel/projects/sensor_opt/.planning/phases/01-claim-evidence-contract/01-CONTEXT.md
@/home/samuel/projects/sensor_opt/.planning/phases/02-formulation-and-theory-bridge/02-CONTEXT.md
@/home/samuel/.claude_codex/get-shit-done/references/planner-mvp-mode.md

<interfaces>
Use these existing contracts and patterns from 03-PATTERNS.md:
- `greedy_posterior_layout(base_matrix, sensor_count, obs_weight, objective)` already supports objective names `a_trace` and `d_logdet`; Phase 3 must expose or preserve them as independent A/D-optimal layout rows per D-09 and BASE-02.
- `degree_layout(distance, sensor_count)`, `top_variance_layout(train, sensor_count)`, and `coverage_layout(distance, sensor_count)` show simple baseline helper style; new helpers should return `np.array(..., dtype=int)` sensor indices.
- `make_laplacian`, `make_similarity`, `posterior_trace_for_layout`, `scenario_cvar_trace_for_layout`, `coverage_penalty`, `validation_mae`, and `evaluate_layout` are existing primitives; reuse them instead of creating a copied evaluator per D-05.
- `layouts` entries must stay shaped as `(layout_type, layout_id, sensors, validation_selected_mae)` and be evaluated by the existing final `evaluate_layout(test, ...)` loop per D-03, D-06, and D-17.
- Artifact writers emit `metrics.csv`, `metrics.json`, `layouts.json`, `rcss_candidates.*`, `certificate_correlations.csv`, `config.json`, and `SUMMARY.md`; do not change raw dataset paths or commit dataset contents per D-18.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add baseline portfolio layout rows to the existing evaluator</name>
  <files>TRC-23-02333/transparent_estimator_eval.py</files>
  <read_first>
    - /home/samuel/projects/sensor_opt/.planning/phases/03-baseline-portfolio/03-CONTEXT.md
    - /home/samuel/projects/sensor_opt/.planning/phases/03-baseline-portfolio/03-PATTERNS.md
    - /home/samuel/projects/sensor_opt/TRC-23-02333/transparent_estimator_eval.py
  </read_first>
  <behavior>
    - Help/CLI behavior: `python TRC-23-02333/transparent_estimator_eval.py --help` lists explicit Phase 3 baseline flags.
    - Static wiring behavior: Phase 3 flags and stable `layout_type` names are present in evaluator source and can be checked without opening raw datasets.
    - Evaluation wiring behavior: implemented layout rows are appended to the existing `layouts` collection before the held-out `evaluate_layout(test, ...)` loop and preserve `method == gls_map` metrics when experiments are later run.
  </behavior>
  <action>
    In `TRC-23-02333/transparent_estimator_eval.py`, add minimal top-level baseline helper functions near the existing layout helpers, following lower_snake_case and returning sorted/typed NumPy integer sensor arrays. Implement `observability_proxy_layout(distance, sensor_count)` as the Phase 3 TSLP/observability proxy per D-12 and BASE-01 using transparent coverage/degree-style graph observability scoring; its objective mismatch must be noted in generated config/summary-facing names, but it must still be evaluated by held-out hidden-node reconstruction per D-03. Implement `graph_sampling_laplacian_layout(laplacian, distance, sensor_count)` for BASE-03 if compatible with current Laplacian primitives: use reusable graph/Laplacian information, not raw dataset reads, and emit a stable layout name such as `graph_sampling_laplacian`; if the current script lacks a safe Laplacian score input at the layout-generation point, bind BASE-03 to existing GSP reconstruction evidence by emitting no fake row and document that representation in Task 3. Implement `qr_pod_layout(train, sensor_count)` for BASE-04 using training traffic matrix modes only: center/standardize as already done for train-derived quantities, compute an SVD/POD basis, use QR with column pivoting or an equivalent deterministic pivot score to select sensor indices, and emit a stable `qr_pod_modes` layout row. Preserve or expose A/D-optimal rows per D-09 and BASE-02 by ensuring `greedy_a_trace` and `greedy_d_logdet` remain independent `layout_type` rows under the baseline portfolio run path and are not only RCSS candidate sources. Add CLI flags such as `--include-baseline-portfolio`, `--include-observability-proxy`, `--include-graph-sampling-baseline`, and `--include-qr-pod-baseline`; `--include-baseline-portfolio` should enable all implemented Phase 3 rows plus existing A/D-optimal and multistart-compatible rows, without changing default Stage 11 behavior. Keep `multistart_swap_by_validation` as a named comparator row per D-14 and BASE-06 when `--include-validation-swap` and multistart settings are active. Do not add a black-box learning method here; learning-based reconstruction is handled by Task 3 as a documented deferral per D-13 and BASE-05 unless a very small existing transparent check can be wired without new dependencies.
  </action>
  <verify>
    <automated>cd /home/samuel/projects/sensor_opt && python TRC-23-02333/transparent_estimator_eval.py --help | grep -E -- '--include-baseline-portfolio|--include-observability-proxy|--include-graph-sampling-baseline|--include-qr-pod-baseline'</automated>
    <automated>cd /home/samuel/projects/sensor_opt && python -m py_compile TRC-23-02333/transparent_estimator_eval.py</automated>
  </verify>
  <acceptance_criteria>
    - BASE-01 has an observability/TSLP-style proxy implementation path or an explicit implementation incompatibility note routed to the inventory.
    - BASE-02 emits independent A-optimal and D-optimal layout rows, using stable names `greedy_a_trace` and `greedy_d_logdet` unless the executor documents an even clearer backwards-compatible alias.
    - BASE-03 has either a real `graph_sampling_laplacian` layout row or a documented tie to existing GSP reconstruction evidence; no fake placeholder row is emitted.
    - BASE-04 emits a QR/SVD/POD-compatible `qr_pod_modes` layout row selected only from training data.
    - BASE-06 keeps `multistart_swap_by_validation` as a named comparator/portfolio member.
  </acceptance_criteria>
  <done>Evaluator help, compilation, and generated layout-row logic support Phase 3 baseline portfolio flags without changing raw dataset handling or Stage 11 defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the baseline portfolio into reproducible Stage 12 artifacts and summaries</name>
  <files>TRC-23-02333/summarize_trace_sl_rcss.py, scripts/run_stage12_pems7_228.sh</files>
  <read_first>
    - /home/samuel/projects/sensor_opt/.planning/phases/03-baseline-portfolio/03-PATTERNS.md
    - /home/samuel/projects/sensor_opt/TRC-23-02333/summarize_trace_sl_rcss.py
    - /home/samuel/projects/sensor_opt/scripts/run_stage11_pems7_228.sh
  </read_first>
  <action>
    Update `TRC-23-02333/summarize_trace_sl_rcss.py` only as needed so Phase 3 layout names are included in `baseline_layouts` or equivalent paired-delta comparison lists: `observability_proxy`, `greedy_a_trace`, `greedy_d_logdet`, `graph_sampling_laplacian` when implemented, `qr_pod_modes`, and `multistart_swap_by_validation`. Preserve `gls_map` as the reconstruction-centered comparison surface per D-03 and D-17, and keep machine-readable CSV outputs before Markdown summaries. Create `scripts/run_stage12_pems7_228.sh` by following the Stage 11 launcher pattern: use `set -euo pipefail`, uppercase environment overrides, BLAS thread caps, `DATA_ROOT` defaulting to `TRC-23-02333/dataset/PeMS7_228`, `OUTPUT_DIR` defaulting to `TRC-23-02333/trace_sl_results/pems7_228_stage12_baseline_portfolio`, small override-friendly defaults for `SEEDS`, `BUDGETS`, `NUM_LAYOUTS`, `RCSS_RANDOM_CANDIDATES`, `RCSS_QUALITY_CANDIDATES`, `SCENARIO_COUNT`, `VALIDATION_SWAP_STARTS`, `VALIDATION_SWAP_ITER`, `VALIDATION_SWAP_ADD_POOL`, `VALIDATION_SWAP_REMOVE_POOL`, and `THREADS_PER_JOB`. The evaluator invocation must include existing core flags `--include-simple-baselines`, `--include-greedy`, `--include-swap`, `--include-scenario-greedy`, `--include-rcss`, `--rcss-auto-weights`, `--include-validation-swap`, plus the Phase 3 `--include-baseline-portfolio` flag and any specific baseline flags added in Task 1. End the script by running `TRC-23-02333/summarize_trace_sl_rcss.py --input-root "${OUTPUT_DIR}" --output-dir "${OUTPUT_DIR}"`. This task implements D-07, D-08, D-15, D-16, BASE-01..BASE-04, and BASE-06 by making the portfolio reusable and paper-facing without broad refactors.
  </action>
  <verify>
    <automated>cd /home/samuel/projects/sensor_opt && bash -n scripts/run_stage12_pems7_228.sh</automated>
    <automated>cd /home/samuel/projects/sensor_opt && python -m py_compile TRC-23-02333/summarize_trace_sl_rcss.py</automated>
    <automated>cd /home/samuel/projects/sensor_opt && grep -v '^#' scripts/run_stage12_pems7_228.sh | grep -E -- '--include-baseline-portfolio|--include-validation-swap|--include-greedy'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/run_stage12_pems7_228.sh` is a reusable Phase 3 launcher with repo-relative paths and thread caps.
    - The launcher includes all implemented Phase 3 portfolio flags and preserves validation/test separation by relying on the evaluator.
    - The summarizer recognizes Phase 3 layout names in `gls_map` aggregate comparisons where those rows exist.
    - No broad modularization, copied evaluator, or raw-dataset artifact is introduced.
  </acceptance_criteria>
  <done>Stage 12 can be invoked with small environment overrides to generate Phase 3 baseline portfolio artifacts and aggregate summaries.</done>
</task>

<task type="auto">
  <name>Task 3: Create baseline inventory and run schema-focused smoke verification</name>
  <files>.planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md, .planning/phases/03-baseline-portfolio/check_phase3_baseline_wiring.py</files>
  <read_first>
    - /home/samuel/projects/sensor_opt/.planning/REQUIREMENTS.md
    - /home/samuel/projects/sensor_opt/.planning/phases/03-baseline-portfolio/03-CONTEXT.md
    - /home/samuel/projects/sensor_opt/.planning/phases/01-claim-evidence-contract/01-CONTEXT.md
    - /home/samuel/projects/sensor_opt/.planning/phases/02-formulation-and-theory-bridge/02-CONTEXT.md
    - /home/samuel/projects/sensor_opt/TRC-23-02333/trace_sl_results/README.md
    - /home/samuel/projects/sensor_opt/TRC-23-02333/transparent_estimator_eval.py
    - /home/samuel/projects/sensor_opt/TRC-23-02333/summarize_trace_sl_rcss.py
    - /home/samuel/projects/sensor_opt/scripts/run_stage12_pems7_228.sh
  </read_first>
  <action>
    Create `.planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md` as the phase-local coverage artifact required by D-15 and BASE-01..BASE-06. Include a table with columns `Requirement`, `Reviewer objection answered`, `Portfolio status`, `Implementation hook`, `Evaluator layout_type / artifact`, `Held-out evidence status`, and `Deferral reason / Phase 4 action`. For BASE-01, map the observability/TSLP-style proxy to `observability_proxy` and state that it is optimized for observability/coverage-style structure but compared only by held-out hidden-node reconstruction per D-03 and D-12. For BASE-02, map A-optimal and D-optimal independent rows to `greedy_a_trace` and `greedy_d_logdet` per D-09. For BASE-03, map graph sampling/GSP to `graph_sampling_laplacian` if implemented; otherwise mark `already represented` by the evaluator's existing GSP reconstruction evidence and state why no separate layout row was safe. For BASE-04, map QR/SVD/POD to `qr_pod_modes` per D-11. For BASE-05, either document a completed simple learning-based check if Task 1 added one without new dependencies, or mark `deferred with reason` because a black-box learned reconstruction method would shift the paper contribution away from transparent placement and no predeclared training protocol exists in Phase 3; include the concrete Phase 4/Phase 7 handling action and do not call it a future enhancement. For BASE-06, map multistart validation refinement to `multistart_swap_by_validation` as a named comparator or predeclared portfolio member per Phase 1 D-07..D-09 and Phase 3 D-14. Add a guardrail paragraph that Phase 3 verification deliberately does not open `TRC-23-02333/dataset/` raw files and must not commit them per D-18. Create `.planning/phases/03-baseline-portfolio/check_phase3_baseline_wiring.py` as a no-raw-data static/AST verification script. The script must read source text only from `TRC-23-02333/transparent_estimator_eval.py`, `TRC-23-02333/summarize_trace_sl_rcss.py`, `scripts/run_stage12_pems7_228.sh`, and `.planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md`; it must not open `TRC-23-02333/dataset/` paths. It should assert: Phase 3 CLI flags exist; stable layout_type strings `observability_proxy`, `greedy_a_trace`, `greedy_d_logdet`, `qr_pod_modes`, and `multistart_swap_by_validation` appear in evaluator wiring; `graph_sampling_laplacian` appears either in evaluator source or in inventory as already represented by GSP evidence; evaluator source contains both `layouts.append` and `evaluate_layout(test` after Phase 3 row definitions; summarizer source or existing grouping logic can include Phase 3 layout names; the Stage 12 launcher passes `--include-baseline-portfolio`; and the inventory covers BASE-01..BASE-06. Add a `--strict` flag only if useful, but the default command must be enough for executor verification. Do not run any PeMS/Seattle smoke command in Phase 3 verification; write the Stage 12 launcher for later Phase 4/core evidence runs but prove wiring now through compile, help, shell syntax, and the static checker.
  </action>
  <verify>
    <automated>cd /home/samuel/projects/sensor_opt && test -f .planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md && grep -E 'BASE-01|BASE-02|BASE-03|BASE-04|BASE-05|BASE-06' .planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md</automated>
    <automated>cd /home/samuel/projects/sensor_opt && python .planning/phases/03-baseline-portfolio/check_phase3_baseline_wiring.py</automated>
    <automated>cd /home/samuel/projects/sensor_opt && python TRC-23-02333/transparent_estimator_eval.py --help >/tmp/trace_sl_phase3_help.txt && grep -E -- '--include-baseline-portfolio|--include-qr-pod' /tmp/trace_sl_phase3_help.txt</automated>
  </verify>
  <acceptance_criteria>
    - Inventory covers all six BASE requirements and maps each to a reviewer objection, implementation hook, stable layout/artifact name, and held-out evidence status.
    - BASE-05 is either run as a lightweight check or explicitly deferred with a concrete reason tied to transparent-method positioning.
    - Phase 3 verification does not open PeMS/Seattle raw dataset files; full data runs are left to Phase 4/core evidence execution.
    - Static/AST schema checks prove evaluator flags, stable layout_type names, launcher wiring, summarizer hooks, and inventory coverage expose the Phase 3 portfolio surface.
  </acceptance_criteria>
  <done>Phase 3 has a machine-auditable baseline inventory plus smoke/schema evidence that the portfolio can be generated or, when data is unavailable, that executable flags and artifacts are wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args to evaluator | Untrusted command-line flags influence dataset paths, output paths, baseline inclusion, and numerical knobs. |
| Local dataset directory to experiment process | Raw traffic files remain local/ignored; Phase 3 verification must not read them and later evidence runs require explicit experiment-phase handling. |
| Generated artifact tree to git | CSV/JSON/Markdown outputs may be committed only if curated and non-sensitive; smoke outputs should be reviewed before staging. |
| Shell launcher environment to Python process | Environment overrides control seeds, budgets, candidate counts, and thread caps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `--output-dir` and Stage 12 `OUTPUT_DIR` | mitigate | Use repo-relative default under `TRC-23-02333/trace_sl_results/`; do not write outside requested output paths; preserve `set -euo pipefail`. |
| T-03-02 | Information Disclosure | `TRC-23-02333/dataset/` | mitigate | Do not read or commit raw datasets during Phase 3 verification; inventory must state raw datasets are not evidence artifacts and full data runs are deferred to Phase 4. |
| T-03-03 | Repudiation | Baseline inclusion and deferral status | mitigate | Record BASE-01..BASE-06 mapping in `03-BASELINE-INVENTORY.md` with stable layout/artifact names and Phase 4 evidence status. |
| T-03-04 | Denial of Service | Dense matrix and SVD/QR baseline computations | mitigate | Stage 12 exposes small smoke overrides and thread caps; default smoke uses reduced seeds/budgets/candidate counts. |
| T-03-05 | Spoofing/Tampering | Baseline rows that are labels without real evaluation | mitigate | Every implemented `layout_type` row must enter the existing `evaluate_layout(test, ...)` loop; documented representation/deferral is allowed, fake metrics are not. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager install is planned; use existing NumPy/pandas/SciPy/scikit-learn dependencies only. |
</threat_model>

<must_haves>
## Observable Truths
- A reviewer can see observability/TSLP-style, A/D-optimal, graph/GSP, QR/SVD/POD, learning-style, and multistart coverage in one inventory.
- A help/static-schema check shows Phase 3 baseline flags and stable `layout_type` names are wired into the existing evaluator surface without reading raw datasets.
- Implemented baselines are compared by held-out hidden-node reconstruction metrics rather than validation MAE alone.
- Learning-based reconstruction is either a completed lightweight check or a documented deferral with reason, not silently omitted.

## Required Artifacts
- `TRC-23-02333/transparent_estimator_eval.py` exposes Phase 3 baseline CLI flags and layout rows.
- `TRC-23-02333/summarize_trace_sl_rcss.py` recognizes Phase 3 baseline names in aggregate comparisons.
- `scripts/run_stage12_pems7_228.sh` defines the baseline portfolio stage with small override knobs for later evidence runs.
- `.planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md` maps BASE-01..BASE-06 to implementation/evidence status.

## Key Links
- Baseline helper functions -> `layouts` tuples -> `evaluate_layout(test, ...)` -> `metrics.csv` rows.
- Stage 12 launcher -> evaluator flags -> summarizer -> `gls_map_layout_summary.csv`.
- BASE requirements -> inventory rows -> Phase 4 evidence-generation actions.
</must_haves>

<verification>
Run after all tasks:
1. `cd /home/samuel/projects/sensor_opt && python -m py_compile TRC-23-02333/transparent_estimator_eval.py TRC-23-02333/summarize_trace_sl_rcss.py`
2. `cd /home/samuel/projects/sensor_opt && bash -n scripts/run_stage12_pems7_228.sh`
3. `cd /home/samuel/projects/sensor_opt && python TRC-23-02333/transparent_estimator_eval.py --help | grep -E -- '--include-baseline-portfolio|--include-observability-proxy|--include-graph-sampling-baseline|--include-qr-pod-baseline'`
4. `cd /home/samuel/projects/sensor_opt && grep -E 'BASE-01|BASE-02|BASE-03|BASE-04|BASE-05|BASE-06' .planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md`
5. `cd /home/samuel/projects/sensor_opt && python .planning/phases/03-baseline-portfolio/check_phase3_baseline_wiring.py`
6. Do not run PeMS/Seattle raw-data smoke commands in Phase 3 verification; Stage 12 data runs are Phase 4/core evidence work unless explicitly authorized.
</verification>

<success_criteria>
- BASE-01 through BASE-06 are all explicitly covered in code, launcher/summarizer wiring, or the baseline inventory.
- The evaluator remains the source of truth; no copied evaluator or broad refactor is created.
- A/D-optimal, QR/SVD/POD, observability proxy, graph sampling/GSP coverage, and multistart comparator labels are stable and paper-readable.
- Held-out `gls_map` reconstruction metrics remain the comparison surface for implemented layout rows.
- Raw datasets are not read or committed during Phase 3 verification; Stage 12 launcher data runs are deferred to Phase 4/core evidence unless explicitly authorized.
</success_criteria>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 3 | Build reviewer-resistant baseline set for OR, graph sampling, sparse reconstruction, learning-style alternatives, and multistart caveat | 03-01 | COVERED | Objective plus Tasks 1-3 cover implementation, wiring, and inventory. |
| REQ | BASE-01 | Observability-inspired or TSLP-style baseline with objective-difference explanation | 03-01 | COVERED | Task 1 `observability_proxy_layout`; Task 3 inventory documents mismatch and held-out evaluation. |
| REQ | BASE-02 | A-optimal and D-optimal independent baselines | 03-01 | COVERED | Task 1 preserves/exposes `greedy_a_trace` and `greedy_d_logdet`; Task 2 includes them in summaries. |
| REQ | BASE-03 | Graph sampling/GSP baseline included or justified as represented | 03-01 | COVERED | Task 1 implements `graph_sampling_laplacian` when safe; Task 3 documents representation by existing GSP evidence if not. |
| REQ | BASE-04 | QR/SVD/POD sparse sensor placement baseline | 03-01 | COVERED | Task 1 implements `qr_pod_layout` / `qr_pod_modes`. |
| REQ | BASE-05 | Simple learning-based reconstruction check or documented deferral | 03-01 | COVERED | Task 3 requires completed check or defensible documented deferral per D-13. |
| REQ | BASE-06 | Multistart validation refinement as comparator/predeclared portfolio member | 03-01 | COVERED | Task 1 and Task 3 keep `multistart_swap_by_validation` named. |
| CONTEXT | D-01 | Portfolio is claim defense, not feature expansion | 03-01 | COVERED | Objective and inventory map baselines to reviewer objections. |
| CONTEXT | D-02 | Prefer evaluator support; prose deferral only for learning/incompatible objectives | 03-01 | COVERED | Tasks implement feasible baselines; Task 3 allows only documented learning/GSP incompatibility deferral. |
| CONTEXT | D-03 | TSLP/observability still evaluated by held-out hidden-node reconstruction | 03-01 | COVERED | Task 1 routes rows through `evaluate_layout(test, ...)`. |
| CONTEXT | D-04 | Do not weaken strong TRACE-SL claim | 03-01 | COVERED | Objective and inventory strengthen claim defense. |
| CONTEXT | D-05 | Reuse `transparent_estimator_eval.py` as source of truth | 03-01 | COVERED | Task 1 modifies existing evaluator only. |
| CONTEXT | D-06 | Preserve artifact schemas and add layout_type rows | 03-01 | COVERED | Tasks keep `metrics.csv`, `layouts.json`, and layout rows. |
| CONTEXT | D-07 | Run-script wiring only for reusable paper-facing baseline stage | 03-01 | COVERED | Task 2 creates Stage 12 launcher only. |
| CONTEXT | D-08 | Validate with small smoke and artifact schema checks | 03-01 | COVERED | Task 3 verification includes smoke/schema checks. |
| CONTEXT | D-09 | A/D-optimal as independent rows | 03-01 | COVERED | Task 1 and Task 2. |
| CONTEXT | D-10 | Graph sampling/GSP by compatible baseline or existing evidence tie | 03-01 | COVERED | Task 1 and Task 3. |
| CONTEXT | D-11 | QR/SVD/POD sparse placement where compatible | 03-01 | COVERED | Task 1. |
| CONTEXT | D-12 | Observability proxy and objective mismatch documentation | 03-01 | COVERED | Task 1 and Task 3. |
| CONTEXT | D-13 | Learning check lightweight or documented deferral | 03-01 | COVERED | Task 3. |
| CONTEXT | D-14 | Multistart validation refinement named comparator/portfolio member | 03-01 | COVERED | Task 1 and Task 3. |
| CONTEXT | D-15 | Baseline inventory/coverage artifact | 03-01 | COVERED | Task 3 creates `03-BASELINE-INVENTORY.md`. |
| CONTEXT | D-16 | Stable paper-readable baseline names | 03-01 | COVERED | Tasks specify stable `layout_type` names. |
| CONTEXT | D-17 | Validation MAE selection only; claims rely on held-out test metrics | 03-01 | COVERED | Task 1 and verification preserve held-out evaluation. |
| CONTEXT | D-18 | Raw datasets stay local/ignored | 03-01 | COVERED | Threat model, Task 3, and success criteria. |
| RESEARCH | PATTERNS | Existing evaluator, summarizer, launcher, and inventory patterns | 03-01 | COVERED | Tasks directly reference 03-PATTERNS.md analogs. |
</source_audit>

<output>
Create `/home/samuel/projects/sensor_opt/.planning/phases/03-baseline-portfolio/03-01-SUMMARY.md` when done.
</output>
