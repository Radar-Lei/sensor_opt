---
phase: 08.5-stage12-performance-unblock
plan: 02
type: execute
wave: 2
depends_on:
  - 08.5-01
files_modified:
  - TRC-23-02333/transparent_estimator_eval.py
  - tests/test_stage12_runtime_trace_cache.py
autonomous: true
requirements:
  - RUN-02
must_haves:
  truths:
    - "Posterior and scenario trace scoring avoid repeated full N×N inversions for candidate layouts where cached base inverses are valid."
    - "Trace, condition number, and scenario CVaR values remain numerically equivalent within documented tolerances."
    - "Selection semantics are not silently changed; any numerical tolerance is explicit in tests."
  artifacts:
    - path: "TRC-23-02333/transparent_estimator_eval.py"
      provides: "Posterior/scenario trace cache helpers wired into RCSS scoring and validation swap"
      contains: "posterior_trace_for_layout"
    - path: "tests/test_stage12_runtime_trace_cache.py"
      provides: "Synthetic parity tests for cached trace metrics"
      contains: "unittest"
  key_links:
    - from: "make_rcss_row"
      to: "posterior_trace_for_layout / scenario_cvar_trace_for_layout"
      via: "candidate certificate diagnostics"
      pattern: "trace_cache"
    - from: "validation_swap_search"
      to: "posterior_inverse_for_layout / posterior_trace_for_layout"
      via: "remove/add pool scoring"
      pattern: "posterior"
---

<objective>
Replace repeated posterior/scenario full inversions during RCSS candidate and validation-swap scoring with reusable cache-aware metric helpers.

Purpose: Per D-07, Stage12 candidate scoring needs posterior/scenario trace caching or Woodbury-style reuse where numerically appropriate. This plan preserves selection metrics and documents parity before launcher/full-seed work.
Output: Updated evaluator with explicit cache plumbing plus synthetic parity tests for posterior trace, condition number, and scenario CVaR trace.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.5-stage12-performance-unblock/08.5-CONTEXT.md
@.planning/phases/08.5-stage12-performance-unblock/08.5-PATTERNS.md
@.planning/phases/08-external-stage12-evidence/08-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@TRC-23-02333/transparent_estimator_eval.py

<interfaces>
Executor starts after Plan 01, so preserve its selected-method validation and solve_quadratic fast paths.
Relevant current contracts:
- posterior_inverse_for_layout(base_matrix, sensors, obs_weight) returns the posterior inverse for one base matrix and layout.
- posterior_trace_for_layout(base_matrix, sensors, obs_weight) returns a float trace.
- posterior_condition_for_layout(base_matrix, sensors, obs_weight) returns a float condition number.
- scenario_cvar_trace_for_layout(base_matrices, sensors, obs_weight, tail_fraction) returns tail-mean trace across scenario base matrices.
- make_rcss_row(...) creates validation_mae, posterior_trace, scenario_cvar_trace, condition_number, and coverage_penalty for each candidate row.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cached posterior metric parity tests</name>
  <files>tests/test_stage12_runtime_trace_cache.py</files>
  <read_first>
    - @.planning/codebase/TESTING.md
    - @.planning/phases/08.5-stage12-performance-unblock/08.5-PATTERNS.md
    - @TRC-23-02333/transparent_estimator_eval.py
  </read_first>
  <behavior>
    - Cached posterior trace equals direct linalg.inv trace for multiple synthetic layouts.
    - Cached scenario CVaR trace equals direct scenario_cvar_trace_for_layout semantics for multiple SPD scenario matrices.
    - Cached condition number equals direct condition-number semantics within tolerance.
    - Repeated calls for the same base matrix and layout reuse the cache interface without changing returned values.
  </behavior>
  <action>Create a unittest module with deterministic SPD matrices and layout arrays. Use direct inverse calculations as the oracle. The tests should accept helper names chosen by the executor, but they must exercise the public functions that make_rcss_row and validation_swap_search use after caching is wired. Do not use raw traffic datasets or full Stage12 runs.</action>
  <verify>
    <automated>python -m unittest tests.test_stage12_runtime_trace_cache -v</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and uses synthetic matrices only.
    - Tests assert numerical tolerance of rtol=1e-8 and atol=1e-8 or stricter for trace/CVaR and rtol=1e-7 and atol=1e-7 or stricter for condition number.
    - Tests fail if cached helper returns a different metric definition or changes tail_fraction behavior.
  </acceptance_criteria>
  <done>Cache parity tests define the acceptable numerical envelope before evaluator changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cache-aware posterior and scenario metrics</name>
  <files>TRC-23-02333/transparent_estimator_eval.py, tests/test_stage12_runtime_trace_cache.py</files>
  <read_first>
    - @TRC-23-02333/transparent_estimator_eval.py
    - @.planning/phases/08.5-stage12-performance-unblock/08.5-CONTEXT.md
    - @.planning/phases/08.5-stage12-performance-unblock/08.5-PATTERNS.md
  </read_first>
  <action>Add explicit cache plumbing for posterior/scenario metrics per D-07 without introducing mutable global experiment state. Prefer top-level helper functions near posterior_inverse_for_layout that accept an optional cache dictionary or lightweight cache object passed through make_rcss_row/build_rcss_rows/validation_swap_search. Cache base_matrix inverses or reusable factorizations keyed by stable local identifiers, and evaluate layout-specific posterior metrics through a Woodbury/Sherman-Morrison-style selected-sensor system when numerically valid. Keep direct inversion fallback available for invalid cache inputs or nonpositive weights. Do not change posterior_trace, scenario_cvar_trace, condition_number, or coverage_penalty definitions; do not add validation-swap top-K proxy filtering in this plan because D-09 fences that as optional search-change work.</action>
  <verify>
    <automated>python -m unittest tests.test_stage12_runtime_trace_cache -v</automated>
    <automated>python -m unittest tests.test_stage12_runtime_fast_paths tests.test_stage12_runtime_trace_cache -v</automated>
  </verify>
  <acceptance_criteria>
    - make_rcss_row/build_rcss_rows uses a passed cache for posterior_trace and scenario_cvar_trace rather than constructing a fresh full inverse for every row when cache is valid.
    - validation_swap_search reuses the cache for remove_pool/add_pool posterior scoring where it already calls posterior trace/inverse helpers.
    - Direct fallback remains for numerically inappropriate cache use and preserves current semantics.
    - No selected layouts, score weights, or candidate filtering logic is intentionally changed beyond numerical tolerance.
  </acceptance_criteria>
  <done>Posterior/scenario trace scoring has cache-aware reuse while preserving metric definitions and selection semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify smoke parity and evidence-gate noninterference</name>
  <files>TRC-23-02333/transparent_estimator_eval.py, tests/test_stage12_runtime_trace_cache.py</files>
  <read_first>
    - @TRC-23-02333/transparent_estimator_eval.py
    - @TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json
    - @TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json
    - @.planning/phases/08-external-stage12-evidence/08-CONTEXT.md
  </read_first>
  <action>Run a PeMS7_228 smoke evaluator command with RCSS enabled and confirm cache wiring does not break artifact schema. Confirm this plan does not edit external_evidence_gate.json, external evidence contracts, or Seattle stage12_status.json; those gates must remain fail-closed per D-12 and D-13. If the smoke command cannot run because local raw datasets are unavailable, record the missing dataset path in the plan summary and rely on unit tests plus DRY_RUN launcher checks from later plans; do not fabricate outputs.</action>
  <verify>
    <automated>python -m unittest tests.test_stage12_runtime_fast_paths tests.test_stage12_runtime_trace_cache -v</automated>
    <automated>python TRC-23-02333/transparent_estimator_eval.py --data-root TRC-23-02333/dataset/PeMS7_228 --output-dir /tmp/trace_sl_phase085_plan02_smoke --budgets "0.10" --num-layouts 1 --max-test-steps 1 --include-simple-baselines --include-rcss --rcss-random-candidates 1 --rcss-quality-candidates 1 --scenario-count 1</automated>
    <automated>git diff -- TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.csv TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.json TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json</automated>
  </verify>
  <acceptance_criteria>
    - Unit tests pass.
    - Smoke output, when dataset is present, contains metrics.csv, rcss_candidates.csv, certificate_correlations.csv, config.json, and SUMMARY.md.
    - git diff for fail-closed gate/status artifacts is empty.
    - No claim-facing artifact marks EVID-03 or EVID-04 complete.
  </acceptance_criteria>
  <done>Cached metric implementation is verified on synthetic tests and a small evaluator smoke path without altering evidence gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| numerical cache → selection metrics | Cache-derived values feed RCSS ranking and validation swap scoring. |
| local raw data → smoke outputs | Smoke command may read ignored local datasets and write temporary artifacts. |
| runtime feasibility → evidence claims | Runtime improvements can be mistaken for completed Stage12 evidence if gates are modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.5-05 | Tampering | posterior/scenario cache | mitigate | Parity tests compare cached metrics to direct inverse definitions within explicit tolerance. |
| T-08.5-06 | Repudiation | metric semantics | mitigate | Acceptance requires no intentional candidate filtering, weight, or layout selection changes; D-10 search-change fence remains intact. |
| T-08.5-07 | Information Disclosure | smoke commands | mitigate | Outputs write to /tmp and tests use synthetic matrices; no raw datasets are copied or committed. |
| T-08.5-08 | Elevation of Privilege | evidence gate | mitigate | Automated git diff check proves fail-closed gate/status artifacts remain unchanged. |
| T-08.5-SC | Tampering | package installs | accept | No npm/pip/cargo install tasks are present. |
</threat_model>

<verification>
Overall checks for this plan:
- python -m unittest tests.test_stage12_runtime_fast_paths tests.test_stage12_runtime_trace_cache -v
- python TRC-23-02333/transparent_estimator_eval.py --data-root TRC-23-02333/dataset/PeMS7_228 --output-dir /tmp/trace_sl_phase085_plan02_smoke --budgets "0.10" --num-layouts 1 --max-test-steps 1 --include-simple-baselines --include-rcss --rcss-random-candidates 1 --rcss-quality-candidates 1 --scenario-count 1
- git diff -- TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.csv TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.json TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json
</verification>

<success_criteria>
RUN-02 is covered by cached posterior/scenario trace scoring and dense-solve fast paths from Plan 01. Metric parity is documented by automated tests, and no evidence gate semantics are changed.
</success_criteria>

<output>
Create `.planning/phases/08.5-stage12-performance-unblock/08.5-02-SUMMARY.md` when done.
</output>
