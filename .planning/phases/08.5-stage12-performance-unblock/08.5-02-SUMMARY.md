---
phase: 08.5-stage12-performance-unblock
plan: 02
subsystem: runtime
tags: [python, numpy, scipy, unittest, stage12, trace-cache, rcss]

requires:
  - phase: 08.5-stage12-performance-unblock
    provides: Selected-method validation MAE and constant observation-weight solve fast paths
provides:
  - Cache-aware posterior trace, condition-number, and scenario CVaR helpers
  - Woodbury-style selected-sensor posterior metric reuse for RCSS and validation-swap scoring
  - Synthetic parity tests plus PeMS7_228 RCSS smoke verification without evidence-gate edits
affects: [08.5-stage12-performance-unblock, RUN-02, stage12-runtime]

tech-stack:
  added: []
  patterns:
    - Optional local trace_cache dictionaries passed through evaluator helper call chains
    - Woodbury posterior updates from cached base inverses for scalar positive observation weights
    - Direct inverse fallback for cache-ineligible posterior metric calls

key-files:
  created:
    - tests/test_stage12_runtime_trace_cache.py
  modified:
    - TRC-23-02333/transparent_estimator_eval.py
    - tests/test_stage12_runtime_trace_cache.py

key-decisions:
  - "Posterior/scenario metric caching is explicit and local, using optional trace_cache dictionaries instead of mutable module-level experiment state."
  - "Cached posterior trace uses a selected-sensor Woodbury system for scalar positive observation weights, with direct inversion fallback for cache-ineligible inputs."
  - "RCSS candidate scoring and validation-swap remove/add pool scoring reuse the same cache without adding top-K proxy filtering or changing search semantics."

patterns-established:
  - "Trace cache entries are grouped by base, trace, and condition buckets keyed by matrix identity plus sorted sensor tuple and scalar observation weight."
  - "Synthetic Stage12 runtime tests import the monolithic evaluator from file path and compare cache-aware metrics to direct inverse oracles."

requirements-completed: [RUN-02]

duration: 6min
completed: 2026-05-25
---

# Phase 08.5 Plan 02: Cache-aware posterior/scenario trace scoring Summary

**Reusable local trace-cache helpers for posterior trace, condition number, and scenario CVaR scoring in Stage12 RCSS paths**

## Performance

- **Duration:** 6 min
- **Started:** 2026-05-25T08:24:37Z
- **Completed:** 2026-05-25T08:30:31Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Added synthetic unittest coverage proving cache-aware posterior trace, condition number, scenario CVaR, and RCSS row metrics match direct inverse definitions within the plan tolerances.
- Implemented optional cache plumbing around posterior helpers using cached base inverses and selected-sensor Woodbury systems for scalar positive observation weights.
- Wired the cache through RCSS row construction, auto-weight selection, direct RCSS selection, layout trace recording, and validation-swap remove/add pool scoring while preserving fail-closed evidence gates.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add cached posterior metric parity tests** - `1b9cb98` (test)
2. **Task 2: Implement cache-aware posterior and scenario metrics** - `cd1edfb` (feat)
3. **Task 3: Verify smoke parity and evidence-gate noninterference** - `da129b7` (test)

**Plan metadata:** pending final docs commit

_Note: TDD sequence used a RED test commit followed by GREEN implementation and verification integration commits._

## Files Created/Modified

- `tests/test_stage12_runtime_trace_cache.py` - Synthetic unittest module for posterior trace, scenario CVaR trace, condition number, repeated cache calls, and RCSS row cache integration.
- `TRC-23-02333/transparent_estimator_eval.py` - Optional local trace-cache helpers plus cache wiring for RCSS and validation-swap scoring.

## Decisions Made

- Kept cache state explicit and local by passing `trace_cache` dictionaries through helper calls rather than introducing mutable global state.
- Used matrix object identity as a stable per-process local cache key because base/scenario matrices are constructed once per evaluator run and reused across candidate scoring.
- Preserved D-09 by not adding validation-swap top-K proxy filtering; only existing posterior trace and inverse scoring calls were made cache-aware.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- RED tests failed as expected before implementation because existing metric helper signatures did not accept `trace_cache`.
- Task 3 had no production-code changes after smoke verification, so a small RCSS row cache integration assertion was added to the synthetic test module to keep the task commit concrete and traceable.

## Verification

- `python -m unittest tests.test_stage12_runtime_trace_cache -v` — passed.
- `python -m unittest tests.test_stage12_runtime_fast_paths tests.test_stage12_runtime_trace_cache -v` — passed.
- `python TRC-23-02333/transparent_estimator_eval.py --data-root TRC-23-02333/dataset/PeMS7_228 --output-dir /tmp/trace_sl_phase085_plan02_smoke --budgets "0.10" --num-layouts 1 --max-test-steps 1 --include-simple-baselines --include-rcss --rcss-random-candidates 1 --rcss-quality-candidates 1 --scenario-count 1` — passed and produced `metrics.csv`, `rcss_candidates.csv`, `certificate_correlations.csv`, `config.json`, and `SUMMARY.md` under `/tmp/trace_sl_phase085_plan02_smoke`.
- `git diff --quiet -- TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.csv TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.json TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json` — passed.

## Known Stubs

None.

## Threat Flags

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- RUN-02 is now covered by Plan 01 dense solve fast paths and this plan's posterior/scenario trace cache scoring.
- Plan 08.5-03 can add progress/checkpoint diagnostics on top of the cached evaluator path.
- External evidence gate artifacts remain fail-closed; EVID-03 and EVID-04 are still incomplete until full ten-split aggregate evidence exists.

---
*Phase: 08.5-stage12-performance-unblock*
*Completed: 2026-05-25*
