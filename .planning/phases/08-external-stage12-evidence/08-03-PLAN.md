---
phase: 08-external-stage12-evidence
plan: 3
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/SUMMARY.md
  - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/combined_metrics.csv
  - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/gls_map_layout_summary.csv
  - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/gls_map_delta_summary.csv
  - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/gls_map_paired_delta_tests.csv
  - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/gls_map_win_counts.csv
  - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/rcss_selected_sources.csv
  - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json
autonomous: true
requirements: [EVID-04]
must_haves:
  truths:
    - "Author can inspect completed Seattle Stage12 ten-split external/transfer-style held-out evidence before Seattle appears in any core claim artifact."
    - "Seattle always has a machine-readable completed or blocked status artifact after this plan, so Plan 04 can generate a fail-closed gate."
    - "Seattle evidence is traceable to committed aggregate summaries and launcher scripts, not raw traffic datasets."
  artifacts:
    - path: "TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/combined_metrics.csv"
      provides: "All Seattle Stage12 seed-level evaluation rows when completed"
      contains: "split_seed"
    - path: "TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/gls_map_layout_summary.csv"
      provides: "Seattle Stage12 budget/layout aggregate table when completed"
      contains: "validation_swap_selected"
    - path: "TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json"
      provides: "Machine-readable Seattle completed/blocked status consumed by Plan 04"
      contains: "evid_04_complete"
    - path: "TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/SUMMARY.md"
      provides: "Generated human-readable aggregate view or blocker summary"
  key_links:
    - from: "scripts/run_stage12_seattle.sh"
      to: "TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/combined_metrics.csv"
      via: "real Stage12 execution and summarizer"
      pattern: "seattle_stage12_baseline_portfolio"
    - from: "TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json"
      to: "Plan 04 external_evidence_gate.json"
      via: "completed or blocker status"
      pattern: "evid_04_complete"
---

<objective>
Generate and curate real Seattle Stage12 ten-split external/transfer-style evidence, or produce a machine-readable Seattle blocker.

Purpose: EVID-04 requires Seattle evidence before Seattle can appear in any core claim artifact; if the real run cannot complete, the phase must preserve a blocker rather than claim success.
Output: Committed Seattle Stage12 aggregate CSV/Markdown artifacts under `TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/`, or `stage12_status.json` plus a blocker summary consumed by Plan 04 gate generation.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-external-stage12-evidence/08-CONTEXT.md
@.planning/phases/08-external-stage12-evidence/08-PATTERNS.md
@scripts/run_stage12_seattle.sh
@TRC-23-02333/summarize_trace_sl_rcss.py

<interfaces>
Seattle Stage12 evidence must be produced by `scripts/run_stage12_seattle.sh` from local ignored inputs at `TRC-23-02333/dataset/Seattle/`, including the tensor/adjacency files expected by `load_seattle_dataset`. The summarizer emits top-level aggregate files including `combined_metrics.csv`, `gls_map_layout_summary.csv`, `gls_map_delta_summary.csv`, `gls_map_paired_delta_tests.csv`, `gls_map_win_counts.csv`, `rcss_selected_sources.csv`, and `SUMMARY.md` when completed. Regardless of success or failure, this plan must create `TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json` with `dataset=Seattle`, `requirement_id=EVID-04`, `status` equal to `completed` or `blocked`, `evid_04_complete` boolean, `seattle_core_claim_blocked` boolean, `required_split_count=10`, `actual_split_count`, `required_artifacts`, and `blocker_reason` when blocked. Plan 04 consumes this JSON and must fail closed when `evid_04_complete=false`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Seattle Stage12 or write machine-readable blocker status</name>
  <read_first>
    - scripts/run_stage12_seattle.sh
    - TRC-23-02333/summarize_trace_sl_rcss.py
    - .planning/phases/08-external-stage12-evidence/08-CONTEXT.md
  </read_first>
  <files>TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/, TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json</files>
  <action>Execute the real Seattle Stage12 launcher from the repository root with default ten split seeds and baseline-portfolio flags. Before the full run, use `DRY_RUN=1` only as a syntax/scope check; do not mark completion from dry-run output. If local raw Seattle files are missing or the run cannot complete in the available environment, create the output directory, write `stage12_status.json` with `status=blocked`, `evid_04_complete=false`, `seattle_core_claim_blocked=true`, `v1_1_completion_allowed=false`, `actual_split_count` from any existing aggregate if safely available or 0 otherwise, and a concrete `blocker_reason`; also write a concise blocker `SUMMARY.md` stating that EVID-04 remains incomplete and Seattle stays excluded from core claims. Do not create fake aggregate CSV evidence. If the run completes, ensure the summarizer has produced the required aggregate files and write `stage12_status.json` with `status=completed`, `evid_04_complete=true`, and the measured split count. Do not read, delete, print, move, or commit raw Seattle dataset files.</action>
  <verify>
    <automated>DRY_RUN=1 bash scripts/run_stage12_seattle.sh &gt; /tmp/seattle_stage12.verify &amp;&amp; test "$(grep -c 'transparent_estimator_eval.py' /tmp/seattle_stage12.verify)" -eq 10 &amp;&amp; (bash scripts/run_stage12_seattle.sh || python - &lt;&lt;'PY'
from pathlib import Path
import json
root = Path('TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio')
root.mkdir(parents=True, exist_ok=True)
payload = {
    'dataset': 'Seattle',
    'requirement_id': 'EVID-04',
    'status': 'blocked',
    'evid_04_complete': False,
    'seattle_core_claim_blocked': True,
    'v1_1_completion_allowed': False,
    'required_split_count': 10,
    'actual_split_count': 0,
    'required_artifacts': ['SUMMARY.md','combined_metrics.csv','gls_map_layout_summary.csv','gls_map_delta_summary.csv','gls_map_paired_delta_tests.csv','gls_map_win_counts.csv','rcss_selected_sources.csv'],
    'blocker_reason': 'Seattle Stage12 real run did not complete in the available environment; EVID-04 remains incomplete and Seattle is excluded from core claims.',
}
(root / 'stage12_status.json').write_text(json.dumps(payload, indent=2, sort_keys=True) + '\n', encoding='utf-8')
(root / 'SUMMARY.md').write_text('# Seattle Stage12 Evidence Blocker\n\nEVID-04 remains incomplete. Seattle Stage12 ten-split evidence did not complete in the available environment; Seattle stays excluded from core claims until real aggregate evidence is generated and tracked.\n', encoding='utf-8')
PY
) &amp;&amp; python - &lt;&lt;'PY'
from pathlib import Path
import json
status = json.loads(Path('TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json').read_text())
if status.get('dataset') != 'Seattle' or status.get('requirement_id') != 'EVID-04':
    raise SystemExit('Seattle status missing dataset/requirement identity')
if status.get('status') not in {'completed', 'blocked'}:
    raise SystemExit(f'invalid Seattle status: {status.get("status")}')
if status.get('status') == 'blocked' and (status.get('evid_04_complete') is not False or status.get('seattle_core_claim_blocked') is not True or not status.get('blocker_reason')):
    raise SystemExit('blocked Seattle status must fail closed with blocker_reason')
print('seattle status artifact validation ok')
PY</automated>
  </verify>
  <acceptance_criteria>Real run exits 0 and status reports completed with ten `seed_*` directories plus nonempty aggregate CSV/Markdown files, or status reports blocked with EVID-04 incomplete, Seattle core claims blocked, and no fake aggregate evidence.</acceptance_criteria>
  <done>Seattle Stage12 ten-split aggregate evidence exists, or Seattle has a machine-readable fail-closed blocker status for Plan 04.</done>
</task>

<task type="auto">
  <name>Task 2: Validate and stage Seattle aggregate or blocker provenance</name>
  <read_first>
    - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json
    - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/SUMMARY.md
    - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/combined_metrics.csv
    - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/gls_map_layout_summary.csv
    - TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/gls_map_paired_delta_tests.csv
    - scripts/run_stage12_seattle.sh
  </read_first>
  <files>TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/</files>
  <action>Load `stage12_status.json` first. If `status=completed`, run a machine validation over the Seattle Stage12 aggregate files: verify exactly ten split seeds in `combined_metrics.csv`, budgets `0.10`, `0.20`, and `0.30`, presence of `validation_swap_selected` plus reviewer-facing baselines including `best_random_by_validation`, `random`, `top_variance`, `greedy_a_trace`, `observability_proxy`, `graph_sampling_laplacian`, and `qr_pod_modes` where evaluator output supports them, and paired-test CSV schema without fabricated values. If validation fails, rewrite `stage12_status.json` to `status=blocked`, `evid_04_complete=false`, and a concrete `blocker_reason`; do not leave a completed status. If `status=blocked`, verify `SUMMARY.md` is a blocker summary and that no task has fabricated required aggregate CSVs. In both branches, check `git status --short -- TRC-23-02333/dataset` is empty, then explicitly stage `stage12_status.json` and `SUMMARY.md`; additionally stage required aggregate CSVs only when status remains completed. Plan 04 must treat Seattle as incomplete unless `stage12_status.json` says completed and `git ls-files --error-unmatch` succeeds for all completed aggregate source files after commit.</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
import json
import pandas as pd
root = Path('TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio')
status_path = root / 'stage12_status.json'
if not status_path.exists():
    raise SystemExit('missing Seattle stage12_status.json')
status = json.loads(status_path.read_text())
required = ['SUMMARY.md','combined_metrics.csv','gls_map_layout_summary.csv','gls_map_delta_summary.csv','gls_map_paired_delta_tests.csv','gls_map_win_counts.csv','rcss_selected_sources.csv']
if status.get('status') == 'completed':
    missing = [name for name in required if not (root / name).exists() or (root / name).stat().st_size == 0]
    if missing:
        status.update({'status': 'blocked', 'evid_04_complete': False, 'seattle_core_claim_blocked': True, 'v1_1_completion_allowed': False, 'blocker_reason': f'completed status invalid because aggregate files are missing/nonempty: {missing}'})
        status_path.write_text(json.dumps(status, indent=2, sort_keys=True) + '\n', encoding='utf-8')
        raise SystemExit(status['blocker_reason'])
    combined = pd.read_csv(root / 'combined_metrics.csv')
    seeds = sorted(combined['split_seed'].dropna().astype(int).unique())
    if seeds != list(range(25, 35)):
        raise SystemExit(f'expected split seeds 25..34, got {seeds}')
    budgets = {round(float(v), 2) for v in combined['budget'].dropna().unique()}
    if budgets != {0.10, 0.20, 0.30}:
        raise SystemExit(f'expected budgets 0.10/0.20/0.30, got {budgets}')
    layout = pd.read_csv(root / 'gls_map_layout_summary.csv')
    labels = set(layout['layout_type'].dropna().astype(str))
    required_labels = {'validation_swap_selected','best_random_by_validation','random','top_variance','greedy_a_trace','observability_proxy','graph_sampling_laplacian','qr_pod_modes'}
    missing_labels = sorted(required_labels - labels)
    if missing_labels:
        raise SystemExit(f'missing required layout labels: {missing_labels}')
    paired = pd.read_csv(root / 'gls_map_paired_delta_tests.csv')
    required_paired_cols = {'budget','layout','baseline','delta_mean','paired_t_p','wilcoxon_p','win_count'}
    if not required_paired_cols.issubset(paired.columns):
        raise SystemExit(f'missing paired columns: {sorted(required_paired_cols - set(paired.columns))}')
    if status.get('evid_04_complete') is not True or status.get('seattle_core_claim_blocked') is not False:
        raise SystemExit('completed Seattle status must mark EVID-04 complete and unblock Seattle core claims')
    print('seattle stage12 aggregate validation ok')
elif status.get('status') == 'blocked':
    if status.get('evid_04_complete') is not False or status.get('seattle_core_claim_blocked') is not True or not status.get('blocker_reason'):
        raise SystemExit('blocked Seattle status must fail closed with blocker_reason')
    summary = (root / 'SUMMARY.md').read_text(encoding='utf-8')
    if 'EVID-04' not in summary or 'incomplete' not in summary.lower():
        raise SystemExit('blocked Seattle SUMMARY must state EVID-04 incomplete')
    print('seattle blocker status validation ok')
else:
    raise SystemExit(f'invalid Seattle status: {status.get("status")}')
PY

test -z "$(git status --short -- TRC-23-02333/dataset)" &amp;&amp; git add TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/SUMMARY.md &amp;&amp; python - &lt;&lt;'PY'
from pathlib import Path
import json
status = json.loads(Path('TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json').read_text())
if status.get('status') == 'completed':
    import subprocess
    files = ['combined_metrics.csv','gls_map_layout_summary.csv','gls_map_delta_summary.csv','gls_map_paired_delta_tests.csv','gls_map_win_counts.csv','rcss_selected_sources.csv']
    subprocess.run(['git', 'add', *[f'TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/{name}' for name in files]], check=True)
PY

git diff --cached --name-only -- TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio | grep -E '^TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/(stage12_status.json|SUMMARY.md|combined_metrics.csv|gls_map_layout_summary.csv|gls_map_delta_summary.csv|gls_map_paired_delta_tests.csv|gls_map_win_counts.csv|rcss_selected_sources.csv)$'</automated>
  </verify>
  <acceptance_criteria>Validation proves completed Seattle ten-split aggregates or a blocked status; required provenance artifacts are staged explicitly; raw dataset git status is clean; failed validation becomes a blocker, not partial completion.</acceptance_criteria>
  <done>Seattle Stage12 evidence or blocker status is ready for Plan 04 gate generation with machine-readable provenance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ignored Seattle raw data -&gt; evaluator | Local tensor/adjacency inputs drive numerical results but must stay out of git and evidence manifests. |
| evaluator outputs -&gt; core-claim gate | Seattle completion status controls whether v1.1 can proceed with Seattle in core claims. |
| Seattle status JSON -&gt; gate generator | Blocked or completed status must be consumed as machine-readable truth by Plan 04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Repudiation | Seattle aggregate files/status | mitigate | Verify split seed set, budgets, layout labels, required aggregate file sizes, and status booleans with parsers. |
| T-08-08 | Information Disclosure | `TRC-23-02333/dataset/Seattle` | mitigate | Never read raw contents for validation and require clean git status under `TRC-23-02333/dataset`. |
| T-08-09 | Elevation of Privilege | Seattle claim eligibility | mitigate | Treat validation failure, missing run, untracked aggregates, or blocked status as a blocker consumed by Plan 04 gate; never infer core eligibility from Stage11 or DRY_RUN. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks are planned in this phase. |
</threat_model>

<verification>
Real Stage12 run completes and status validates completed evidence, or `stage12_status.json` validates a blocked Seattle path; `git status --short -- TRC-23-02333/dataset` prints nothing; required status/aggregate provenance files are staged explicitly.
</verification>

<success_criteria>
- `seattle_stage12_baseline_portfolio` contains ten-split aggregate evidence generated from real evaluator runs, or a machine-readable Seattle blocker.
- The aggregate evidence includes required budgets, reviewer-facing baselines, paired-stat schema, and generated summary artifacts when complete.
- EVID-04 is not marked complete unless real aggregate validation passes, `stage12_status.json` says completed, and required aggregate source files become git-tracked before Plan 04 gate generation.
</success_criteria>

<output>
Create `.planning/phases/08-external-stage12-evidence/08-03-SUMMARY.md` when done.
</output>
