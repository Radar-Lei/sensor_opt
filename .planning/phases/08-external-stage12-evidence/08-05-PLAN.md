---
phase: 08-external-stage12-evidence
plan: 5
type: execute
wave: 4
depends_on: [08-04]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [EVID-03, EVID-04]
must_haves:
  truths:
    - "Project planning state reflects whether PeMS7_1026 Stage12 ten-split evidence completed or remains pending tracking."
    - "Project planning state reflects whether Seattle Stage12 ten-split evidence completed, is pending tracking, or remains a v1.1 blocker."
    - "Phase 9 EVID-05 remains pending and no manuscript drafting scope is introduced."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Current milestone status and blockers/concerns"
      contains: "Phase 8"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 plan list and completion conditions"
      contains: "08-05-PLAN.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "EVID-03/EVID-04 checkbox truth matching gate results"
      contains: "EVID-04"
  key_links:
    - from: "TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json"
      to: ".planning/STATE.md"
      via: "blocked/pending/completed Phase 8 status"
      pattern: "v1_1_completion_allowed"
    - from: "external evidence contract/gate"
      to: ".planning/REQUIREMENTS.md"
      via: "EVID-03/EVID-04 completion checkboxes"
      pattern: "EVID-03|EVID-04"
---

<objective>
Synchronize Phase 8 planning status with the machine gate.

Purpose: The repository must not let v1.1 proceed with stale planning state if Seattle Stage12 evidence is missing, untracked, or excluded from core claims, and must not prematurely mark EVID-03/EVID-04 complete.
Output: Updated planning metadata that reflects the generated gate and aggregate evidence status without manuscript prose or Phase 9 classification work.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-external-stage12-evidence/08-CONTEXT.md
@TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json
@TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.csv
@TRC-23-02333/trace_sl_results/seattle_stage12_baseline_portfolio/stage12_status.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update roadmap Phase 8 plan list and gate-aware status</name>
  <read_first>
    - .planning/ROADMAP.md
    - TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json
    - TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.csv
    - .planning/phases/08-external-stage12-evidence/08-04-SUMMARY.md
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>Update the Phase 8 `Plans:` section to list all five Phase 8 plan files and wave grouping. Keep the Phase 8 goal and success criteria intact. If `external_evidence_gate.json` has `v1_1_completion_allowed=true`, mark Phase 8 plans complete only if all plan summaries exist, all expected artifacts exist, and the gate has no `pending_tracking_sources`. If the gate blocks Seattle, keep Phase 8 not complete and add a concise blocker note under Phase 8 success criteria or progress that says Seattle Stage12 ten-split evidence is missing/incomplete and v1.1 cannot complete with Seattle in core claims. If the gate reports `pending_tracking` for PeMS7_1026 or Seattle, keep Phase 8 not complete and record that generated aggregate evidence must be committed/tracked before completion. Do not add dataset classification language reserved for Phase 9 and do not draft manuscript prose.</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
import json, re
roadmap = Path('.planning/ROADMAP.md').read_text()
gate = json.loads(Path('TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json').read_text())
for name in ['08-01-PLAN.md','08-02-PLAN.md','08-03-PLAN.md','08-04-PLAN.md','08-05-PLAN.md']:
    if name not in roadmap:
        raise SystemExit(f'missing Phase 8 plan in ROADMAP: {name}')
phase8 = re.search(r'### Phase 8:.*?### Phase 9:', roadmap, flags=re.S).group(0)
if gate.get('seattle_core_claim_blocked') and 'Seattle' not in phase8:
    raise SystemExit('Seattle blocker not reflected in ROADMAP Phase 8 text')
if any((info.get('evidence_status') == 'pending_tracking') for info in gate.get('datasets', {}).values()) and 'pending' not in phase8.lower() and 'tracking' not in phase8.lower():
    raise SystemExit('pending tracking status not reflected in ROADMAP Phase 8 text')
if 'EVID-05' in phase8:
    raise SystemExit('Phase 8 roadmap section should not claim EVID-05')
print('roadmap phase8 status validation ok')
PY</automated>
  </verify>
  <acceptance_criteria>ROADMAP lists Phase 8 plans and reflects completed, blocked, or pending-tracking gate status without modifying Phase 8 goal scope or Phase 9 requirements.</acceptance_criteria>
  <done>Roadmap is synchronized with Phase 8 plan/gate truth.</done>
</task>

<task type="auto">
  <name>Task 2: Update requirements and state from gate truth</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json
    - TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.csv
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Parse `external_evidence_gate.json` and update only EVID-03/EVID-04 checkboxes according to real completion: mark EVID-03 complete only when `pems7_1026_stage12_complete=true` and no PeMS7_1026 pending-tracking sources exist; mark EVID-04 complete only when `seattle_stage12_complete=true`, `seattle_core_claim_blocked=false`, and no Seattle pending-tracking sources exist. Keep EVID-05 pending for Phase 9. Update `.planning/STATE.md` current focus/status/blockers so it either records Phase 8 completed and ready for Phase 9, records explicit pending-tracking work that prevents completion, or records the explicit Seattle blocker and prevents v1.1 completion. Preserve existing decisions about raw datasets and no manuscript drafting.</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
import json, re
gate = json.loads(Path('TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json').read_text())
req = Path('.planning/REQUIREMENTS.md').read_text()
state = Path('.planning/STATE.md').read_text()
def checked(req_id):
    m = re.search(rf'- \[([ xX])\] \*\*{req_id}\*\*', req)
    if not m:
        raise SystemExit(f'missing requirement {req_id}')
    return m.group(1).lower() == 'x'
def has_pending(dataset_key):
    info = gate.get('datasets', {}).get(dataset_key, {})
    return bool(info.get('pending_tracking_sources')) or info.get('evidence_status') == 'pending_tracking'
expected_evid03 = bool(gate.get('pems7_1026_stage12_complete')) and not has_pending('pems7_1026')
if checked('EVID-03') != expected_evid03:
    raise SystemExit('EVID-03 checkbox does not match gate/tracking truth')
expected_evid04 = bool(gate.get('seattle_stage12_complete')) and not bool(gate.get('seattle_core_claim_blocked')) and not has_pending('seattle')
if checked('EVID-04') != expected_evid04:
    raise SystemExit('EVID-04 checkbox does not match gate/tracking truth')
if checked('EVID-05'):
    raise SystemExit('EVID-05 must remain pending for Phase 9')
if gate.get('seattle_core_claim_blocked') and 'Seattle' not in state:
    raise SystemExit('STATE must preserve Seattle blocker when gate blocks')
if any((info.get('evidence_status') == 'pending_tracking') for info in gate.get('datasets', {}).values()) and 'tracking' not in state.lower():
    raise SystemExit('STATE must preserve pending-tracking blocker when gate reports it')
if 'TRC-23-02333/dataset/' in state and 'Raw traffic datasets remain local/ignored' not in state:
    raise SystemExit('STATE should not introduce raw dataset evidence paths')
print('requirements/state gate validation ok')
PY

test -z "$(git status --short -- TRC-23-02333/dataset)"</automated>
  </verify>
  <acceptance_criteria>Requirements checkboxes exactly match gate truth and tracking status, EVID-05 remains pending, STATE preserves blocker/pending/completion status, and raw dataset git status is clean.</acceptance_criteria>
  <done>Planning state cannot silently advance v1.1 if Seattle Stage12 evidence is missing, blocked, or not tracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate JSON -&gt; planning metadata | Machine evidence status controls roadmap, requirements, and state transitions. |
| planning metadata -&gt; future phases | Phase 9/10 may proceed only from truthful Phase 8 state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Tampering | `.planning/REQUIREMENTS.md` | mitigate | Parser checks EVID-03/EVID-04 checkboxes exactly match gate booleans, pending-tracking status, and EVID-05 remains pending. |
| T-08-14 | Repudiation | `.planning/STATE.md` | mitigate | State must preserve explicit Seattle blocker or pending-tracking blocker when gate blocks, or record readiness only when gate allows. |
| T-08-15 | Elevation of Privilege | Phase progression | mitigate | Roadmap/state updates are driven by `external_evidence_gate.json`, not by narrative claims, dry-run output, or untracked aggregate files. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install tasks are planned in this phase. |
</threat_model>

<verification>
Run roadmap validation, requirements/state gate validation, and raw dataset git-status check.
</verification>

<success_criteria>
- `.planning/ROADMAP.md` lists the five Phase 8 plans and reflects blocker/pending/completion status.
- `.planning/REQUIREMENTS.md` marks only evidence requirements actually completed by the gate with tracked aggregate sources.
- `.planning/STATE.md` blocks v1.1 completion if Seattle Stage12 evidence is incomplete, excluded from core claims, or pending tracking.
- No manuscript prose, raw dataset commits, or Phase 9 EVID-05 completion occurs.
</success_criteria>

<output>
Create `.planning/phases/08-external-stage12-evidence/08-05-SUMMARY.md` when done.
</output>
