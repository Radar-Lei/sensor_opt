---
phase: 06-reproducibility-and-artifact-curation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_trace_sl_repro_manifest.py
  - scripts/test_generate_trace_sl_repro_manifest.py
  - TRC-23-02333/trace_sl_results/reproducibility_manifest.json
  - TRC-23-02333/trace_sl_results/REPRODUCIBILITY_MANIFEST.md
autonomous: true
requirements:
  - REPRO-01
  - REPRO-02
  - REPRO-03
must_haves:
  truths:
    - "A reproducibility manifest records provenance for all curated paper-visible Phase 4/5 result directories without reading raw datasets."
    - "Seeds, budgets, candidate counts, launcher commands, Python/package versions where available, and git provenance are machine-readable."
    - "Raw dataset paths are represented only as protected local inputs, not as evidence artifacts."
  artifacts:
    - path: "scripts/generate_trace_sl_repro_manifest.py"
      provides: "Deterministic provenance manifest generator"
      exports: ["main"]
    - path: "scripts/test_generate_trace_sl_repro_manifest.py"
      provides: "Synthetic regression checks for manifest generation"
    - path: "TRC-23-02333/trace_sl_results/reproducibility_manifest.json"
      provides: "Machine-readable provenance inventory"
    - path: "TRC-23-02333/trace_sl_results/REPRODUCIBILITY_MANIFEST.md"
      provides: "Human-readable provenance inventory"
  key_links:
    - from: "scripts/generate_trace_sl_repro_manifest.py"
      to: "scripts/run_stage12_pems7_228.sh"
      via: "launcher parsing"
      pattern: "run_stage12_pems7_228"
    - from: "scripts/generate_trace_sl_repro_manifest.py"
      to: "TRC-23-02333/trace_sl_results/reproducibility_manifest.json"
      via: "JSON writer"
      pattern: "reproducibility_manifest\.json"
---

<objective>
Create a deterministic provenance manifest for curated TRACE-SL evidence.

Purpose: Satisfy REPRO-01..03 by making Phase 4/5 evidence traceable to scripts, committed aggregate artifacts, environment/package metadata, and protected raw-data hygiene without rerunning long experiments.
Output: A reusable manifest generator, regression tests, and generated JSON/Markdown provenance artifacts under `TRC-23-02333/trace_sl_results/`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-reproducibility-and-artifact-curation/06-CONTEXT.md
@.planning/phases/04-core-experiment-evidence/04-VERIFICATION.md
@.planning/phases/05-robustness-and-generality/05-VERIFICATION.md
@TRC-23-02333/trace_sl_results/README.md
@scripts/run_stage12_pems7_228.sh
@scripts/run_stage13_candidate_sensitivity_pems7_228.sh
@scripts/run_stage14_pems7_228_robustness.sh
@scripts/run_stage14_candidate_sensitivity_pems7_228.sh

<interfaces>
Existing launcher defaults to parse for REPRO-01 per D-01/D-03/D-06:
- Stage 12 launcher exposes `SEEDS`, `BUDGETS`, `NUM_LAYOUTS`, `RCSS_RANDOM_CANDIDATES`, `RCSS_QUALITY_CANDIDATES`, `SCENARIO_COUNT`, validation-swap knobs, thread variables, `DATA_ROOT`, `OUTPUT_DIR`, and evaluator/summarizer command structure.
- Stage 13/14 candidate launchers expose `CANDIDATE_COUNTS`, timing CSV names, candidate candidate-count knobs, and summarizer command structure.
- Stage 14 robustness launcher exposes robustness conditions, seeds, budgets, reduced candidate defaults, and condition-specific CLI flags.
Existing curated result directories from `trace_sl_results/README.md` to inventory as paper-visible evidence:
`pems7_228_stage12_baseline_portfolio`, `pems7_228_stage13_candidate_sensitivity`, `pems7_228_stage14_robustness`, `pems7_228_stage14_candidate_sensitivity`, plus lower-power/supporting directories `pems7_1026_stage11_auto_weight` and `seattle_stage11_auto_weight_light` with claim-status labels.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify manifest extraction behavior</name>
  <files>scripts/test_generate_trace_sl_repro_manifest.py</files>
  <behavior>
    - Test 1: Given a synthetic launcher containing `SEEDS`, `BUDGETS`, `CANDIDATE_COUNTS`, and `OUTPUT_DIR`, the parser returns those defaults as strings/lists without executing the launcher.
    - Test 2: Given a synthetic result directory with aggregate CSV/Markdown files, the inventory records relative paths, file sizes, and marks raw dataset prefixes as non-evidence.
    - Test 3: Package metadata collection returns a dictionary for Python plus numpy/pandas/scipy/sklearn entries when importable, and records unavailable packages without failing.
  </behavior>
  <action>Create a standard-library `unittest` test script for the manifest generator per D-03 and D-06. Use temporary directories and tiny synthetic files; do not touch real raw datasets and do not depend on network, pytest, or new packages. Tests should import functions that the implementation will expose: `parse_shell_defaults`, `inventory_result_dir`, and `collect_environment`.</action>
  <verify>
    <automated>python scripts/test_generate_trace_sl_repro_manifest.py</automated>
  </verify>
  <done>The tests fail before implementation because `scripts/generate_trace_sl_repro_manifest.py` is absent or missing required functions, and they define the manifest contracts needed for REPRO-01..03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and generate reproducibility manifest</name>
  <files>scripts/generate_trace_sl_repro_manifest.py, TRC-23-02333/trace_sl_results/reproducibility_manifest.json, TRC-23-02333/trace_sl_results/REPRODUCIBILITY_MANIFEST.md</files>
  <behavior>
    - Test 1: `parse_shell_defaults` extracts quoted shell defaults without sourcing scripts.
    - Test 2: `inventory_result_dir` inventories curated aggregate artifacts and excludes raw dataset files as evidence.
    - Test 3: CLI generation writes JSON and Markdown deterministically from existing committed result directories.
  </behavior>
  <action>Implement `scripts/generate_trace_sl_repro_manifest.py` per D-01, D-02, D-03, and D-06. The script must use no new dependencies beyond the standard library. It should: define curated stage entries with claim status from `TRC-23-02333/trace_sl_results/README.md`; parse launcher defaults from Stage 12/13/14 scripts; collect `python --version`, importlib metadata versions for numpy, pandas, scipy, scikit-learn/sklearn where feasible, git commit, and git tracked raw dataset paths using `git ls-files -- TRC-23-02333/dataset/`; inventory required aggregate files in curated directories with relative path, size, and existence; write stable sorted/indented JSON and a Markdown table. Do not read raw dataset contents, do not run experiments, and do not cite `TRC-23-02333/dataset/` as evidence except in a protected-input hygiene section saying it must remain local/ignored.</action>
  <verify>
    <automated>python scripts/test_generate_trace_sl_repro_manifest.py &amp;&amp; python scripts/generate_trace_sl_repro_manifest.py --project-root /home/samuel/projects/sensor_opt --output-json TRC-23-02333/trace_sl_results/reproducibility_manifest.json --output-md TRC-23-02333/trace_sl_results/REPRODUCIBILITY_MANIFEST.md</automated>
  </verify>
  <done>`reproducibility_manifest.json` and `REPRODUCIBILITY_MANIFEST.md` exist, list Phase 4/5 curated result directories, record launcher defaults/provenance/package metadata, and report zero tracked raw dataset files or an explicit fail-visible raw-data hygiene field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem-to-manifest | Existing files and launcher text are untrusted inputs to the manifest generator. |
| git metadata-to-manifest | Git command output is local provenance data and must not become an execution path. |
| raw dataset boundary | `TRC-23-02333/dataset/` may exist locally but must not be read, committed, or paper-cited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `scripts/generate_trace_sl_repro_manifest.py` file inventory | mitigate | Use deterministic relative-path inventory and existence/size checks; final validator in Plan 06-03 cross-checks required directories. |
| T-06-02 | Information Disclosure | raw dataset boundary | mitigate | Do not open files under `TRC-23-02333/dataset/`; use `git ls-files` path-only hygiene scan and protected-input wording. |
| T-06-03 | Repudiation | provenance metadata | mitigate | Record git commit/status summary, launcher defaults, package versions, and generated timestamp/source script. |
| T-06-SC | Tampering | package installs | accept | No package-manager install task is planned; use installed metadata only and add no dependencies. |
</threat_model>

<verification>
Run `python scripts/test_generate_trace_sl_repro_manifest.py` and then run the generator command in Task 2. Inspect JSON/Markdown existence through the automated command output; do not run long experiment launchers.
</verification>

<success_criteria>
- REPRO-01: Manifest records seeds, budgets, candidate counts, package versions where feasible, launcher defaults, git provenance, and result inventory.
- REPRO-02: Manifest reports raw dataset hygiene without reading or citing raw dataset files as evidence.
- REPRO-03: Manifest inventories only curated present result directories and claim-status labels from the existing results README.
</success_criteria>

<output>
Create `.planning/phases/06-reproducibility-and-artifact-curation/06-01-SUMMARY.md` when done.
</output>
