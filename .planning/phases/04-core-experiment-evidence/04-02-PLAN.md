---
phase: 04-core-experiment-evidence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - TRC-23-02333/summarize_trace_sl_rcss.py
autonomous: true
requirements: [EXP-04, EXP-05]
must_haves:
  truths:
    - "D-09: Paper-visible comparisons include paired deltas, uncertainty intervals, effect sizes, and paired tests."
    - "D-11: Certificate-error correlations are summarized as empirical support for certificate-guided selection."
    - "D-10: Existing Stage 11/12 result directories can be re-aggregated through the main summarizer without notebooks or ad hoc calculations."
  artifacts:
    - path: "TRC-23-02333/summarize_trace_sl_rcss.py"
      provides: "Main aggregation script with intervals, effect sizes, paired tests, and empirical certificate summaries"
      exports: ["main"]
  key_links:
    - from: "TRC-23-02333/summarize_trace_sl_rcss.py"
      to: "TRC-23-02333/trace_sl_results/*/gls_map_paired_delta_tests.csv"
      via: "paired comparison CSV writer"
      pattern: "gls_map_paired_delta_tests"
    - from: "TRC-23-02333/summarize_trace_sl_rcss.py"
      to: "TRC-23-02333/trace_sl_results/*/certificate_correlation_summary.csv"
      via: "certificate correlation aggregation"
      pattern: "certificate_correlation_summary"
---

<objective>
Extend the existing TRACE-SL summarizer so every paper-visible comparison has the statistical fields required by EXP-04 and every certificate summary is explicitly empirical per EXP-05.

Purpose: D-09: requires paired deltas, intervals, effect sizes, and paired tests; D-10: requires the existing summarizer to remain the main aggregation point.
Output: Enhanced `TRC-23-02333/summarize_trace_sl_rcss.py` that regenerates richer CSV/Markdown summaries from existing Stage artifacts.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-core-experiment-evidence/04-CONTEXT.md
@.planning/phases/03-baseline-portfolio/03-BASELINE-INVENTORY.md
@TRC-23-02333/summarize_trace_sl_rcss.py
@TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split/combined_metrics.csv
@TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split/combined_certificate_correlations.csv
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add interval and effect-size fields to paired comparisons</name>
  <files>TRC-23-02333/summarize_trace_sl_rcss.py</files>
  <read_first>
    <file>.planning/phases/04-core-experiment-evidence/04-CONTEXT.md</file>
    <file>TRC-23-02333/summarize_trace_sl_rcss.py</file>
    <file>TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split/gls_map_paired_delta_tests.csv</file>
    <file>TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split/combined_metrics.csv</file>
  </read_first>
  <behavior>
    - For each paired layout-baseline comparison, compute `delta_mean`, `delta_std`, `delta_sem`, `ci95_low`, `ci95_high`, `cohens_dz`, `paired_t_p`, `wilcoxon_p`, `win_count`, and `count` from matched split-level GLS/MAP MAE deltas.
    - Keep existing layout labels stable, including `validation_swap_selected`, `rcss_selected`, `multistart_swap_by_validation`, `observability_proxy`, `graph_sampling_laplacian`, and `qr_pod_modes`, per D-03:.
    - Compute uncertainty from held-out test MAE rows only where `method == "gls_map"`, per D-01:.
    - Preserve existing CSV names and add columns rather than replacing output files, per D-13:.
  </behavior>
  <action>Refactor the paired-comparison block into small helper functions such as `paired_delta_stats(delta)` and `build_paired_comparisons(pivot, comparison_layouts, baseline_layouts)`. Use SciPy's existing `ttest_rel` and `wilcoxon`; use a 95% t-interval when count is at least 2 and leave interval/effect fields as `pd.NA` when insufficient matched pairs exist. Include Phase 3 portfolio labels in `baseline_layouts` so Stage 12 outputs are automatically summarized.</action>
  <verify>
    <automated>python -m py_compile TRC-23-02333/summarize_trace_sl_rcss.py &amp;&amp; python TRC-23-02333/summarize_trace_sl_rcss.py --input-root TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split --output-dir /tmp/trace_sl_phase4_stats_check &amp;&amp; python - <<'PY'
import pandas as pd
p = '/tmp/trace_sl_phase4_stats_check/gls_map_paired_delta_tests.csv'
df = pd.read_csv(p)
required = {'delta_mean','delta_std','delta_sem','ci95_low','ci95_high','cohens_dz','paired_t_p','wilcoxon_p','win_count','count'}
missing = required - set(df.columns)
assert not missing, missing
assert {'validation_swap_selected','rcss_selected'}.issubset(set(df['layout']))
assert (df['count'] >= 2).any()
print('paired-stats-ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - `gls_map_paired_delta_tests.csv` regenerated from PeMS7_228 10-split contains all required interval/effect-size columns.
    - Existing columns used by downstream docs remain present.
    - D-04 caveat remains possible because multistart comparator rows are retained in paired comparisons.
    - No new dependency is added to `requirements.txt`.
  </acceptance_criteria>
  <done>Summarizer produces EXP-04-compliant paired comparison statistics from existing result directories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add empirical certificate and source-summary reporting</name>
  <files>TRC-23-02333/summarize_trace_sl_rcss.py</files>
  <read_first>
    <file>.planning/phases/04-core-experiment-evidence/04-CONTEXT.md</file>
    <file>TRC-23-02333/summarize_trace_sl_rcss.py</file>
    <file>TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split/combined_certificate_correlations.csv</file>
    <file>TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split/combined_rcss_candidates.csv</file>
  </read_first>
  <behavior>
    - Certificate output includes Pearson and Spearman mean/std/min/max/count by `method` and `certificate`.
    - Markdown `SUMMARY.md` labels certificate correlations as empirical support, not formal guarantees, per D-11:.
    - RCSS selected-source summaries remain present so the 10% PeMS7_228 multistart caveat stays visible per D-04:.
  </behavior>
  <action>Update certificate aggregation to include `count` columns and update the generated Markdown headings/text to say empirical certificate-error correlation. Ensure `combined_certificate_correlations.csv`, `certificate_correlation_summary.csv`, `combined_rcss_candidates.csv`, and `rcss_selected_sources.csv` are still emitted when source files exist. Do not change evaluator outputs or raw experiment data.</action>
  <verify>
    <automated>python TRC-23-02333/summarize_trace_sl_rcss.py --input-root TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split --output-dir /tmp/trace_sl_phase4_cert_check &amp;&amp; python - <<'PY'
from pathlib import Path
import pandas as pd
summary = Path('/tmp/trace_sl_phase4_cert_check/SUMMARY.md').read_text(encoding='utf-8').lower()
assert 'empirical' in summary and 'certificate' in summary
cert = pd.read_csv('/tmp/trace_sl_phase4_cert_check/certificate_correlation_summary.csv')
assert any('count' in c for c in cert.columns), cert.columns.tolist()
selected = pd.read_csv('/tmp/trace_sl_phase4_cert_check/rcss_selected_sources.csv')
assert {'budget','source','selected_count'}.issubset(selected.columns)
print('certificate-summary-ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - Certificate summary has count information and remains grouped by method/certificate.
    - Generated Markdown uses empirical wording and does not contain formal-certified phrasing such as `guaranteed optimal` or `certified optimal`.
    - Selected-source summary still includes `multistart_swap_by_validation` when present in the input artifacts.
  </acceptance_criteria>
  <done>Summarizer output supports EXP-05 and the certificate-guided claim without overclaiming formal certification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| result CSVs -> summarizer | Generated experiment files can have missing columns, duplicate rows, or insufficient paired splits. |
| statistical output -> paper claims | Numeric summaries can be overinterpreted if intervals/effect sizes or caveats are missing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | paired comparison CSV | mitigate | Compute matched split deltas from pivoted held-out GLS/MAP rows and include count/CI/effect-size fields. |
| T-04-02-02 | Repudiation | certificate evidence wording | mitigate | Generated summary labels correlations as empirical and includes counts. |
| T-04-02-03 | Denial of Service | insufficient comparison pairs | mitigate | Emit `pd.NA` for interval/effect fields with insufficient pairs rather than crashing when some layouts are absent. |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the two `/tmp/trace_sl_phase4_*_check` aggregation commands from task verification and confirm generated CSV schemas include interval/effect/certificate count fields.
</verification>

<success_criteria>
- EXP-04 statistical fields are generated by the main summarizer.
- EXP-05 certificate correlations are explicitly empirical.
- Existing Stage 11 outputs can be re-aggregated without raw dataset reads or new packages.
</success_criteria>

<output>
Create `.planning/phases/04-core-experiment-evidence/04-02-SUMMARY.md` when done.
</output>
