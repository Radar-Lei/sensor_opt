---
phase: 04-core-experiment-evidence
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - TRC-23-02333/summarize_trace_sl_rcss.py
  - scripts/run_stage13_candidate_sensitivity_pems7_228.sh
  - TRC-23-02333/trace_sl_results/pems7_228_stage13_candidate_sensitivity/SUMMARY.md
autonomous: true
requirements: [EXP-06]
must_haves:
  truths:
    - "D-12: Runtime and candidate-count sensitivity are visible for main claim settings as tractability and selection-stability evidence."
    - "D-12: Sensitivity is framed as practical tractability and selection stability, not a broad scalability claim."
    - "D-13: EXP-06 completion requires evidence-backed candidate-count and runtime/tractability outputs, not unavailable notes or pending commands only."
    - "D-14: Stage 13 sensitivity evidence writes to a descriptive `trace_sl_results/<dataset>_stage<stage>_<descriptor>/` directory reviewed against `.gitignore` before committing."
  artifacts:
    - path: "TRC-23-02333/summarize_trace_sl_rcss.py"
      provides: "Candidate/runtime sensitivity aggregation from candidate/config/log artifacts"
    - path: "scripts/run_stage13_candidate_sensitivity_pems7_228.sh"
      provides: "Reproducible PeMS7_228 candidate-count sensitivity launcher"
    - path: "TRC-23-02333/trace_sl_results/pems7_228_stage13_candidate_sensitivity/SUMMARY.md"
      provides: "Evidence-backed candidate-count and runtime/tractability sensitivity summary"
  key_links:
    - from: "scripts/run_stage13_candidate_sensitivity_pems7_228.sh"
      to: "TRC-23-02333/trace_sl_results/pems7_228_stage13_candidate_sensitivity/"
      via: "candidate-count sweep outputs"
      pattern: "RCSS_RANDOM_CANDIDATES|RCSS_QUALITY_CANDIDATES"
    - from: "TRC-23-02333/summarize_trace_sl_rcss.py"
      to: "combined_rcss_candidates.csv"
      via: "candidate-count and selection-source aggregation"
      pattern: "combined_rcss_candidates"
---

<objective>
Add the Phase 4 runtime/candidate-count sensitivity slice for EXP-06 using existing evidence-backed artifacts when possible and a minimal reproducible Stage 13 sweep when existing artifacts do not contain timing/tractability outputs.

Purpose: D-12: requires practical tractability and selection-stability evidence, while D-13: requires auditable scripts and committed summaries instead of notebook calculations.
Output: Summarizer sensitivity support, Stage 13 launcher, and evidence-backed candidate/runtime sensitivity summary.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-core-experiment-evidence/04-CONTEXT.md
@.planning/phases/04-core-experiment-evidence/04-EVIDENCE-AUDIT.md
@TRC-23-02333/summarize_trace_sl_rcss.py
@scripts/run_stage11_pems7_228.sh
@scripts/run_stage12_pems7_228.sh
@TRC-23-02333/transparent_estimator_eval.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add candidate/runtime sensitivity aggregation</name>
  <files>TRC-23-02333/summarize_trace_sl_rcss.py</files>
  <read_first>
    <file>.planning/phases/04-core-experiment-evidence/04-CONTEXT.md</file>
    <file>TRC-23-02333/summarize_trace_sl_rcss.py</file>
    <file>TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split/combined_rcss_candidates.csv</file>
    <file>TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split/SUMMARY.md</file>
  </read_first>
  <behavior>
    - Summarizer emits a candidate sensitivity CSV summarizing available candidate counts, selected source counts, and selected candidate diagnostic distributions by budget.
    - When measured runtime fields or Stage 13 timing artifacts are present, summarizer emits runtime/candidate sensitivity outputs with actual timing or tractability measurements.
    - If current Stage 11/12 artifacts lack measured runtime fields, summarizer must mark EXP-06 aggregation as incomplete for runtime and route closure to Task 2 Stage 13 execution; a runtime-unavailable note alone must not be treated as passing evidence.
    - Wording says practical tractability/selection stability per D-12: and does not claim full scalability.
  </behavior>
  <action>Add helper logic that reads `combined_rcss_candidates.csv` when present and writes `candidate_sensitivity_summary.csv` with budget, source, candidate row counts, selected counts, and mean/std for available diagnostics such as `validation_mae`, `posterior_trace`, `scenario_cvar_trace`, `condition_number`, `coverage_penalty`, and `rcss_score`. Add runtime aggregation only from actual measured timing fields, launcher timing logs, or Stage 13 status artifacts; do not satisfy EXP-06 with `runtime unavailable`, `pending Stage 13`, or command-only text. Preserve existing summarizer outputs.</action>
  <verify>
    <automated>python -m py_compile TRC-23-02333/summarize_trace_sl_rcss.py &amp;&amp; python TRC-23-02333/summarize_trace_sl_rcss.py --input-root TRC-23-02333/trace_sl_results/pems7_228_stage11_auto_weight_10split --output-dir /tmp/trace_sl_phase4_sensitivity_check &amp;&amp; python - <<'PY'
from pathlib import Path
import pandas as pd
csv = Path('/tmp/trace_sl_phase4_sensitivity_check/candidate_sensitivity_summary.csv')
assert csv.exists(), 'candidate_sensitivity_summary.csv missing'
df = pd.read_csv(csv)
assert {'budget','source'}.issubset(df.columns), df.columns.tolist()
summary = Path('/tmp/trace_sl_phase4_sensitivity_check/SUMMARY.md').read_text(encoding='utf-8').lower()
assert 'candidate' in summary and ('tractability' in summary or 'stability' in summary)
for forbidden in ['runtime unavailable as complete', 'pending stage 13 as complete', 'command-only evidence']:
    assert forbidden not in summary, forbidden
print('candidate-sensitivity-aggregation-ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - Candidate sensitivity CSV exists when RCSS candidate inputs exist.
    - Runtime evidence is explicit and evidence-backed when marked complete; missing timing routes to Task 2 execution and cannot close EXP-06.
    - No wording claims broad scalability beyond practical tractability/stability.
  </acceptance_criteria>
  <done>Existing artifacts can produce candidate sensitivity summaries; EXP-06 runtime closure remains blocked until measured runtime/tractability evidence exists.</done>
</task>

<task type="auto">
  <name>Task 2: Execute or audit Stage 13 PeMS7_228 candidate-count sensitivity evidence</name>
  <files>scripts/run_stage13_candidate_sensitivity_pems7_228.sh, TRC-23-02333/trace_sl_results/pems7_228_stage13_candidate_sensitivity/SUMMARY.md</files>
  <read_first>
    <file>.planning/phases/04-core-experiment-evidence/04-CONTEXT.md</file>
    <file>scripts/run_stage11_pems7_228.sh</file>
    <file>scripts/run_stage12_pems7_228.sh</file>
    <file>TRC-23-02333/summarize_trace_sl_rcss.py</file>
    <file>TRC-23-02333/transparent_estimator_eval.py</file>
  </read_first>
  <action>Create a Stage 13 launcher that sweeps candidate budgets such as `50 100 200 500` for PeMS7_228 main settings, writes each sweep under `TRC-23-02333/trace_sl_results/pems7_228_stage13_candidate_sensitivity/candidates_<N>/`, uses stable seeds/budgets and BLAS caps, records actual wallclock/runtime or run-status timing for each completed sweep, and runs the summarizer for each sweep plus an aggregate evidence file. Add a `DRY_RUN=1` path for command verification. Then satisfy EXP-06 by either auditing already-complete evidence-backed Stage 13 outputs or executing at least a minimal Stage 13 sweep with two or more candidate counts that produces actual candidate-count and runtime/tractability outputs. If no such evidence can be produced, fail/block/escalate; do not mark the task complete with pending commands or runtime-unavailable notes only.</action>
  <verify>
    <automated>bash -n scripts/run_stage13_candidate_sensitivity_pems7_228.sh &amp;&amp; DRY_RUN=1 bash scripts/run_stage13_candidate_sensitivity_pems7_228.sh &amp;&amp; python - <<'PY'
from pathlib import Path
import pandas as pd
root = Path('TRC-23-02333/trace_sl_results/pems7_228_stage13_candidate_sensitivity')
summary = root / 'SUMMARY.md'
assert summary.exists(), 'Stage 13 SUMMARY.md missing'
text = summary.read_text(encoding='utf-8')
lower = text.lower()
for token in ['candidate', 'runtime', 'tractability']:
    assert token in lower, token
for forbidden in ['runtime unavailable', 'pending stage 13', 'regeneration pending', 'exact command only', 'command-only']:
    assert forbidden not in lower, forbidden
candidate_dirs = [p for p in root.glob('candidates_*') if p.is_dir()]
assert len(candidate_dirs) >= 2, 'need at least two completed candidate-count directories'
for directory in candidate_dirs:
    assert (directory / 'combined_metrics.csv').exists() or (directory / 'SUMMARY.md').exists(), f'missing evidence in {directory}'
runtime_csv = root / 'runtime_candidate_sensitivity.csv'
assert runtime_csv.exists(), 'runtime_candidate_sensitivity.csv missing'
df = pd.read_csv(runtime_csv)
required_cols = {'candidate_count', 'runtime_seconds'}
missing_cols = required_cols - set(df.columns)
assert not missing_cols, 'missing runtime columns: ' + ', '.join(sorted(missing_cols))
assert df['candidate_count'].nunique() >= 2, 'runtime summary needs at least two candidate counts'
assert (df['runtime_seconds'] >= 0).all(), 'runtime_seconds must be measured nonnegative values'
assert 'scalability guarantee' not in lower
print('stage13-evidence-backed-sensitivity-ok')
PY</automated>
  </verify>
  <acceptance_criteria>
    - Stage 13 launcher exists, passes shell syntax, and `DRY_RUN=1` does not read raw datasets.
    - Launcher uses descriptive output directory under `trace_sl_results/` per D-14:.
    - Stage 13 summary is evidence-backed with actual candidate-count and runtime/tractability outputs; pending commands or runtime-unavailable notes alone fail this task.
    - At least two candidate-count settings have completed/audited outputs so sensitivity is comparative rather than single-point.
    - Summary frames EXP-06 as runtime/candidate practical tractability and selection stability per D-12.
  </acceptance_criteria>
  <done>EXP-06 has evidence-backed candidate-count and runtime/tractability sensitivity outputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate artifacts -> sensitivity claims | Candidate rows may not contain true runtime timing fields. |
| Stage 13 launcher -> raw data | Sensitivity runs read local ignored PeMS data if executed. |
| sensitivity summary -> paper wording | Practical sensitivity can be overclaimed as full scalability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Repudiation | runtime availability | mitigate | Summary must be backed by actual timing/runtime artifacts; missing timing fails or routes to Stage 13 execution, not completion. |
| T-04-05-02 | Tampering | candidate sensitivity CSV | mitigate | Generate from `combined_rcss_candidates.csv` with explicit budget/source grouping. |
| T-04-05-03 | Information Disclosure | raw PeMS data | mitigate | `DRY_RUN=1` verification path avoids raw reads; actual runs only through explicit launcher. |
| T-04-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the summarizer `/tmp/trace_sl_phase4_sensitivity_check` command and the Stage 13 evidence-backed runtime/candidate verification.
</verification>

<success_criteria>
- Candidate sensitivity summary exists from current or Stage 13 artifacts.
- Runtime/tractability status is evidence-backed by measured outputs, not unavailable/pending notes.
- Stage 13 provides completed comparative evidence for EXP-06 or the plan blocks/escalates.
</success_criteria>

<output>
Create `.planning/phases/04-core-experiment-evidence/04-05-SUMMARY.md` when done.
</output>
