---
phase: 09-ablation-and-evidence-classification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_trace_sl_ablation_evidence_contracts.py
  - scripts/test_generate_trace_sl_ablation_evidence_contracts.py
autonomous: true
requirements: [ABLT-01, ABLT-02, ABLT-03, ABLT-04, EVID-05]
user_setup: []
must_haves:
  truths:
    - "Author can generate one machine-readable ablation contract covering random mean, validation-selected random, certificate-only greedy, RCSS-selected, validation-swap-selected, and multistart validation-swap variants."
    - "Author can inspect machine-checkable fields that separate certificate candidate generation, validation selection, and validation-aware local refinement."
    - "Author can classify each dataset into an allowed evidence lane without completing or implying EVID-03/EVID-04."
  artifacts:
    - path: "scripts/generate_trace_sl_ablation_evidence_contracts.py"
      provides: "Phase 9 generator for ablation and dataset-classification contracts"
      exports: ["build_all_contracts", "build_ablation_contract", "build_dataset_classification", "main"]
    - path: "scripts/test_generate_trace_sl_ablation_evidence_contracts.py"
      provides: "Fail-closed generator regression tests"
  key_links:
    - from: "scripts/generate_trace_sl_ablation_evidence_contracts.py"
      to: "TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json"
      via: "read-only fail-closed gate input"
      pattern: "external_evidence_gate"
    - from: "scripts/generate_trace_sl_ablation_evidence_contracts.py"
      to: "TRC-23-02333/trace_sl_results/paper_sources/ablation_contract.csv"
      via: "deterministic writer"
      pattern: "ablation_contract"
---

<objective>
Create the tested Phase 9 generator that derives ablation and dataset-classification contracts from existing committed evidence artifacts only.

Purpose: This gives the author a reproducible, fail-closed way to explain which TRACE-SL layer each ablation row supports and where each dataset may be used, while preserving the Phase 8 external evidence gate truth.
Output: A standalone generator script plus regression tests that are ready to produce CSV/JSON-first paper-source artifacts in the next plan.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-ablation-and-evidence-classification/09-CONTEXT.md
@.planning/phases/09-ablation-and-evidence-classification/09-PATTERNS.md
@.planning/phases/07-claim-and-main-table-contract/07-02-SUMMARY.md
@.planning/phases/08-external-stage12-evidence/08-04-SUMMARY.md
@.planning/phases/08.5-stage12-performance-unblock/08.5-04-SUMMARY.md
@scripts/generate_trace_sl_external_evidence_contracts.py
@scripts/generate_trace_sl_claim_contracts.py
@scripts/test_generate_trace_sl_external_evidence_contracts.py
@TRC-23-02333/summarize_trace_sl_rcss.py

<interfaces>
Phase 9 must follow the pattern map in `09-PATTERNS.md`: standalone Python CLI, deterministic CSV/JSON/Markdown writers, explicit ordered columns, path hygiene rejecting raw dataset paths, and tests that import the generator by file path.
Required generator contracts:
- `build_all_contracts(project_root: Path, output_dir: Path | None = None) -> dict[str, object]`
- `build_ablation_contract(project_root: Path) -> list[dict[str, object]]`
- `build_dataset_classification(project_root: Path) -> list[dict[str, object]]`
- `main(argv: list[str] | None = None) -> None`
Required output names for downstream Plan 09-02: `ablation_contract.csv/json/md`, `dataset_evidence_classification.csv/json/md`, and README block updates under `TRC-23-02333/trace_sl_results/paper_sources/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write fail-closed generator tests for ablation and dataset classification</name>
  <files>scripts/test_generate_trace_sl_ablation_evidence_contracts.py</files>
  <read_first>
    - scripts/test_generate_trace_sl_external_evidence_contracts.py
    - scripts/test_generate_trace_sl_paper_sources.py
    - .planning/phases/09-ablation-and-evidence-classification/09-CONTEXT.md
    - .planning/phases/09-ablation-and-evidence-classification/09-PATTERNS.md
    - TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json
  </read_first>
  <behavior>
    - Test that required ablation row families exist: random mean, validation-selected random, certificate-only greedy, RCSS-selected, validation-swap-selected, and multistart validation-swap per Phase 9 context decisions.
    - Test that missing required core PeMS7_228 ablation rows fail closed with a clear `missing required layout_type` or equivalent ValueError.
    - Test that PeMS7_1026 Stage11 and one-seed Stage12 feasibility sources are classified as non-completing for EVID-03.
    - Test that Seattle is blocked from core eligibility when the external evidence gate or `stage12_status.json` says ten-split Stage12 evidence is incomplete.
    - Test that generated rows never contain `TRC-23-02333/dataset/` paths and never mark validation MAE as held-out test evidence.
  </behavior>
  <action>Create `scripts/test_generate_trace_sl_ablation_evidence_contracts.py` using `unittest`, dynamic import by file path, temporary git repo setup, and small synthetic CSV/JSON fixtures. Implement tests before the generator passes them. The tests must encode all locked Phase 9 decisions from `09-CONTEXT.md`: held-out aggregate artifacts are evidence where available, validation MAE is selection evidence only, missing components are labeled missing/unsupported/conditional, PeMS7_228 remains the core ablation dataset, PeMS7_1026 and Seattle one-seed feasibility outputs are non-evidence for ten-split claims, and robustness rows stay supporting or appendix-only. Do not read raw datasets and do not add any package dependency.</action>
  <verify>
    <automated>python scripts/test_generate_trace_sl_ablation_evidence_contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - Test file imports `scripts/generate_trace_sl_ablation_evidence_contracts.py` by absolute generator path and uses only temporary curated result fixtures.
    - At least one test fails if `pems7_1026_stage12_complete`, `seattle_stage12_complete`, or `v1_1_completion_allowed` is inferred from Stage11, DRY_RUN, or one-seed feasibility artifacts.
    - At least one test fails if any generated provenance field contains `TRC-23-02333/dataset/`.
    - Tests mention the required layout/source families by exact identifiers: `random`, `best_random_by_validation`, `greedy_a_trace`, `rcss_selected`, `validation_swap_selected`, and `multistart_swap_by_validation`.
  </acceptance_criteria>
  <done>Regression tests define the Phase 9 evidence policy and fail against a missing or incomplete generator for the required ablation and classification behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the ablation and dataset-classification generator</name>
  <files>scripts/generate_trace_sl_ablation_evidence_contracts.py, scripts/test_generate_trace_sl_ablation_evidence_contracts.py</files>
  <read_first>
    - scripts/generate_trace_sl_external_evidence_contracts.py
    - scripts/generate_trace_sl_claim_contracts.py
    - TRC-23-02333/summarize_trace_sl_rcss.py
    - TRC-23-02333/trace_sl_results/paper_sources/main_table_contract.csv
    - TRC-23-02333/trace_sl_results/paper_sources/external_evidence_gate.json
    - TRC-23-02333/trace_sl_results/paper_sources/external_evidence_contract.csv
    - .planning/phases/09-ablation-and-evidence-classification/09-PATTERNS.md
  </read_first>
  <action>Implement `scripts/generate_trace_sl_ablation_evidence_contracts.py` as a standalone CLI following the external-evidence generator style. Add ordered column constants for ablation and dataset classification. Build ablation rows from existing curated aggregate summaries, prioritizing `TRC-23-02333/trace_sl_results/pems7_228_stage12_baseline_portfolio/` for core in-domain rows and using only committed aggregate files when asserting complete evidence. Include machine-checkable fields `component_layer`, `supported_question`, `comparison_target`, `evidence_status`, `paired_evidence_status`, `claim_route`, `caveat_tag`, `actual_split_count`, `required_split_count`, and `provenance`. Map RCSS layers per Phase 9 context: certificate candidate generation, validation-based candidate selection, and validation-aware local refinement. Classify PeMS7_228 as core where Stage12 or other committed core aggregate evidence supports it; classify PeMS7_1026 as external/supporting/conditional without satisfying EVID-03 from Stage11 or one-seed feasibility; classify Seattle as conditional and blocked from core while the gate is incomplete; classify robustness/stress evidence as supporting or appendix-only unless multi-seed perturbation evidence is explicitly present. Keep `external_evidence_gate.json` read-only and fail-closed; do not synthesize evidence, rerun expensive experiments, draft manuscript prose, or touch raw datasets.</action>
  <verify>
    <automated>python scripts/test_generate_trace_sl_ablation_evidence_contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - `build_all_contracts()` returns nonempty `ablation_contract` and `dataset_evidence_classification` collections plus JSON metadata with deterministic `generated_at_utc` and schema markers.
    - Required ablation row families are present or explicitly represented as `missing`, `unsupported`, or `conditional`; none are silently omitted.
    - PeMS7_228 low-budget multistart caveat is present on relevant 10% rows and multistart comparisons.
    - `external_evidence_gate.json` booleans remain fail-closed unless real tracked ten-split evidence exists; one-seed feasibility directories are labeled non-evidence for EVID-03/EVID-04.
    - `python scripts/test_generate_trace_sl_ablation_evidence_contracts.py` passes without network access, raw dataset access, or package installation.
  </acceptance_criteria>
  <done>The generator passes the Phase 9 test suite and can deterministically produce ablation and dataset-classification contracts from existing curated artifacts only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated result files -> generator | Existing CSV/JSON artifacts are parsed as inputs and must not be trusted to imply unsupported evidence. |
| generator -> paper_sources artifacts | Generated CSV/JSON/Markdown files become claim-facing evidence contracts. |
| local filesystem -> provenance fields | File paths can accidentally expose raw dataset paths or untracked outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `build_ablation_contract` evidence-source loading | mitigate | Require expected aggregate filenames, nonempty rows, explicit missing/unsupported statuses, and tests for missing required layout families. |
| T-09-02 | Information Disclosure | provenance/path serialization | mitigate | Reuse raw-dataset path rejection pattern and test that no row contains `TRC-23-02333/dataset/`. |
| T-09-03 | Repudiation | dataset classification status | mitigate | Include schema marker, deterministic generated timestamp, source artifact list, split counts, gate status, and blocker reasons. |
| T-09-04 | Elevation of Privilege | PeMS7_1026/Seattle evidence promotion | mitigate | Treat Stage11, DRY_RUN, untracked, and one-seed feasibility artifacts as non-completing; preserve gate booleans from `external_evidence_gate.json`. |
| T-09-SC | Tampering | package installs | accept | No npm/pip/cargo install tasks are present; use existing Python stdlib and pandas already in project dependencies. |
</threat_model>

<verification>
Run `python scripts/test_generate_trace_sl_ablation_evidence_contracts.py`. The command must pass and must not create paper-source outputs outside temporary fixtures during Plan 09-01.
</verification>

<success_criteria>
- Generator and tests exist at the planned paths.
- Tests encode all Phase 9 locked decisions and pass against the implemented generator.
- No manuscript prose, raw dataset reads, package installs, or external evidence gate openings occur.
- The generator exposes deterministic functions and CLI needed by Plan 09-02.
</success_criteria>

<output>
Create `.planning/phases/09-ablation-and-evidence-classification/09-01-SUMMARY.md` when done.
</output>
