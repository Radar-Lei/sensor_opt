---
phase: 02-formulation-and-theory-bridge
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md
autonomous: true
requirements:
  - THEORY-01
  - THEORY-02
  - THEORY-03
  - THEORY-04
  - THEORY-05
user_setup: []
must_haves:
  truths:
    - "D-01: A reader can identify TRACE-SL as a budgeted sparse sensor-set design method for hidden-node reconstruction, not as candidate-pool tuning."
    - "D-02: A reader can see train-derived reconstruction ingredients, validation-based layout selection, and held-out test evaluation as separate protocol stages."
    - "D-03: A reader can see traffic-network notation for graph nodes/links, observed subset S, hidden complement H, state x, observations y_S, reconstruction map, and budget k."
    - "D-04: A reader can trace RCSS surrogate terms to validation loss, posterior trace, scenario CVaR trace, condition number, and coverage."
    - "D-05: A reader can see auto-weight selection as inner-validation model selection over a fixed weight grid with selector/tuner validation splits."
    - "D-06: A reader can see validation-aware swap as fixed-universe local refinement accepting only validation GLS/MAP loss improvements."
    - "D-07: A reader can follow the linear-Gaussian GLS/MAP posterior-error identity and its squared-error scope."
    - "D-08: A reader can see the Gaussian/linear, train-derived covariance or precision, regularized prior, fixed observations, and squared-error assumptions."
    - "D-09: A reader can see posterior variance as an empirical MAE-oriented guide rather than a formal MAE guarantee."
    - "D-10: A reader can see validation-aware swap complexity in terms of candidate universe size, budget k, validation samples, reconstruction solve cost, starts, and swap iterations."
    - "D-11: A reader can see fixed-candidate one-swap local optimality under the implemented validation-loss acceptance rule."
    - "D-12: A reader can see certificate terminology scoped to certificate-guided or posterior-certificate-aware language, not broad certified optimization."
  artifacts:
    - path: .planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md
      provides: "Manuscript-ready formulation, RCSS surrogate, posterior-error bridge, MAE caveats, validation-swap analysis, and self-check"
      contains: "THEORY-01"
  key_links:
    - from: .planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md
      to: TRC-23-02333/transparent_estimator_eval.py
      via: "Named implementation identifiers such as solve_quadratic, certificate, rcss_candidate_scores, select_auto_rcss_weights, validation_swap_search"
      pattern: "solve_quadratic|rcss_candidate_scores|validation_swap_search"
    - from: .planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md
      to: .planning/phases/01-claim-evidence-contract/01-CLAIM-EVIDENCE-CONTRACT.md
      via: "Certificate terminology and validation/test evidence guardrails"
      pattern: "certificate-guided|Validation MAE is selection evidence"
---

<objective>
Create the core Phase 2 method/theory bridge artifact as a complete paper-facing slice: formal budgeted sensor-set formulation, RCSS surrogate, GLS/MAP posterior-error derivation, MAE bridge caveats, and validation-aware swap analysis.

Purpose: Make TRACE-SL read as a reconstruction-aware design method while preserving Phase 1 claim guardrails and certificate-guided wording.
Output: `.planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md`.
</objective>

<execution_context>
@$HOME/.claude_codex/get-shit-done/workflows/execute-plan.md
@$HOME/.claude_codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-claim-evidence-contract/01-CONTEXT.md
@.planning/phases/01-claim-evidence-contract/01-CLAIM-EVIDENCE-CONTRACT.md
@.planning/phases/02-formulation-and-theory-bridge/02-CONTEXT.md
@.planning/phases/02-formulation-and-theory-bridge/02-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@TRC-23-02333/transparent_estimator_eval.py
@NARRATIVE_REPORT.md

<mode_note>
MVP mode is active, but this is a documentation/theory phase. Treat this plan as a vertical manuscript artifact slice: after completion, a paper writer has a usable method/theory bridge artifact rather than a horizontal outline.
</mode_note>

<implementation_identifiers>
Use these exact implementation anchors when grounding the artifact: `split_daily_frame`, `solve_quadratic`, `certificate`, `evaluate_layout`, `posterior_trace_for_layout`, `scenario_cvar_trace_for_layout`, `posterior_condition_for_layout`, `coverage_penalty`, `rcss_candidate_scores`, `make_rcss_row`, `split_validation_for_tuning`, `parse_weight_grid`, `select_auto_rcss_weights`, `validation_swap_search`.
</implementation_identifiers>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the formal sensor-set formulation slice</name>
  <files>.planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md</files>
  <read_first>
    - .planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md if it already exists; otherwise create it.
    - .planning/ROADMAP.md Phase 2 goal and success criteria.
    - .planning/REQUIREMENTS.md THEORY-01 and global out-of-scope constraints.
    - .planning/phases/02-formulation-and-theory-bridge/02-CONTEXT.md decisions D-01, D-02, D-03.
    - .planning/phases/02-formulation-and-theory-bridge/02-PATTERNS.md artifact header, source register, and train/validation/test separation patterns.
    - .planning/phases/01-claim-evidence-contract/01-CLAIM-EVIDENCE-CONTRACT.md validation/test and certificate terminology guardrails.
  </read_first>
  <action>Create the Phase 2 bridge artifact with a header, status/date, phase boundary, source register, notation block, and formal budgeted sensor-set optimization section. Implement D-01 by stating the design decision as choosing `S` with `|S| <= k` or a budget fraction to minimize hidden-node reconstruction loss on complement `H`. Implement D-02 by separating train-derived reconstruction ingredients, validation layout selection, and held-out test evaluation in distinct bullets or subsections. Implement D-03 by using traffic-network notation `G`, nodes/links, observed subset `S`, hidden complement `H`, traffic state `x`, observations `y_S`, reconstruction map `\hat{x}_H(S)`, and budget `k`. Do not introduce raw dataset reads, new experiments, baseline work, or final manuscript sections.</action>
  <acceptance_criteria>
    - The artifact contains sections named `Phase Boundary`, `Source Register`, `Notation`, `Budgeted Reconstruction-Aware Sensor-Set Problem`, and `Protocol Separation`.
    - The artifact explicitly contains `S`, `H`, `x`, `y_S`, `\hat{x}_H(S)`, `|S| <= k`, `validation`, and `held-out test`.
    - THEORY-01 is named in a traceability row or self-check item.
    - No source path under `TRC-23-02333/dataset/` is cited as an evidence source.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('.planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md'); s=p.read_text(); required=['Phase Boundary','Source Register','Notation','Budgeted Reconstruction-Aware Sensor-Set Problem','Protocol Separation','THEORY-01','S','H','x','y_S','\\hat{x}_H(S)','|S| <= k','validation','held-out test']; missing=[x for x in required if x not in s]; assert not missing, missing; assert 'TRC-23-02333/dataset/' not in s"</automated>
  </verify>
  <done>The bridge artifact gives a complete formal problem statement and protocol boundary that satisfies THEORY-01 and decisions D-01..D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Add RCSS surrogate and posterior-error derivation slice</name>
  <files>.planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md</files>
  <read_first>
    - .planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md.
    - .planning/REQUIREMENTS.md THEORY-02, THEORY-03, THEORY-04.
    - .planning/phases/02-formulation-and-theory-bridge/02-CONTEXT.md decisions D-04, D-05, D-07, D-08, D-09, D-12.
    - .planning/phases/02-formulation-and-theory-bridge/02-PATTERNS.md RCSS scoring, auto-weight selection, GLS/MAP reconstruction, posterior trace, condition, CVaR, and certificate-validity patterns.
    - TRC-23-02333/transparent_estimator_eval.py implementation anchors `solve_quadratic`, `certificate`, `posterior_trace_for_layout`, `scenario_cvar_trace_for_layout`, `rcss_candidate_scores`, `select_auto_rcss_weights`.
  </read_first>
  <action>Extend the same artifact with a `TRACE-SL/RCSS Surrogate Objective` section implementing D-04 and D-05: define RCSS as a predeclared tractable surrogate/algorithmic portfolio with named terms for validation loss, posterior trace, scenario CVaR trace, condition number, and coverage penalty/term; state auto-weight selection as inner-validation model selection over a fixed weight grid using selector/tuner validation splits. Add a `Linear-Gaussian GLS/MAP Posterior-Error Bridge` section implementing D-07 and D-08: derive the MAP/GLS posterior covariance relationship to expected hidden-state squared error and posterior trace as an A-optimal uncertainty proxy under Gaussian/linear observations, train-derived covariance or precision, regularized graph/precision prior, fixed sensor observations, and squared-error analysis. Add an `MAE-Oriented Interpretation and Caveats` section implementing D-09 and D-12: state posterior variance motivates MAE-oriented selection empirically via uncertainty reduction and observed certificate-error alignment, while real traffic data, non-Gaussian errors, temporal shift, validation selection, and MAE-vs-squared-error mismatch prevent broad MAE guarantees or certified optimization wording.</action>
  <acceptance_criteria>
    - The artifact contains sections named `TRACE-SL/RCSS Surrogate Objective`, `Linear-Gaussian GLS/MAP Posterior-Error Bridge`, and `MAE-Oriented Interpretation and Caveats`.
    - The surrogate section names all five RCSS diagnostics: validation loss, posterior trace, scenario CVaR trace, condition number, and coverage.
    - The auto-weight section names selector/tuner validation splits and fixed weight grid selection.
    - The posterior bridge names posterior covariance trace, expected hidden-state squared error, and A-optimal proxy.
    - The caveat text contains `certificate-guided` or `posterior-certificate-aware` and does not contain broad `certified optimization` branding as a claimed project label.
    - THEORY-02, THEORY-03, and THEORY-04 are named in traceability rows or self-check items.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; s=Path('.planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md').read_text(); required=['TRACE-SL/RCSS Surrogate Objective','Linear-Gaussian GLS/MAP Posterior-Error Bridge','MAE-Oriented Interpretation and Caveats','validation loss','posterior trace','scenario CVaR trace','condition number','coverage','selector','tuner','fixed weight grid','posterior covariance trace','expected hidden-state squared error','A-optimal','THEORY-02','THEORY-03','THEORY-04']; missing=[x for x in required if x not in s]; assert not missing, missing; assert ('certificate-guided' in s or 'posterior-certificate-aware' in s); assert 'broad certified optimization' not in s.lower()"</automated>
  </verify>
  <done>The bridge artifact defines the RCSS surrogate and theory bridge in a way that satisfies THEORY-02..THEORY-04 and decisions D-04, D-05, D-07..D-09, and D-12.</done>
</task>

<task type="auto">
  <name>Task 3: Add validation-swap analysis, guardrails, and source coverage audit</name>
  <files>.planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md</files>
  <read_first>
    - .planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md.
    - .planning/REQUIREMENTS.md THEORY-05 and formulation/theory scope.
    - .planning/phases/02-formulation-and-theory-bridge/02-CONTEXT.md decisions D-06, D-10, D-11, D-12 and deferred items.
    - .planning/phases/02-formulation-and-theory-bridge/02-PATTERNS.md validation-aware swap acceptance, local-optimality source, dense-solver limitation, guardrail, and self-check patterns.
    - refine-logs/WRITING_NOTES_GREEDY_OFFLINE_PLANNING.md offline planning framing.
    - TRC-23-02333/transparent_estimator_eval.py implementation anchor `validation_swap_search`.
  </read_first>
  <action>Add a `Validation-Aware Swap Analysis` section implementing D-06, D-10, and D-11. Define the fixed add-node universe from the OR-guided candidate pool; state that swaps are accepted only when validation GLS/MAP reconstruction loss improves under the implemented acceptance rule. Give complexity in terms of candidate universe size, budget `k`, validation samples/time steps, reconstruction solve cost, number of starts, and swap iterations, and state dense-solver scaling as a limitation tied to explicit inverse/solve behavior. State fixed-candidate local optimality: at termination no single remove/add swap from the fixed universe improves the chosen validation objective under the implemented acceptance rule. Add `Guardrails`, `Requirement Traceability`, and `Self-Check` sections covering THEORY-01..THEORY-05 and D-01..D-12. Explicitly exclude deferred Phase 3/4/5/7 work and raw dataset reads.</action>
  <acceptance_criteria>
    - The artifact contains `Validation-Aware Swap Analysis`, `Guardrails`, `Requirement Traceability`, and `Self-Check` sections.
    - The analysis contains `fixed add-node universe`, `OR-guided candidate pool`, `validation GLS/MAP reconstruction loss`, `single remove/add swap`, `local optimality`, `dense-solver scaling`, `candidate universe size`, `budget k`, `validation samples`, `number of starts`, and `swap iterations`.
    - The self-check marks THEORY-01..THEORY-05 and D-01..D-12 as covered.
    - The artifact states no new experiments, baseline implementation, robustness tests, final manuscript writing, or raw dataset reads are part of this phase-local artifact.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; s=Path('.planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md').read_text(); required=['Validation-Aware Swap Analysis','Guardrails','Requirement Traceability','Self-Check','fixed add-node universe','OR-guided candidate pool','validation GLS/MAP reconstruction loss','single remove/add swap','local optimality','dense-solver scaling','candidate universe size','budget k','validation samples','number of starts','swap iterations','THEORY-05']; required += [f'D-{i:02d}' for i in range(1,13)] + [f'THEORY-0{i}' for i in range(1,6)]; missing=[x for x in required if x not in s]; assert not missing, missing; forbidden=['new baseline experiments','regenerate split evidence','robustness tests are implemented here','final Transportation Science manuscript package']; assert not any(x in s for x in forbidden); assert 'raw dataset reads' in s"</automated>
  </verify>
  <done>The bridge artifact is source-complete for THEORY-01..THEORY-05, maps locked decisions D-01..D-12, and preserves Phase 2 boundaries.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Convert RCSS from heuristic framing into a formal reconstruction-aware design method | 02-01, 02-02 | COVERED | Plan 02-01 creates core formulation/theory artifact; Plan 02-02 exposes and extends it.
REQ | THEORY-01 | Budgeted sensor-set formulation with hidden-node reconstruction, held-out loss, transparent model | 02-01 | COVERED | Task 1.
REQ | THEORY-02 | TRACE-SL/RCSS surrogate with validation loss, posterior trace, scenario CVaR, condition, coverage | 02-01 | COVERED | Task 2.
REQ | THEORY-03 | Linear-Gaussian GLS/MAP posterior-error derivation | 02-01 | COVERED | Task 2.
REQ | THEORY-04 | MAE-oriented posterior variance caveats | 02-01 | COVERED | Task 2.
REQ | THEORY-05 | Validation-swap complexity and local optimality | 02-01 | COVERED | Task 3.
REQ | THEORY-06 | TR Part B optional theory-gap note | 02-02 | COVERED | Plan 02-02 Task 1.
CONTEXT | D-01..D-03 | Formalization target and traffic-network notation | 02-01 | COVERED | Task 1 references D-01..D-03.
CONTEXT | D-04..D-06 | RCSS surrogate, auto weights, validation swap definition | 02-01 | COVERED | Tasks 2 and 3 reference D-04..D-06.
CONTEXT | D-07..D-09 | Posterior-error derivation and MAE bridge caveats | 02-01 | COVERED | Task 2 references D-07..D-09.
CONTEXT | D-10..D-12 | Complexity, local optimality, certificate terminology | 02-01 | COVERED | Task 3 references D-10..D-12.
CONTEXT | D-13..D-14 | TR Part B extension boundary | 02-02 | COVERED | Plan 02-02 Task 1 references D-13..D-14.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Curated artifacts -> method/theory text | Existing code/result summaries are transformed into paper-facing claims; inaccurate transcription could mislead downstream writing. |
| Local ignored datasets -> repository docs | Raw dataset paths must not become evidence sources or committed content. |
| Validation selection -> held-out claim wording | Validation MAE must not cross into final performance evidence wording. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | 02-FORMULATION-THEORY-BRIDGE.md source claims | mitigate | Ground every method statement in named implementation identifiers from `transparent_estimator_eval.py` and Phase 1 guardrails. |
| T-02-02 | Information Disclosure | Raw traffic datasets | mitigate | Do not read or cite `TRC-23-02333/dataset/`; verify the artifact does not contain that path. |
| T-02-03 | Repudiation | Requirement/decision coverage | mitigate | Add `Requirement Traceability` and `Self-Check` sections naming THEORY-01..THEORY-05 and D-01..D-12. |
| T-02-04 | Spoofing | Certificate terminology | mitigate | Use `certificate-guided`, `posterior-certificate-aware`, or diagnostic wording only; forbid broad certified-optimization branding. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This plan performs no package-manager installs. |
</threat_model>

<verification>
Run the automated checks embedded in each task. Then run `python -c "from pathlib import Path; s=Path('.planning/phases/02-formulation-and-theory-bridge/02-FORMULATION-THEORY-BRIDGE.md').read_text(); assert all(x in s for x in ['THEORY-01','THEORY-02','THEORY-03','THEORY-04','THEORY-05']); assert 'TRC-23-02333/dataset/' not in s"` from the repository root.
</verification>

<success_criteria>
- `02-FORMULATION-THEORY-BRIDGE.md` exists and is manuscript-ready as a phase-local method/theory bridge.
- THEORY-01..THEORY-05 and D-01..D-12 are explicitly covered.
- Validation selection, held-out test evidence, empirical certificate guidance, and scoped linear-Gaussian derivation remain separated.
- No raw datasets are read or cited as evidence sources.
</success_criteria>

<output>
Create `.planning/phases/02-formulation-and-theory-bridge/02-01-SUMMARY.md` when done.
</output>
