# Quick Plan — 260528-8kn

## Intent

Synchronize remaining reviewer-facing current-best artifacts and method
contracts with the strongest paper-facing lane:

- formal CVaR tail-risk epigraph
- Holm-corrected `189/189` all-baseline wins with no surviving challenger
- network-level planning readouts aggregated at the dataset level

## Steps

1. Inspect reviewer-facing generators and `TRACE_BIOPT_SPEC.md` for stale
   strongest-baseline-only or pre-CVaR wording.
2. Upgrade the affected generators and spec to the current-best contract.
3. Extend `scripts/audit_trace_biopt_claims.py` so the stronger wording is
   machine-checked.
4. Re-run the full current-best refresh chain and verify submission-level green
   status.

## Guardrails

- Keep all claims bounded to the tested datasets, budgets, and pre-registered
  non-BiOpt registry.
- Do not upgrade Seattle `10%` beyond its retained fail-closed Stage15 lane.
- Treat reviewer-facing tables as generated artifacts, not hand-maintained TeX.
