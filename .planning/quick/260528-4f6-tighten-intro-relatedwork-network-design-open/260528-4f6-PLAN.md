# Quick Plan — 260528-4f6

## Goal

Tighten the opening of Section 1 and Section 2 so a TR-B reviewer reads the
paper earlier as a fixed-infrastructure transportation network-design argument
built around a transparent inverse problem, rather than as a sparse-sensor
benchmark or estimator-side comparison.

## Scope

- Tighten:
  - `paper/sections/1_introduction.tex`
  - `paper/sections/2_related_work.tex`
- Extend `scripts/audit_trace_biopt_claims.py` with checks for the new
  introduction and related-work opening sentences.
- Re-run the full current-best paper chain.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
