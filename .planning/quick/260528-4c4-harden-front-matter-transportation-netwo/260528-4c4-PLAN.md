# Quick Plan — 260528-4c4

## Goal

Harden the front matter itself so the first screen a TR-B reviewer sees
shorttitle/highlights/keywords/abstract-close all reinforce the same identity:
TRACE-BiOpt is transportation network design built around a transparent inverse
problem.

## Scope

- Tighten:
  - `paper/main.tex`
  - `paper/sections/0_abstract.tex`
- Extend `scripts/audit_trace_biopt_claims.py` with checks for the new
  shorttitle, highlights, keywords, and abstract-close wording.
- Re-run the full current-best paper chain.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
