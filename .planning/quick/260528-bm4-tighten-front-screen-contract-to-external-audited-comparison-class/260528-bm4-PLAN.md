# Quick Plan — 260528-bm4

## Goal

Tighten the manuscript's first-screen contract from a generic strongest-baseline wording to a stronger `external audited comparison-class contract` wording, while making the same contract visible in the front-page generated tables and keeping the full submission chain green.

## Scope

- Update front-screen manuscript wording in the compiled abstract, standalone abstract, introduction, related-work opening, and conclusion.
- Upgrade front-page generated tables so the reader guide and contribution stack use the same `external audited comparison-class contract` language and explicitly mention the deterministic solver.
- Synchronize discussion-facing boundary wording and the machine claim audit so the new comparison-class contract is enforced automatically.
- Rebuild the current-best paper chain and verify that the LaTeX log remains clean.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_reader_guide_table.py scripts/generate_trace_biopt_contribution_stack_table.py scripts/generate_trace_biopt_discussion_boundary_table.py scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
- `rg -n -e 'Underfull' -e 'Overfull' -e 'Extra alignment' -e 'Misplaced \\\\noalign' -e 'undefined' paper/main.log`
