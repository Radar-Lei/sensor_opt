# Quick Plan — 260528-c1m

## Goal

Add a Section 3 reviewer-facing problem contract so the formal problem definition reads as a transportation network-design contract rather than as a generic sparse-sensor benchmark setup.

## Scope

- Create an automatic `TRACE-BiOpt problem contract` generator.
- Insert the generated table into `paper/sections/3_problem.tex`.
- Wire the new generator into the current-best refresh chain.
- Extend the machine claim audit so the new problem-contract table and its wording are required, not decorative.
- Revalidate the full paper chain and check that `paper/main.log` remains clean.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_problem_contract_table.py scripts/audit_trace_biopt_claims.py`
- `python scripts/generate_trace_biopt_problem_contract_table.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
- `rg -n -e 'Underfull' -e 'Overfull' -e 'Extra alignment' -e 'Misplaced \\\\noalign' -e 'undefined' paper/main.log`
