# Quick Plan — 260528-4r1

## Goal

Tighten the manuscript's problem-definition-level TR-B framing so that
`recoverability-driven network design` is expressed consistently in the
title/front matter, introduction, formal problem section, and conclusion.

## Scope

- Strengthen wording in:
  - `paper/sections/1_introduction.tex`
  - `paper/sections/3_problem.tex`
  - `paper/sections/8_conclusion.tex`
- Extend `scripts/audit_trace_biopt_claims.py` so the stronger network-design
  wording becomes machine-checked and cannot drift back toward a benchmark-only
  reading.
- Re-run the full current-best paper chain and verify that the submission gate
  remains green.

## Constraints

- Do not widen claims beyond the existing current-best evidence chain.
- Preserve the final evidence mix:
  - six PeMS rows + Seattle 20/30 promoted via Stage16 replaceable reruns
  - Seattle 10% intentionally fail-closed on Stage15 main evidence
- Keep all audits and submission verification passing.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
