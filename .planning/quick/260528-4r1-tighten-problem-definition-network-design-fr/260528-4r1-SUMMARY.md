# Quick Summary — 260528-4r1

## What changed

- Tightened the front-of-paper identity so the manuscript now states more
  explicitly that:
  - the design object is recoverability of the hidden network state rather than
    visibility of the observed subset,
  - the paper studies sparse traffic sensing as recoverability-driven network
    design for transparent state reconstruction,
  - the contribution is not a candidate-pool selector or estimator benchmark on
    a fixed graph.
- Renamed Section 3 to `Recoverability-driven bilevel network design`.
- Strengthened the conclusion so the intended reading is explicitly a
  transportation network-design paper with a transparent inverse problem at its
  core, not a benchmark paper.
- Extended `scripts/audit_trace_biopt_claims.py` to machine-check these new
  framing sentences.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py` — PASS
- `python scripts/audit_trace_biopt_claims.py` — PASS (`1397` checks)
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh` — PASS
  - `paper-claim-audit`: PASS
  - `proof-checker`: PASS
  - `citation-audit`: PASS
  - `kill-argument`: PASS
  - submission verifier: all `OK / PASS / stale=false`

## Result

The current-best paper chain remains green, `paper/main.pdf` rebuilds
successfully to 47 pages, and the manuscript now signals
`recoverability-driven network design` not only in the title/front matter but
also in the problem definition and closing interpretation.
