# Quick Plan — 260528-bx4

## Goal

Add a Section 2 reviewer-facing comparison-class contract so the paper states, in one machine-audited table, how TRACE-BiOpt should be classified before the detailed literature split begins.

## Scope

- Create an automatic `TRACE-BiOpt comparison-class contract` generator.
- Insert the generated table into `paper/sections/2_related_work.tex`.
- Wire the new generator into the current-best refresh chain.
- Extend the machine claim audit so the new comparison-class table and its wording are required, not decorative.
- Revalidate the full paper chain and check that `paper/main.log` remains clean.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_comparison_class_table.py scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
- `rg -n -e 'Underfull' -e 'Overfull' -e 'Extra alignment' -e 'Misplaced \\\\noalign' -e 'undefined' paper/main.log`
