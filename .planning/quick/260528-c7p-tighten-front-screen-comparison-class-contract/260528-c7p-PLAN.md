# Quick Plan — 260528-c7p

## Goal

Tighten the manuscript's front-screen comparison-class wording so the main claim is no longer described only as strongest-baseline dominance, but as an external audited comparison-class contract that compresses to row-wise strongest-baseline wins and then survives all-baseline corrected paired tests.

## Scope

- Update the compiled front-matter abstract, standalone abstract, introduction opening, related-work opening, and conclusion closing to the stronger comparison-class wording.
- Keep the numerical evidence unchanged; only strengthen the reviewer-facing contract to match the already audited all-baseline evidence.
- Extend the machine claim audit so the new wording is required, not ad hoc.
- Revalidate the full paper chain and check that `paper/main.log` remains clean.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
- `rg -n -e 'Underfull' -e 'Overfull' -e 'Extra alignment' -e 'Misplaced \\\\noalign' -e 'undefined' paper/main.log`
