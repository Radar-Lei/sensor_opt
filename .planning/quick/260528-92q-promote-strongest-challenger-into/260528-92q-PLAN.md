# Quick Plan — 260528-92q

## Intent

Move the row-wise strongest challenger out of prose and directly into the main
current-best dominance table so the primary experiments artifact matches the
reviewer-facing shape suggested in `gpt_pro_suggestion_round1.md`.

## Steps

1. Extend the dominance-table generator to show the strongest challenger name in
   each dataset-budget row.
2. Update the experiments text so the manuscript explicitly points reviewers to
   the table rather than to a prose footnote.
3. Extend `scripts/audit_trace_biopt_claims.py` to machine-check the challenger
   identities shown in the compiled table.
4. Re-run the full current-best refresh chain and verify that the submission
   gates stay green without new LaTeX table warnings.

## Guardrails

- Keep challenger labels tied to the audited current-best evidence sources, not
  to hand-maintained prose.
- Do not change any row-level MAE, delta, win-count, or p-value figures.
- Preserve the bounded-claim policy: direct challenger names improve visibility
  but do not widen the claim scope beyond the current-best evidence contract.
