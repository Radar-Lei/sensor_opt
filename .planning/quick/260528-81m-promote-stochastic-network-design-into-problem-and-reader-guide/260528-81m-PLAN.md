# Quick Plan — 260528-81m

## Goal

Push the `bilevel stochastic network design` identity one layer earlier in the
paper by moving it into the formal problem definition and the front-page reader
guide, not only the method and conclusion.

## Scope

- Tighten:
  - `paper/sections/3_problem.tex`
  - `paper/sections/1_introduction.tex`
- Update the generated reader-guide table:
  - `scripts/generate_trace_biopt_reader_guide_table.py`
- Extend `scripts/audit_trace_biopt_claims.py` so the stochastic problem
  definition and reader-guide wording are machine-checked.
- Re-run the full current-best paper chain.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_reader_guide_table.py scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
