# Quick Plan — 260528-9bf

## Intent

Make the all-baseline superiority claim more reviewer-facing by exposing, in
the main experiments section, the difference between the row-wise strongest
mean challenger and the hardest challenger that remains closest after Holm
correction.

## Steps

1. Generate a paper-visible challenger-posture artifact from the live
   current-best dominance and significance CSVs.
2. Insert that artifact into the main experiments section with bounded wording
   that explains why mismatched challenger families strengthen the claim.
3. Extend `scripts/audit_trace_biopt_claims.py` so the new table, mismatch
   count, and key row-level examples are machine-checked.
4. Re-run the current-best chain, clear any LaTeX table warnings, and confirm
   all submission gates remain green.

## Guardrails

- Do not widen the claim beyond the audited current-best baseline registry.
- Keep the distinction explicit: strongest mean challenger is not always the
  same as the hardest corrected challenger.
- Treat formatting cleanup as secondary to preserving machine-audited wording
  and evidence values.
