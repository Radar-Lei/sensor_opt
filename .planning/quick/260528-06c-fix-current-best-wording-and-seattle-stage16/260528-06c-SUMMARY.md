# Quick Task 260528-06c Summary

## Outcome

This round fixed stale public wording in the current-best paper chain and
prepared the remaining Seattle rows for Stage16 promotion.

The wording repair had two parts. First, `paper/main.tex` and
`paper/sections/A_appendix.tex` no longer describe the current-best chain as if
the calibrated reruns were still incomplete. The manuscript now says the six
PeMS rows are already promoted through replaceable Stage16 reruns and that only
the three Seattle rows remain on the Stage15 main path. Second,
`scripts/generate_trace_biopt_claim_contract.py` now emits row-specific current-
best wording instead of hard-coding `Stage15 evidence run`; promoted rows cite
their replaceable Stage16 root and retained Seattle rows explicitly say they
are still Stage15-backed.

The execution-side change was to add
`scripts/run_stage16_seattle_calibrated_sweep.sh`. It launches a Seattle
10/20/30% calibrated rerun with the existing Stage15 Seattle solver settings
and `--trace-biopt-risk-source train_val`, which is the conservative next step
for promoting the last three current-best rows without changing multiple axes
at once. The refresh chain and watcher were extended to include the new root
`stage16_calibrated_trace_sweep/seattle_10_20_30_trainval`.

Because shell-background jobs were getting reaped when the command session
exited, the Seattle sweep and watcher were finally started as user transient
services rather than as ordinary `nohup` children:

- `codex-seattle-stage16.service`
- `codex-current-best-watcher.service`

At the end of the task, the Seattle rerun had begun on `seed_25`; the latest
checkpoint shows budget `0.2`, stage `trace_biopt_forward_step`, iteration
`59`, and objective `3.9545567341`.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_claim_contract.py scripts/audit_trace_biopt_claims.py`
- `pytest -q scripts/test_generate_trace_biopt_claim_contract.py`
- `bash -n scripts/run_stage16_seattle_calibrated_sweep.sh`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
- `systemctl --user status codex-seattle-stage16 --no-pager`
- `systemctl --user status codex-current-best-watcher --no-pager`

## Result

The current-best paper chain remained fully green after the wording and refresh
changes:

- `paper-claim-audit`: `PASS` at `1259` checks
- `proof-checker`: `PASS`
- `citation-audit`: `PASS`
- `kill-argument`: `PASS`
- submission verifier: all four audits `OK / PASS / stale=false`
- `paper/main.pdf`: `43` pages

Seattle Stage16 is now actively running instead of just being planned. The
current-best chain still uses Stage15 evidence on Seattle `10/20/30%`, but the
promotion path is now concrete, wired into the watcher, and already producing
live checkpoint artifacts under
`TRC-23-02333/trace_sl_results/stage16_calibrated_trace_sweep/seattle_10_20_30_trainval/`.
