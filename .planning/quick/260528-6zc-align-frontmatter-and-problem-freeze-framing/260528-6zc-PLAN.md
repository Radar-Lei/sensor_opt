# Quick Plan — 260528-6zc

## Goal

Remove a real front-matter drift and push the same fixed-infrastructure
freeze-before-observation framing into the formal problem definition, so the
paper's title page and Section 3 state the same transportation network-design
timing logic.

## Scope

- Tighten:
  - `paper/main.tex`
  - `paper/sections/3_problem.tex`
- Extend `scripts/audit_trace_biopt_claims.py` with checks for:
  - front-matter abstract freeze-before-observation wording
  - Section 3 deployment-time layout timing wording
- Re-run the full current-best paper chain.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
