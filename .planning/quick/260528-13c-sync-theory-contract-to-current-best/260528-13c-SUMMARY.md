# Quick Summary — 260528-13c

## What changed

- Updated `TRACE_BIOPT_THEORY.md` so it now matches the current manuscript and
  evidence state:
  - method scope is `recoverability-driven bilevel stochastic network design`,
  - Proposition T6 is now the `CVaR Tail-Risk Epigraph and Interpretation`,
  - the evidence gate now reflects the current-best chain rather than the old
    Stage15-only directional contract,
  - it explicitly records:
    - `supported_submission_ready`,
    - `9/9` submission-ready paired-dominance rows,
    - `8/9` Stage16-promoted rows,
    - `Seattle 10%` retained as `fail_closed`,
    - `189` Holm-corrected current-best row-baseline comparisons with no tied
      or better challenger,
    - `27/27` bounded exact subnetwork hits.
- Updated `scripts/audit_trace_biopt_proofs.py` so the generated proof
  skeleton no longer refers to a stale `CVaR remark`, but to the formal CVaR
  epigraph proposition.
- Extended `scripts/audit_trace_biopt_claims.py` so the machine audit now
  checks the theory contract for:
  - the new CVaR proposition title,
  - `supported_submission_ready`,
  - the corrected `189`-comparison significance lane,
  - the `Seattle 10%` fail-closed status.

## Verification

- `python scripts/audit_trace_biopt_proofs.py` — PASS
  - `proof-checker`: PASS (`52` obligations)
- `python scripts/audit_trace_biopt_claims.py` — PASS
  - `paper-claim-audit`: PASS (`1488` checks)
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh` — PASS
  - `paper-claim-audit`: PASS (`1488` checks)
  - `proof-checker`: PASS
  - `citation-audit`: PASS
  - `kill-argument`: PASS
  - submission verifier: all `OK / PASS / stale=false`
  - `paper/main.pdf`: rebuilt at `49` pages

## Result

The theory-side contracts are now aligned with the paper-facing current-best
story. That removes a real methodology credibility gap: the formal theory
contract no longer trails the manuscript on CVaR theory strength or on the
current-best evidence state.
