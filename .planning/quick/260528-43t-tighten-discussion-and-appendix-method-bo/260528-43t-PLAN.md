# Quick Plan — 260528-43t

## Goal

Tighten the Discussion and Appendix so the paper's late-stage method framing no
longer slips back toward `layout generator`, `heuristic`, or `selector`
language when describing TRACE-BiOpt itself.

## Scope

- Tighten:
  - `paper/sections/7_discussion.tex`
  - `paper/sections/A_appendix.tex`
- Extend `scripts/audit_trace_biopt_claims.py` with checks for the new
  discussion/appendix wording.
- Re-run the full current-best paper chain.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
