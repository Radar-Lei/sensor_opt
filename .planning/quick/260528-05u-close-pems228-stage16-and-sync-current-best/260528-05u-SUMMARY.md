# Quick Task 260528-05u Summary

## Outcome

Closed the `PeMS7_228 20/30` Stage16 calibrated full-search sweep and absorbed
it into the current-best paper chain. Both rows are now `replaceable`, so the
current-best dominance chain uses Stage16 evidence on all three `PeMS7_1026`
rows and all three `PeMS7_228` rows; only the three Seattle rows remain backed
by Stage15 main evidence.

This round also added a reviewer-facing budget-phasing table:
`paper/tables/table_trace_biopt_budget_phasing.tex` with backing CSV
`current_best_trace_biopt_evidence/trace_biopt_budget_phasing.csv`. The table
turns the current-best 10/20/30% curves into a staged deployment readout
instead of another dominance summary.

Because the new `PeMS7_228 20/30` rows changed the current-best source mix,
the update had to propagate through multiple current-best artifacts. I repaired
the Stage16-root handling and refreshed the paper text and audits for:

- MAP stability posture;
- solver scale and compute posture;
- exchange certificates;
- objective descent and exchange-gap figures;
- claim-boundary and discussion-boundary tables;
- paired-margin wording and other current-best counts.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_budget_phasing_table.py scripts/generate_trace_biopt_compute_posture_table.py scripts/generate_trace_biopt_map_stability_table.py scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`

## Result

The refreshed current-best paper chain is fully green:

- `paper-claim-audit`: `PASS` at `1259` checks
- `proof-checker`: `PASS`
- `citation-audit`: `PASS`
- `kill-argument`: `PASS`
- submission verifier: all four audits `OK / PASS / stale=false`
- `paper/main.pdf`: `43` pages

The Stage16 replacement gate is now closed for every in-flight PeMS row:

- `PeMS7_1026 10/20/30`: `replaceable`
- `PeMS7_228 10/20/30`: `replaceable`
- `Seattle 10/20/30`: still current-best `Stage15 main evidence`

The sweep/progress lane is no longer in-flight for `PeMS7_228 20/30`: the
replacement summary shows `10/10` completed seeds and the live-progress CSV is
empty.
