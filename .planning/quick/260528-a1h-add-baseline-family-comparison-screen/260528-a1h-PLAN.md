# Quick Plan — 260528-a1h

## Intent

Lift the comparison class itself into the main experiments section by
compressing the 21 pre-registered non-BiOpt baselines into a reviewer-facing
family-level screen, so the manuscript can show that TRACE-BiOpt does not only
beat one convenient challenger but clears an audited, diverse baseline family
registry.

## Steps

1. Generate a current-best family-level comparison artifact from the existing
   baseline registry, strongest-challenger, and all-baseline significance CSVs.
2. Insert that artifact into the pre-registered baseline subsection with
   bounded wording that emphasizes breadth of the comparison class rather than
   overclaiming universal superiority.
3. Extend `scripts/audit_trace_biopt_claims.py` so the family counts, key
   family-level near-miss facts, and table wording are machine-checked.
4. Re-run the full current-best paper chain, clear any LaTeX warnings, and
   confirm all submission gates remain green.

## Guardrails

- Keep the claim scoped to the audited pre-registered baseline registry, not
  all possible future baselines.
- Preserve the strongest family-level fact exactly: the prior TRACE-SL / RCSS
  family provides `8/9` strongest-mean challengers and `7/9` hardest corrected
  challengers, yet still leaves zero corrected survivors.
- Treat formatting cleanup as secondary to preserving machine-audited family
  counts, deltas, and corrected-test readouts.
