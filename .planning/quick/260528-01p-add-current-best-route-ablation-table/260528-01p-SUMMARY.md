# Quick Task 260528-01p Summary

## Outcome

Added a reviewer-facing current-best route-ablation table:
`paper/tables/table_trace_biopt_route_ablation.tex` with backing CSV
`current_best_trace_biopt_evidence/trace_biopt_route_ablation_summary.csv`.

This upgrades the ablation layer from the old TRACE-SL component ladder to
bounded route perturbations inside the current TRACE-BiOpt family. The new
table now makes five paper-visible comparisons explicit:

- `PeMS7_228 10%`: the promoted calibrated low-cert route beats the original
  Stage15 certified route by `0.129` MAE;
- `PeMS7_228 10%`: the same promoted route also beats the zero-weight
  strong-search probe by `0.169` MAE;
- `Seattle 20/30%`: removing the posterior/CVaR/spatial terms under stronger
  search still worsens held-out MAE by `0.036` and `0.032`;
- `PeMS7_1026 30%`: the calibrated rerun lowers mean held-out MAE from
  `3.071` to `3.037` inside the same objective family rather than by switching
  to a different baseline family.

This improves the TR-B-facing method story. The manuscript no longer leans
only on a legacy Stage12 TRACE-SL ablation background. It now also shows, in
the current-best evidence chain, which route ingredients mattered on the rows
that determined the final claim: upper-level risk calibration, certificate
weights, and exchange-search strength.

The audit layer was extended at the same time. `scripts/audit_trace_biopt_claims.py`
now checks the new table inclusion, the five-row backing CSV, the route-level
MAE and delta values, and the reviewer-facing wording in Section 6.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_route_ablation_table.py scripts/audit_trace_biopt_claims.py`
- `python scripts/generate_trace_biopt_route_ablation_table.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`

## Result

The refreshed current-best paper chain stayed fully green. `paper-claim-audit`
expanded to `1180` checks, `proof-checker`, `citation-audit`, and
`kill-argument` all remained `PASS`, the submission verifier stayed
`OK / PASS / stale=false` on all four audits, and `paper/main.pdf` rebuilt
cleanly at `41` pages. During the refresh, the in-flight `PeMS7_228 20/30`
Stage16 calibrated sweep remained at `8/10` completed seeds per budget and the
live checkpoint advanced to `seed_33`, `30%` budget, `trace_biopt_exchange_step`
iteration `10`.
