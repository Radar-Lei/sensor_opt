# Quick Task 260528-04n Summary

## Outcome

Added a reviewer-facing discussion boundary table:
`paper/tables/table_trace_biopt_discussion_boundary.tex` with backing CSV
`current_best_trace_biopt_evidence/trace_biopt_discussion_boundary.csv`.

This promotes the existing appendix-level claim-boundary matrix into the main
discussion section without changing claim scope. The new table compresses the
manuscript's five boundary lanes into a main-text readout:

- baseline scope;
- calibrated rerun status;
- theory and solver scope;
- mechanism diagnostics;
- stress evidence.

For each lane, the table states what still stands, what the paper explicitly
does not claim, and how a reviewer should read that limitation. This makes the
TR-B-facing discussion sharper: the paper now places its strongest wins and its
explicit non-claims side by side in the main text instead of leaving that
structure only in the appendix.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_discussion_boundary_table.py scripts/audit_trace_biopt_claims.py`
- `python scripts/generate_trace_biopt_discussion_boundary_table.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`

## Result

The refreshed current-best paper chain stayed fully green:

- `paper-claim-audit`: `PASS` at `1226` checks
- `proof-checker`: `PASS`
- `citation-audit`: `PASS`
- `kill-argument`: `PASS`
- submission verifier: all four audits `OK / PASS / stale=false`
- `paper/main.pdf`: `42` pages

The external evidence gap remains unchanged: `PeMS7_228 20/30` is still
`9/10` completed in the in-flight Stage16 calibrated rerun, so those two rows
remain `in_progress` rather than `replaceable`.
