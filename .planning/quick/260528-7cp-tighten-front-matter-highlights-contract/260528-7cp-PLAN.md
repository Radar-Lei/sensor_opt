# Quick Plan — 260528-7cp

## Goal

Tighten the title-page highlights so the paper's front matter states the full
TR-B contribution stack in five short lines: fixed-infrastructure problem
class, one method, strongest-baseline evidence, theory package, and bounded
exactness.

## Scope

- Tighten:
  - `paper/main.tex`
- Extend `scripts/audit_trace_biopt_claims.py` with checks for the new
  highlights wording.
- Re-run the full current-best paper chain.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
