# Quick Summary: Diagnose PeMS7_228 Low-Budget Objective

Completed: 2026-05-27T06:35:51+08:00

## Result

PeMS7_228 at 10% budget was improved through a method-level calibration-risk change, not by post-hoc baseline selection.

New implementation:

- added `--trace-biopt-risk-source {val,train,train_val}`;
- default remains `val`, preserving existing Stage15 evidence;
- `train_val` estimates upper-level TRACE-BiOpt reconstruction risk on train+validation days to reduce two-day validation overfitting.

Uniform diagnostic configuration:

- `--trace-biopt-risk-source train_val`;
- `beta=0.5`, `gamma=0.01`, `eta=0.0`, Huber delta `1.0`;
- complete one-exchange search (`forward_pool=0`, `exchange_add_pool=0`, `exchange_remove_pool=0`);
- original Stage15 split seeds 25-34.

Evidence:

- default Stage15 margin: `-0.0162`;
- calibrated-risk margin: `-0.1022`;
- mean TRACE-BiOpt MAE gain: `0.0860`;
- seed wins: `10/10`;
- calibrated-risk paired t-test: `p=6.4e-05`;
- Wilcoxon signed-rank: `p=0.0010`.

## Artifacts

- `TRC-23-02333/trace_sl_results/stage15_biopt_pems228_10_risksource_probe/train_val_lowcert_delta1_fullsearch/`
- `TRC-23-02333/trace_sl_results/paper_sources/trace_biopt_calibration_risk_probe.csv`
- `paper/tables/table_trace_biopt_calibration_probe.tex`
- `scripts/generate_trace_biopt_calibration_probe_table.py`

## Verification

- `python scripts/generate_trace_biopt_calibration_probe_table.py`
- `python scripts/audit_trace_biopt_claims.py` -> PASS, 432 checks
- `python scripts/generate_paper_audit_artifacts.py`
- `latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex`
- `bash /home/samuel/aris_repo/tools/verify_paper_audits.sh paper/ --assurance submission` -> all mandatory audits PASS and stale=false
- `python -m py_compile TRC-23-02333/transparent_estimator_eval.py scripts/generate_trace_biopt_calibration_probe_table.py scripts/generate_trace_biopt_search_probe_table.py scripts/generate_paper_audit_artifacts.py scripts/audit_trace_biopt_claims.py`
- `git diff --check`

## Claim Boundary

This diagnostic supports a calibration-risk refinement for low-budget regimes. It does not replace the full nine-regime Stage15 main table until the same configuration is rerun across all dataset-budget regimes.
