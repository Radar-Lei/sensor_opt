# Quick Plan — 260528-a8v

## Intent

Move the newly audited family-level comparison-class fact from the experiments
section into the manuscript front screen and closing sections, so the paper
states earlier that TRACE-BiOpt clears a broad pre-registered baseline
registry rather than only a row-wise best-baseline table.

## Steps

1. Promote the `21 baselines / 11 families` comparison-class fact into the
   standalone abstract, compiled front-matter abstract, highlights, introduction
   contribution ladder, and conclusion.
2. Extend `scripts/audit_trace_biopt_claims.py` so the new front-screen and
   closing wording is machine-checked.
3. Re-run the full current-best paper chain, confirm the 51-page PDF rebuilds
   cleanly, and verify the submission gates remain green.

## Guardrails

- Keep the scope explicit: the comparison-class claim is limited to the audited
  pre-registered baseline registry, not all imaginable baselines.
- Preserve the stronger empirical structure already in place: 21 baselines,
  11 families, 189 corrected paired comparisons, and zero surviving tied-or-better
  challenger.
- Treat front-screen wording as a compression of audited evidence, not as a
  substitute for the underlying experiments tables.
