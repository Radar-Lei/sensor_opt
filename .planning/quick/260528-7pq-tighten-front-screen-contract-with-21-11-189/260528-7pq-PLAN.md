# Quick Plan — 260528-7pq

## Intent

Tighten the manuscript's first-screen comparison-class contract so the
reviewer-facing summary no longer stops at `one strongest-baseline evidence
contract`, but explicitly carries the audited `21 baselines / 11 families /
189 corrected paired tests / no survivor` fact through the abstract,
introduction, related-work opening, and conclusion.

## Steps

1. Compress the front-screen contract wording in the standalone abstract,
   compiled front-matter abstract, introduction, related-work opening, and
   conclusion so they all surface the same audited comparison-class fact.
2. Extend `scripts/audit_trace_biopt_claims.py` so the stronger
   `21 / 11 / 189 / no survivor` wording is machine-checked in each location.
3. Re-run the full current-best paper chain, confirm the PDF rebuilds cleanly,
   and verify the submission gates and LaTeX log remain green.

## Guardrails

- Keep the statement scoped to the audited pre-registered comparison class; do
  not imply dominance over untested baselines.
- Do not expand the experiment surface or add another table; this is a
  front-screen compression of already audited evidence.
- Preserve the current `TR Part B` reading: fixed-infrastructure decision,
  transparent inverse problem, recoverability-driven bilevel stochastic
  network design, and explicit strongest-baseline evidence contract.
