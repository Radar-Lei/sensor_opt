# Quick Plan — 260528-0403

## Goal

Push the `bilevel stochastic transportation network-design` comparison class
from the method/problem layer into the first compiled screen of the manuscript.

## Scope

- Tighten front matter and abstract wording:
  - `paper/main.tex`
  - `paper/sections/0_abstract.tex`
- Tighten the introduction opening so the stochastic design reading appears
  before the reviewer reaches Section 3:
  - `paper/sections/1_introduction.tex`
- Extend `scripts/audit_trace_biopt_claims.py` so the compiled front-matter
  abstract, highlights, and updated wording are machine-checked.
- Re-run the full current-best paper chain.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
