# Quick Plan — 260528-9mz

## Intent

Turn the TRACE-BiOpt theorem list into a reviewer-facing, current-best bridge
that maps each scoped theory result to the paper-visible evidence lane that a
reader can immediately inspect.

## Steps

1. Generate a six-row theory-bridge artifact that links `T1`--`T6` to the
   current-best diagnostic, exactness, and promoted-evidence tables already in
   the manuscript.
2. Insert that bridge into `Section 4` with bounded wording that explains the
   table is a navigation aid between theory and evidence, not an empirical
   proof of the theorems.
3. Extend `scripts/audit_trace_biopt_claims.py` so the theorem labels,
   row-level readouts, and bridge-language needles are machine-checked.
4. Re-run the full current-best paper chain, clear any LaTeX table warnings,
   and confirm all submission gates remain green.

## Guardrails

- Do not overclaim empirical confirmation of theorems; keep the bridge scoped
  to theorem-facing evidence lanes and reviewer navigation.
- Preserve the exact current-best readouts: `6/9` MAP-stability rows, `4/4`
  matched posterior-risk probes, burden range `0.263` to `0.770`, exactness
  `27/27`, the initializer-family route labels, and CVaR share `0.26%` to
  `1.46%`.
- Keep TeX escaping local to the compiled table; the audit CSV should retain
  the machine-readable raw identifiers.
