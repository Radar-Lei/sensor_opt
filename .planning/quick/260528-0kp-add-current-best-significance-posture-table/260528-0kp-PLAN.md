# Quick Plan — 260528-0kp

## Goal

Make the `beat all baselines` claim more reviewer-facing by turning the
current-best all-baseline paired-significance posture into a paper-visible,
machine-audited main-chain artifact.

## Scope

- Add a current-best significance-posture generator:
  - `scripts/generate_trace_biopt_significance_posture_table.py`
- Surface the resulting summary in the main experiments section:
  - `paper/sections/5_experiments.tex`
- Wire the new generator into the refresh chain:
  - `scripts/refresh_current_best_trace_biopt_paper_chain.sh`
- Extend the claim audit so the corrected all-baseline significance wording
  and generated CSV/TEX artifacts are machine-checked:
  - `scripts/audit_trace_biopt_claims.py`
- Re-run the full current-best paper chain.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_significance_posture_table.py scripts/audit_trace_biopt_claims.py`
- `python scripts/generate_trace_biopt_significance_posture_table.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
