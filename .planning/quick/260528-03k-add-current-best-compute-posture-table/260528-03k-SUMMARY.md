# Quick Task 260528-03k Summary

## Outcome

Added a reviewer-facing current-best compute-posture table:
`paper/tables/table_trace_biopt_compute_posture.tex` with backing CSVs
`current_best_trace_biopt_evidence/trace_biopt_compute_posture.csv` and
`trace_biopt_compute_posture_detail.csv`.

This closes one of the last explicit `gpt_pro_suggestion_round1.md` requests
that had not yet been made paper-visible: a runtime/convergence view of the
current-best TRACE-BiOpt chain. The new artifact stays honest about scope. It
does not claim exact per-budget solver times for mixed-budget source runs.
Instead, it reports source-route wall-clock from progress-file creation to
checkpoint creation, plus route-level peak RSS and accepted exchange counts.

The resulting paper-visible compute picture is:

- the promoted `PeMS7_228 10%` rerun is the lightest current-best route at
  about `1.3` minutes and `0.42` GB peak;
- the promoted `PeMS7_1026 30%` rerun stays moderate at about `12.0` minutes
  and `1.61` GB peak;
- the paired `PeMS7_1026 10/20%` calibrated rerun is the heaviest route at
  about `42.5` minutes on average and `3.02` GB at the slowest split;
- the retained Stage15 main-evidence routes for `PeMS7_228 20/30%` and
  `Seattle 10/20/30%` remain short all-budget runs.

This strengthens the TR-B-facing methods story. The manuscript now does not
only show that calibrated reruns and deeper exchange search matter; it also
shows where the compute burden actually lands inside the current-best evidence
chain.

## Verification

- `python -m py_compile scripts/generate_trace_biopt_compute_posture_table.py scripts/audit_trace_biopt_claims.py`
- `python scripts/generate_trace_biopt_compute_posture_table.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`

## Result

The refreshed current-best paper chain stayed fully green:

- `paper-claim-audit`: `PASS` at `1208` checks
- `proof-checker`: `PASS`
- `citation-audit`: `PASS`
- `kill-argument`: `PASS`
- submission verifier: all four audits `OK / PASS / stale=false`
- `paper/main.pdf`: `42` pages

The main evidence gap remains external rather than manuscript-side:
`PeMS7_228 20/30` still sits at `9/10` completed Stage16 seeds and therefore
remains `in_progress` rather than `replaceable`.
