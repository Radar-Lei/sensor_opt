---
quick_id: 260528-2on
slug: add-bounded-exact-subnetwork-benchmark-t
status: completed
---

# Quick Task 260528-2on: Add bounded exact subnetwork benchmark to current-best paper chain

## Tasks

1. Design a bounded exact benchmark that reuses the row-wise current-best TRACE-BiOpt route parameters on deterministic small induced subnetworks instead of introducing a second solver family.
2. Implement the generator, connect its CSV/TeX outputs to the refresh chain and discussion section, and extend the machine claim audit so the exact-benchmark wording stays synchronized with the artifacts.
3. Re-run the current-best refresh chain, restore all paper audits and the submission verifier to green, and sync the GSD quick-task state.
