# Quick Plan — 260528-58w

## Goal

Remove lingering `portfolio / selector` wording from the experiment and
mechanism sections so the main empirical lane reads as a single-method,
pre-registered-baseline registry comparison rather than as a meta-selection
story.

## Scope

- Tighten:
  - `paper/sections/5_experiments.tex`
  - `paper/sections/6_ablation_robustness.tex`
- Extend `scripts/audit_trace_biopt_claims.py` with the new wording checks.
- Re-run the full current-best paper chain.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`
