# Quick Summary — 260528-58w

## What changed

- Replaced `baseline portfolio` with `pre-registered baseline registry` in the
  main experiments section.
- Replaced `validation-swap TRACE-SL selector` and `pool-centered TRACE-SL
  selector` wording with `layout rule` / `selection rule`.
- Tightened the mechanism section so the old TRACE-SL comparison lane is
  described as a historical selector-centered narrative rather than something
  resembling the current method identity.
- Extended `scripts/audit_trace_biopt_claims.py` to machine-check the new
  wording.

## Verification

- `python -m py_compile scripts/audit_trace_biopt_claims.py` — PASS
- `python scripts/audit_trace_biopt_claims.py` — PASS (`1407` checks)
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh` — PASS
  - `paper-claim-audit`: PASS
  - `proof-checker`: PASS
  - `citation-audit`: PASS
  - `kill-argument`: PASS
  - submission verifier: all `OK / PASS / stale=false`

## Result

The 47-page submission package remains green, and the main empirical lane now
describes itself more consistently as a fixed baseline-registry comparison for
one bilevel network-design method rather than as a portfolio/selector story.
