# Quick Task 260528-09m Summary

## Outcome

This round tightened the Seattle mechanism lane after the final Seattle Stage16
promotion.

The key issue was not a broken main result. The current-best chain was already
green at `8/9` Stage16-promoted rows. The issue was narrower and more serious:
several mechanism-facing tables and paragraphs still described the Seattle
single-seed certificate-removal / weight-sensitivity slices as if they were
current-best Seattle rows, even though the main evidence chain had already
moved Seattle `20/30%` onto Stage16 calibrated reruns.

I first added a new matched probe launcher:

- `scripts/run_stage17_seattle_trainval_zero_weight_probe.sh`

and ran the resulting artifact:

- `TRC-23-02333/trace_sl_results/stage17_seattle_trainval_zero_weight_probe/seed_25`

That probe showed why the old wording should not simply be switched to the
promoted current-best route. On the promoted Seattle `20%` split-25 route, the
matched `train_val + (0,0,0)` zero-weight probe did not preserve the earlier
same-sign held-out penalty. So the right fix was not to force the Seattle
mechanism story to claim more. The right fix was to make the evidence lane
honest.

The manuscript and generators now explicitly treat the Seattle `20/30%`
certificate-removal, posterior-risk, tail-risk, and weight-sensitivity slices
as bounded same-split Seattle diagnostic routes rather than as replacement
current-best rows. At the same time, the main manuscript wording was synced to
the real current-best state:

- all six PeMS rows are Stage16 promoted
- Seattle `20/30%` are Stage16 promoted
- Seattle `10%` remains fail-closed on Stage15 main evidence

This closes a real rigor gap: the paper now separates the audited nine-row
current-best dominance chain from the narrower Seattle mechanism slices instead
of letting the two blur together.

## Verification

- `bash scripts/run_stage17_seattle_trainval_zero_weight_probe.sh`
- `python -m py_compile scripts/generate_trace_biopt_weight_sensitivity_table.py scripts/generate_trace_biopt_certificate_removal_probe_table.py scripts/generate_trace_biopt_route_ablation_table.py scripts/generate_trace_biopt_posterior_risk_posture_table.py scripts/generate_trace_biopt_tail_risk_posture_table.py scripts/generate_trace_biopt_design_protocol_table.py scripts/audit_trace_biopt_claims.py`
- `python scripts/generate_trace_biopt_weight_sensitivity_table.py`
- `python scripts/generate_trace_biopt_certificate_removal_probe_table.py`
- `python scripts/generate_trace_biopt_posterior_risk_posture_table.py`
- `python scripts/generate_trace_biopt_tail_risk_posture_table.py`
- `python scripts/generate_trace_biopt_route_ablation_table.py`
- `python scripts/generate_trace_biopt_design_protocol_table.py`
- `python scripts/audit_trace_biopt_claims.py`
- `bash scripts/refresh_current_best_trace_biopt_paper_chain.sh`

## Result

The paper chain stayed fully green after the Seattle evidence-lane tightening:

- `paper-claim-audit`: `PASS` at `1285` checks
- `proof-checker`: `PASS`
- `citation-audit`: `PASS`
- `kill-argument`: `PASS`
- submission verifier: all four audits `OK / PASS / stale=false`
- `paper/main.pdf`: `44` pages

The substantive improvement is methodological, not only cosmetic:

- Seattle `20/30%` main-row promotion remains intact in the current-best chain.
- The Seattle mechanism tables no longer overstate those same-split diagnostics
  as if they were the promoted current-best route.
- The appendix and data/code availability wording now correctly state the final
  `8/9` Stage16-promoted evidence mix.
