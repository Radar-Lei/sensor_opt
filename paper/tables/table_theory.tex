\begin{table*}[t]
\centering
\caption{Scoped theoretical statements used in the TRACE-BiOpt manuscript. Each statement is intentionally bounded by its assumptions and non-claim boundary.}
\label{tab:theory}
\small
\begin{tabular}{>{\raggedright\arraybackslash}p{0.19\textwidth}>{\raggedright\arraybackslash}p{0.39\textwidth}>{\raggedright\arraybackslash}p{0.33\textwidth}}
\toprule
Statement & Scoped content & Non-claim boundary \\
\midrule
MAP closed form and stability & For a fixed layout, the regularized GLS/MAP lower-level problem is strongly convex and has the closed-form solution $A(\calS)^{-1}b_t(\calS)$ with a Lipschitz stability bound. & does not make the traffic data Gaussian or prove empirical MAE improvement by itself \\
posterior trace Bayes risk & Under the stated linear-Gaussian squared-error model, the posterior covariance trace over hidden components equals conditional expected squared reconstruction error. & does not convert posterior trace into an MAE guarantee for non-Gaussian traffic data \\
uniform layout generalization & For all size-$k$ layouts, bounded validation risk uniformly concentrates around deployment risk at rate $\sqrt{(k\log(e|\calV|/k)+\log(1/\delta))/n_v}$. & depends on independent blocks or effective sample size and an optimization-gap term \\
continuous-relaxation consistency & The capped-simplex relaxation and deterministic top-$k$ rounding produce a budget-feasible warm start whose accepted layout is still optimized under the same discrete TRACE-BiOpt objective $J(\calS)$. & does not create a second evidence-backed solver claim or guarantee that relaxed warm starts dominate the audited `auto` initializer \\
deterministic descent and finite termination & If every accepted exchange decreases $J$ by at least the declared tolerance, the accepted TRACE-BiOpt path is monotone and terminates in finitely many steps over the finite budget-feasible layout family. & does not imply polynomial-time global optimization or optimality beyond the searched neighborhood \\
exchange certificate & If TRACE-BiOpt stops because no searched one-exchange move improves $J$, the returned layout is stationary for the deterministic searched one-exchange active-set neighborhood up to tolerance. & does not assert global optimality outside the searched neighborhood \\
CVaR tail-risk epigraph & Scenario-CVaR admits the standard Rockafellar--Uryasev epigraph form, so the TRACE-BiOpt tail term is a coherent penalty on sampled upper-tail reconstruction risk rather than an ad hoc average-risk proxy. & does not certify robustness against all perturbations or untested traffic regimes \\
\bottomrule
\end{tabular}
\end{table*}
