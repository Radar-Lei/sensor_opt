Data availability

The TRACE-BiOpt code, experiment scripts, generated tables, and paper-facing
evidence artifacts are available in the project repository. Raw PeMS and
Seattle traffic datasets are not redistributed with the repository; the
repository documents the expected local data paths and provides the scripts
needed to regenerate the reported evidence when those datasets are available.
